import { createHash } from "node:crypto";
import { mkdir, mkdtemp, readFile, rm, writeFile } from "node:fs/promises";
import { tmpdir } from "node:os";
import { join, relative, resolve } from "node:path";
import { build } from "vite";

const root = resolve(import.meta.dir, "../..");
const output = resolve(process.argv[2] ?? `.data/app-dock-acceptance/${Date.now()}`);
const profile = await mkdtemp(join(tmpdir(), "agent-app-dock-"));
const sources = ["apps/desktop/src/renderer/SettingsSidebar.tsx", "apps/desktop/src/renderer/settings-sidebar.css", "apps/desktop/src/renderer/LocalEnvironmentSettings.tsx", "apps/desktop/src/renderer/local-environment-settings.css", "apps/desktop/src/renderer/local-environment-state.ts", "apps/desktop/src/window-state.ts", "apps/desktop/src/renderer/project-execution-mode.ts", "apps/desktop/src/renderer/drafts.ts", "apps/desktop/src/renderer/theme.css", "packages/shared/src/preferences.ts", "apps/desktop/src/renderer/ComposerContext.tsx", "apps/desktop/src/renderer/DraftSnapshot.tsx", "apps/desktop/src/renderer/Welcome.tsx", "apps/desktop/src/renderer/composer-context.css", "apps/desktop/src/renderer/App.tsx", "apps/desktop/src/renderer/DetachedQuestionCard.tsx", "apps/desktop/src/renderer/detached-questions.css", "apps/desktop/src/renderer/ComposerSelectionPopup.tsx", "apps/desktop/src/renderer/composer-selection-popup.css", "apps/desktop/src/renderer/styles.css", "apps/desktop/src/renderer/dock-layout.css", "apps/desktop/src/renderer/dock-panel.css", "apps/desktop/src/renderer/use-workbench-dock.tsx", "apps/desktop/src/renderer/DockPanel.tsx", "apps/desktop/src/renderer/DockTerminal.tsx","apps/desktop/src/renderer/NativeTerminalPanel.tsx","apps/desktop/src/renderer/terminal-panel.css","apps/desktop/src/renderer/native-terminal-panel.css","apps/desktop/src/renderer/EnvironmentCard.tsx", "apps/desktop/src/renderer/BrowserPanel.tsx", "apps/desktop/src/renderer/browser-address.ts", "apps/desktop/package.json", "bun.lock", "apps/desktop/src/renderer/browser-panel.css", "apps/desktop/src/renderer/Icons.tsx", "scripts/acceptance/app-dock-browser.tsx"];
const hashes = () => Promise.all(sources.map(async path => [path, createHash("sha256").update(await readFile(join(root, path))).digest("hex")])).then(Object.fromEntries);
await mkdir(output, { recursive: true, mode: 0o700 });
try {
  const before = await hashes();
  await writeFile(join(output, "index.html"), `<!doctype html><meta charset="utf-8"><meta http-equiv="Content-Security-Policy" content="default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data: blob:; connect-src 'none'; worker-src 'self' blob:"><div id="root"></div><script type="module" src="${relative(output, join(import.meta.dir, "app-dock-browser.tsx"))}"></script>`);
  await build({ configFile: join(root, "apps/desktop/vite.config.ts"), root: output, logLevel: "warn", worker: { format: "es" }, build: { outDir: join(output, "web"), emptyOutDir: true, rollupOptions: { input: join(output, "index.html") } } });
  await writeFile(join(output, "main.cjs"), `const{app,BrowserWindow}=require('electron'),fs=require('node:fs'),path=require('node:path');app.setPath('userData',${JSON.stringify(profile)});app.whenReady().then(async()=>{const win=new BrowserWindow({show:false,width:1440,height:1000,webPreferences:{sandbox:true,contextIsolation:true,nodeIntegration:false,backgroundThrottling:false}});try{await win.loadFile(path.join(__dirname,'web/index.html'));const result=await win.webContents.executeJavaScript('runAppDockAcceptance()');result.captures=[];for(const scene of [{name:'wide',width:1780,height:1111,zoom:1},{name:'terminal1440',width:1440,height:1000,zoom:1,terminal:true},{name:'narrow',width:760,height:1000,zoom:1},{name:'zoom150',width:1200,height:1000,zoom:1.5},{name:'short',width:760,height:506,zoom:1,chooser:true,terminal:true}]){if(scene.chooser)await win.webContents.executeJavaScript('prepareShortDockChooser()');win.setContentSize(scene.width,scene.height);win.webContents.setZoomFactor(scene.zoom);await new Promise(r=>setTimeout(r,100));const geometry=await win.webContents.executeJavaScript('appDockGeometry('+Boolean(scene.chooser)+','+Boolean(scene.terminal)+')');fs.writeFileSync(path.join(__dirname,scene.name+'.png'),(await win.webContents.capturePage()).toPNG());result.captures.push({...scene,...geometry})}win.setContentSize(1440,1000);win.webContents.setZoomFactor(1);const fresh=await win.webContents.executeJavaScript('prepareFreshComposer()');fs.writeFileSync(path.join(__dirname,'fresh-power.png'),(await win.webContents.capturePage()).toPNG());result.captures.push({name:'fresh-power',...fresh});await win.webContents.executeJavaScript('prepareComposerContext()');win.webContents.focus();await win.webContents.insertText('Dock');await new Promise(r=>setTimeout(r,80));fs.writeFileSync(path.join(__dirname,'composer-project-menu.png'),(await win.webContents.capturePage()).toPNG());await win.webContents.executeJavaScript('exerciseComposerProject()');await win.webContents.insertText('Keep context draft');const welcome=await win.webContents.executeJavaScript('exerciseWelcomeProjectHeading()');fs.writeFileSync(path.join(__dirname,'welcome-project-menu.png'),(await win.webContents.capturePage()).toPNG());result.captures.push({name:'welcome-project-menu',...welcome});await win.webContents.executeJavaScript('closeWelcomeProjectHeading()');const branchModal=await win.webContents.executeJavaScript('exerciseComposerBranches()');fs.writeFileSync(path.join(__dirname,'composer-create-branch-prefix.png'),(await win.webContents.capturePage()).toPNG());result.captures.push({name:'composer-create-branch-prefix',...branchModal});win.webContents.sendInputEvent({type:'keyDown',keyCode:'TAB',modifiers:['SHIFT']});win.webContents.sendInputEvent({type:'keyUp',keyCode:'TAB',modifiers:['SHIFT']});await win.webContents.executeJavaScript('checkBranchModalTrap()');win.webContents.sendInputEvent({type:'keyDown',keyCode:'ESCAPE'});win.webContents.sendInputEvent({type:'keyUp',keyCode:'ESCAPE'});await win.webContents.executeJavaScript('checkBranchModalEscape()');await win.webContents.executeJavaScript('reopenBranchModal()');const backdrop=await win.webContents.executeJavaScript('branchModalBackdropPoint()');win.webContents.sendInputEvent({type:'mouseMove',x:backdrop.x,y:backdrop.y});win.webContents.sendInputEvent({type:'mouseDown',x:backdrop.x,y:backdrop.y,button:'left',clickCount:1});win.webContents.sendInputEvent({type:'mouseUp',x:backdrop.x,y:backdrop.y,button:'left',clickCount:1});await win.webContents.executeJavaScript('checkBranchModalBackdrop()');await win.webContents.executeJavaScript('reopenBranchModal()');await win.webContents.insertText('new-context-branch');await new Promise(r=>setTimeout(r,80));const validBranch=await win.webContents.executeJavaScript('measureBranchDialog()');fs.writeFileSync(path.join(__dirname,'composer-create-branch.png'),(await win.webContents.capturePage()).toPNG());result.captures.push({name:'composer-create-branch',...validBranch});const context=await win.webContents.executeJavaScript('finishComposerContext()');fs.writeFileSync(path.join(__dirname,'composer-context.png'),(await win.webContents.capturePage()).toPNG());result.captures.push({name:'composer-context',...context});await win.webContents.executeJavaScript('checkDisposedComposerContext()');await win.webContents.executeJavaScript('checkCompletedCheckoutContext()');const worktree=await win.webContents.executeJavaScript('beginWorktreeSubmission()');win.webContents.sendInputEvent({type:'mouseDown',x:Math.round(worktree.prompt.x),y:Math.round(worktree.prompt.y),button:'left',clickCount:1});win.webContents.sendInputEvent({type:'mouseUp',x:Math.round(worktree.prompt.x),y:Math.round(worktree.prompt.y),button:'left',clickCount:1});await win.webContents.executeJavaScript('checkWorktreePromptFocus()');await win.webContents.insertText(' newer edit');await win.webContents.executeJavaScript('finishWorktreeSubmission()');result.checks=(await win.webContents.executeJavaScript('appDockProgress()')).checks;await win.loadFile(path.join(__dirname,'web/index.html'),{query:{question:'1'}});win.webContents.focus();for(const focused of [false,true]){const question=await win.webContents.executeJavaScript('measureQuestionComposer('+focused+')');const name=focused?'question-composer-focused':'question-composer-collapsed';fs.writeFileSync(path.join(__dirname,name+'.png'),(await win.webContents.capturePage()).toPNG());result.captures.push({name,...question})}await win.webContents.insertText('Keep this ordinary draft');const edited=await win.webContents.executeJavaScript('measureQuestionComposer(false,true)');fs.writeFileSync(path.join(__dirname,'question-composer-edited.png'),(await win.webContents.capturePage()).toPNG());result.captures.push({name:'question-composer-edited',...edited});result.hidden=true;result.electron=process.versions.electron;result.passed&&=result.captures.every(scene=>scene.fitting);fs.writeFileSync(path.join(__dirname,'result.json'),JSON.stringify(result,null,2));win.destroy();app.exit(result.passed?0:1)}catch(error){const progress=await win.webContents.executeJavaScript('appDockProgress()').catch(()=>null);fs.writeFileSync(path.join(__dirname,'failure.png'),(await win.webContents.capturePage()).toPNG());fs.writeFileSync(path.join(__dirname,'result.json'),JSON.stringify({passed:false,error:String(error),progress},null,2));app.exit(1)}});`);
  const child = Bun.spawn([process.execPath, join(root, "node_modules/electron/cli.js"), join(output, "main.cjs")], { stdout: Bun.file(join(output, "electron.log")), stderr: Bun.file(join(output, "electron-errors.log")) });
  const timer = setTimeout(() => child.kill("SIGTERM"), 60_000); const code = await child.exited; clearTimeout(timer);
  const result = await Bun.file(join(output, "result.json")).json(); result.sourceAtBuild = before; result.sourceAfter = await hashes(); result.sourceHashesStable = JSON.stringify(result.sourceAtBuild) === JSON.stringify(result.sourceAfter); result.passed &&= result.sourceHashesStable;
  result.scope = "Actual production App, dock panels, workspace/review surfaces, and Environment card in hidden sandboxed Electron. Host, Git, activity, and native transport responses are controlled; no provider or installed service is used.";
  await writeFile(join(output, "result.json"), JSON.stringify(result, null, 2));
  console.log(JSON.stringify({ passed: result.passed, checks: result.checks?.length, captures: result.captures?.length, sourceHashesStable: result.sourceHashesStable, result: join(output, "result.json") }));
  if (code || !result.passed) throw new Error(`App dock acceptance failed; inspect ${output}/result.json`);
} finally { await rm(profile, { recursive: true, force: true }); }
