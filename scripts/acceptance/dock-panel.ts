import { mkdir, mkdtemp, rm, writeFile } from "node:fs/promises";
import { tmpdir } from "node:os";
import { join, resolve, relative } from "node:path";
import { build } from "vite";
const root = resolve(import.meta.dir, "../.."), output = resolve(process.argv[2] ?? `.data/dock-panel-acceptance/${Date.now()}`), profile = await mkdtemp(join(tmpdir(), "agent-dock-panel-")); await mkdir(output, { recursive: true, mode: 0o700 });
try { await writeFile(join(output, "index.html"), `<!doctype html><style>html,body,#root,.fixture{margin:0;width:100%;height:100%}.fixture>.dock-panel{width:100%;height:100%}</style><div id="root"></div><script type="module" src="${relative(output, join(import.meta.dir, "dock-panel-browser.tsx"))}"></script>`); await build({ configFile: join(root, "apps/desktop/vite.config.ts"), root: output, logLevel: "warn", build: { outDir: join(output, "web"), emptyOutDir: true, rollupOptions: { input: join(output, "index.html") } } }); await writeFile(join(output, "main.cjs"), `const{app,BrowserWindow}=require('electron'),fs=require('node:fs'),path=require('node:path');app.setPath('userData',${JSON.stringify(profile)});app.whenReady().then(async()=>{const win=new BrowserWindow({show:false,width:1200,height:800,webPreferences:{sandbox:true,contextIsolation:true,nodeIntegration:false}});try{await win.loadFile(path.join(__dirname,'web/index.html'));const result=await win.webContents.executeJavaScript('runDockPanelAcceptance()');result.geometry=await win.webContents.executeJavaScript('dockPanelGeometry()');result.hidden=true;result.passed&&=result.geometry.fitting;fs.writeFileSync(path.join(__dirname,'result.json'),JSON.stringify(result,null,2));win.destroy();app.exit(result.passed?0:1)}catch(error){fs.writeFileSync(path.join(__dirname,'result.json'),JSON.stringify({passed:false,error:String(error)},null,2));app.exit(1)}});`); const child=Bun.spawn([process.execPath,join(root,"node_modules/electron/cli.js"),join(output,"main.cjs")],{stdout:Bun.file(join(output,"electron.log")),stderr:Bun.file(join(output,"electron-errors.log"))});const timer=setTimeout(()=>child.kill("SIGTERM"),60_000);const code=await child.exited;clearTimeout(timer);const result=await Bun.file(join(output,"result.json")).json();result.scope="Actual DockPanel in hidden sandboxed Electron with controlled local state; no App, host, native terminal, or provider action.";await writeFile(join(output,"result.json"),JSON.stringify(result,null,2));console.log(JSON.stringify({passed:result.passed,checks:result.checks?.length,result:join(output,"result.json")}));if(code||!result.passed)throw new Error(`Dock panel acceptance failed; inspect ${output}/result.json`);}finally{await rm(profile,{recursive:true,force:true});}
