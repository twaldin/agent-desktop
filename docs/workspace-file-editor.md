# Workspace file editor

## Composer file activation

Clicking an existing inline file now opens its owner-workspace file panel without changing the draft or sending a prompt. The node uses its current PM identity and the latest composer callback; foreign-host and outside-workspace paths are not remapped. This matches the pinned composer's ordinary pointer callback. Separate keyboard focus, native tooltip and hover-preview behavior belong to other reference surfaces and are not added here.

16 focused tests/109 assertions, app typecheck/build and fixture-inclusive typecheck pass. Seven isolated renderer checks/nine PNG+Chromium AX captures include real pointer activation, loaded file content, unchanged host draft and zero sessions. These are component checks, not installed or registered native-pixel acceptance. Projectless/outside-workspace opening, clipboard restoration and the remaining rich-editor requirements stay open. Private evidence: `.data/composer-file-open-2026-09-07/REPORT.md`.

## Sent inline-file links

Inline files now serialize into the actual ordinary OMP user text at their authored UTF-16 positions. The surrounding text and whitespace remain intact. This closes the earlier semantic gap where OMP received a native file snapshot but the user text omitted the inline reference. Unpositioned legacy attachments keep their earlier context-only behavior.

The binding-v2 format stores the original authored text, attachment descriptors and exact native entry identities. History verifies the serialized text, recorded file paths, ordering and uniqueness before attaching provenance and folding successful bound file rows into their user message. Older bindings, ambiguous/malformed records and native read failures stay visible. Worker protocol33 rejects older workers; public v8 and SQLite schema10 remain unchanged.

The reference uses a custom Markdown link grammar. This bridge uses CommonMark escaping and encoded path segments because OMP independently scans text for `@` mentions: generated filename labels must not trigger an additional read. The existing production Markdown renderer displays the original filename and opens its literal owner-workspace path. This wire-encoding difference is intentional; it does not establish native pixel parity.

60 focused tests/523 assertions, typecheck and build pass. Validation includes the real pinned OMP worker with a controlled local provider, exact provider-visible text and file snapshot, authenticated command replay/restart, binding corruption cases, CommonMark decoding, and production renderer markup. There are no external provider calls or Work fixture changes. Installed/native appearance, clipboard restoration, file hover/open in the composer, richer selected-text nodes and whole-file steering/command combinations remain open. Private results: `.data/sent-inline-files-2026-09-07/REPORT.md`.

## Inline composer nodes

The main composer replaces the interim separate-row file chips with real ProseMirror inline, atomic, nonselectable nodes. Each whole-file reference may carry `textOffset`, a UTF-16 position in authored draft text; file nodes consume no authored characters. Tied offsets retain array order. Edits preserve positions through editor transactions. A menu insertion is a separate undo step, and completion replacements edit their bounded range. Semantically changed external drafts reset undo history; equal echoes and autocomplete menu renders preserve it.

The pinned OMP completer returns its original insertion text plus an optional absolute regular-file path verified on the owner host. The main @ picker uses that metadata for the same file node as tree Add to chat. Directories and native-resource completions stay on their native text route. Cross-host remapping is not inferred. OMP still reads files through native fileMention admission at Send; inline metadata changes presentation/persistence, not executable ownership or credentials.

Position-bearing commands use v8 and promote storage to schema10. Bounds are validated against authored text before draft writes or command claims. Old endpoints cannot strip positions from an existing draft or consume it through a lower-version request. Packages declare schemas1–10; downgrade to a schema9 reader is refused before stopping the service.

Combined hidden renderer acceptance uses actual context-menu, arrow, Backspace, undo/redo and Enter input against the production editor/autocomplete/controller and an authenticated disposable host. Six checks/eight original PNG+Chromium-AX pairs cover caret insertion, independent file deletion, emoji offset mapping, offline reload and a real native OMP @ result. The capture inspection also corrected a Tailwind reset that made ProseMirror's trailing caret separator a block. See private `.data/inline-files-2026-09-07/REPORT.md` for failed attempts and exact evidence.

This supersedes the main composer's separate-line rendering described in the historical checkpoint below. It does not certify installed pixels or full-window density. Clipboard Markdown/HTML restoration, hover/open, sent inline placement, rich selected-text nodes, command/skill combinations and whole-file steering remain open.

## Whole-file references from the tree

Add to chat now records `{id, source: {kind: "file", hostId, path}}` in `wholeFileAttachments`. References have canonical absolute literal paths, distinct identities/sources and a 100-file transport bound. They name file intent, not captured bytes. Only files on the draft's owning host can be submitted; a same-host file may belong to another directory. The UI does not offer a cross-host path remap. Duplicate staging retains one reference. The draft remains editable offline, and CAS conflicts preserve both versions.

Command v7 and schema9 keep the format sticky after removing the final file; worker32 rejects older runtimes. Submission replay retains original identity/path/text. A verified ordinary native user receipt atomically consumes only its captured draft revision. Unsupported older hosts, steer, slash/skill input and wrong-owner paths are rejected without consuming the draft.

`NativeWholeFilePrompt` calls the pinned generator with native image resizing, file display mode and edit snapshot store. Every selected path must appear in generated native records before any attempt marker is persisted; missing/unreadable files cannot silently disappear from a successful submission. Native binary/tooLarge records count as explicit context. A flushed attempt marker precedes injection of actual fileMention messages; binding metadata names the exact native user and file entries. The model receives native context, never a custom prose surrogate. Lost binding/worker replies remain uncertain and never replay automatically.

Evidence is separated: authenticated HTTP + real pinned-worker/local-provider tests prove delivery, exact-once consumption, files-only and mixed context, reopen and lost replies. Hidden renderer checks use actual pointer input on the fallback tree menu and production draft/controller/chip code against an authenticated temporary host; four checks/five original PNG+Chromium-AX captures prove staging, duplicate handling, offline reload and removal only. No installed main/preload, native menu popup, external provider or Work run was exercised.

Reference anchors: frozen7868 `04-panels/12-file-context-menu` and `02-composer/15-file-mention-chip` (P), with source7982 whole-file descriptor trace. These captures were inspected but are not registered to the isolated fixture. Current separate-line removable chips are an interim rendering; the native inline editor node, insertion/caret/delete behavior, @ picker wiring, hover/open behavior, sent inline placement and full visual parity remain required. Tokens inherit existing accent/secondary/focus settings. See private `.data/whole-file-2026-09-07/REPORT.md`.

## File-tree context actions

Right-click or Shift-F10 on a supported file targets that row, without opening it or substituting the selected editor path. Open in/Open with use the owning host's catalog and durable mutation queue. Save as reuses the existing validated byte-copy engine and local destination chooser; Copy path uses the owning workspace's absolute path. Folder actions are not invented where the pinned target extractor accepts files only.

The pinned default is an Electron native menu. A trusted preload/main bridge now accepts bounded display-only menu descriptors, maps selection IDs back to renderer actions and scopes disposal/replacement to the window. The renderer retains HTML fallback and visible errors. Native application icons, warm discovery timing, keyboard popup placement and visible native interaction remain unverified.

Evidence: `.data/tree-menu-2026-09-07-r4/result.json` passes11 checks/nine PNG+Chromium-AX captures against an authenticated disposable host. Nonselected second.ts is copied/opened while first.ts remains the editor. A real copy through the production engine matches bytes; the controlled OS adapter receives the exact second.ts path. The broader toolbar suite retains five injected launches, one autosave write, one copy, zero sessions and the original revision1 draft; uncertain launch recovery does not replay.34 tests/138 assertions, strict fixture checking, typecheck/build and independent Luna reviews pass. Actual Electron Menu construction and callback identity are tested separately, without showing a popup.

Limitations: the fixture exercises HTML fallback and controlled launch/dialog boundaries, not installed native popup, physical Work input or matched pixels. Failed harness runs remain in `.data/tree-menu-2026-09-07/REPORT.md`; one failed attempt changed clipboard text and did not recover the original, while the corrected run restores its own starting text. Work runs and sealed bundles were untouched.

The later whole-file checkpoint below wires Add to chat to durable descriptors and native OMP admission. Editable inline mention nodes and matched native presentation remain incomplete; selected-text snapshots and literal @path text remain separate paths.


## Workspace file-tree glyphs

Tree rows now use the pinned complete, colored sprite system: 53 file glyphs and one chevron for a folder. The native exact-name map precedes compound suffixes, with complete-set overrides for JSX/TSX and Sass. Ordinary file-tab glyphs retain their distinct resolver. Each Next.js gradient has a local ID so repeated rows cannot reference another instance.

Rows use the native review wrapper's 28px height, 13px type, 6px horizontal padding and 10px gap. Indentation is 17.5px per depth at default geometry, with native muted guide color and hover opacity. All 78 new tree palette, per-glyph and geometry tokens pass through the shared theme file/advanced editor validation; no new dependency or preference store is introduced.

Validation: 26 tests/136 assertions, production typecheck/build and strict acceptance-fixture checking pass. An isolated renderer compares all 53 glyphs and both chevron states at 16/24px in light/dark: 220 canonical-vector/raster comparisons have zero differing channels. Independent extracted CSS produces the same computed icon colors; validated custom blue/React overrides propagate. Differential execution of the pinned pure resolver agrees on 1,263 of 1,267 cases; the four inherited-object names return a generic glyph instead of a prototype object/function. Native uppercase AUTHORS/CHANGELOG/CONTRIBUTORS/LICENSE extension-map quirks are retained.

Private evidence: `.data/tree-icons-2026-09-07/REPORT.md` and `.data/tree-icons-2026-09-07-r3/result.json`. The production preview/tree regression passes seven checks/eight PNG+Chromium-AX captures in `.data/file-preview-tabs-2026-09-07-r10/`, including actual click pairs, replacement/promotion, editor input, restoration and measured row geometry. Source hashes stay stable. Earlier failed vector extraction attempts remain recorded.

This supports static asset equivalence and component integration. Installed whole-App routing, native Work pointer behavior and matched frozen-window pixels remain unverified. Filter focus, row context menus, Git decorations and wider tree interactions still need their own parity checks. No Work app, host, profile or sealed evidence was changed.


## File-tab icon resolver and glyphs

Ordinary file tabs use the pinned7982 `HV` resolver: case-insensitive exact basename (`skill.md`), then extension, then MIME-prefix fallback, then generic file. Paths remain literal; query/hash characters are not stripped. A trailing slash selects a folder. The fallback extension catalog is derived from the pinned MIME database with its original source-precedence rules, without adding a runtime dependency. Own-property lookup corrects the native resolver’s inherited-object results for `__proto__` and `constructor`; neither filename can select an arbitrary property as a component.

The28 inline SVG choices retain their source geometry, fills, strokes and office gradients. Per-instance React IDs prevent repeated gradient collisions. Monochrome glyphs inherit their tab color. Source provenance is app-initial SHA256 `fb72076ee44f6596f8dafa9a3effe37a3527a87db21fde8b4042233957b554bb`, `HV` around4896241; ordinary file route getIcon callsites around5883686/5896167. This resolver is not the tree’s sprite/token system. Inline `zkt` renders caller-supplied children, so its control receives no unsolicited glyph. Review summary icons and native-skill routes are not changed by this ordinary-file integration.

The private extractor reads static SVG descriptors from the pinned bundle. A separate isolated execution of its three pure resolver functions supplies4,782 cases:4,780 agree, with the two intentional prototype-property corrections above.39 tests/218 assertions cover basename/MIME precedence, literal paths, unique gradient identities, preview state and disk persistence. The unchanged preview-tab interaction sequence passes six checks/eight captures with these glyphs (`.data/file-preview-tabs-2026-09-07-r8/`).

`scripts/acceptance/file-icons.ts OUTPUT EXTRACTED_JSON` compares all28 production glyphs to those static source descriptors at16px and24px, in light and dark conditions. Canonical SVG attributes/children agree after normalizing local IDs and decorative wrapper attributes.112 same-parent sequential raster comparisons have zero differing channels; two full renderer PNG+Chromium-AX pairs record the gallery and real DockPanel header. Initial side-by-side CSS24 samples differed in9 channels; moving the reference to a fixed layer introduced further small differences. Both earlier runs remain recorded. The accepted run places each reference in the same container as its candidate before capture, preserving exact geometry and requiring zero differences rather than loosening tolerance.

Accepted evidence: `.data/filetype-icons-2026-09-07-r3/`, resolver/source evidence and qualifications in `.data/filetype-icons-2026-09-07/REPORT.md`. This is same-engine static asset equivalence, not a native Codex window comparison. Source hashes are stable; zero non-file network requests or runtime errors were observed. Strict fixture checking, production typecheck/build and independent review pass. Full-App/installed rendering, tree sprite selection/colors, review-row icons, icon interactions across themes and matched whole-window parity remain open.

## Replaceable file preview tabs

Each right/bottom pane has one replaceable ordinary-file preview, across workspace owners. A new clean preview appends after removing the old preview; existing permanent identities never become previews. Double-clicking the tab, transcript reference or tree file promotes it. The content wrapper pins before pointer/key interaction, with tree and breadcrumb navigation explicitly exempt. Edits and recovered/conflicted buffers also pin, and replacement never saves or discards a protected buffer. Moving a preview retains its status and protects or replaces the destination preview according to the same buffer checks.

Pinned7982 source supports this behavior: the generic panel controller `m8n`, content wrapper `oWa` with `data-tab-preview-pin-exempt`, durable selector `J6n`, and the file-tree/breadcrumb open options. Preview labels use italic text without an extra color or opacity. The existing app choice to retain pane visibility when filtering previews is preserved; exact native restoration visibility is not independently established. Skill-file preview lifetime and cross-window transfer are not covered by this ordinary-file change.

The production window serializer omits preview descriptors and their tab IDs; the parser rejects unfiltered live snapshots. An actual temporary WindowStateStore save/reopen verifies omission and permanent route preservation.82 focused/regression tests pass477 assertions, including owner-bound file/Git operations, autosave, conflicts, offline recovery, unresolved receipts and close preservation.

`scripts/acceptance/file-preview-tabs.ts OUTPUT` runs production hook, dock, reference and workspace components in hidden Electron with a typed controlled workspace transport. Six checks/eight original renderer PNG+Chromium-AX pairs pass: clean replacement, real two-click tab/reference/tree promotion, content promotion before editor input, and actual React unmount/remount from parsed persisted state while a transient preview is present. Four permanent tabs and edited cached text survive; the transient tab does not. This is React restoration, not a desktop restart. Source hashes stay stable, and observed non-file network requests/runtime errors are empty. Typecheck, separate strict fixture checking and build pass.

Accepted evidence: `.data/file-preview-tabs-2026-09-07-r7/`; scope and superseded-run reasons: `.data/file-preview-tabs-2026-09-07/REPORT.md`. Candidate viewport is1100×760, DPR2, zoom1, dark; this component fixture has no matching frozen state and no native window capture. Whole-App/main/preload, physical Work input, graphical cross-pane movement, full conditional file actions, filetype icons and matched-pixel parity remain unverified. No Work fixture, installation or sealed reference was changed.

## File-reference opening interactions

The same FileReferenceControl now renders native literal-path references and parsed Markdown file links. Parsing remains separate: native punctuation is never interpreted as a URL/location, while Markdown line/column/endLine values reach the existing dock request unchanged. Native button Enter and Space activation, the Apple Command modifier (Control elsewhere), and middle click follow the pinned reference. Hover/focus discovery is read-only and cached briefly; selection revalidates targets on the owning host. Owner/connection changes remount the menu to discard late discovery replies.

The context menu provides Open file, the available preferred application, a nested Open with menu, optional Save as, Copy path, and the actual owner’s file manager. Save as reuses owner-host byte retrieval and the existing client save dialog. External opening uses WorkspaceState’s durable mutation queue without requiring an already-open dock. Server rejection or an unresolved receipt is surfaced; another click cannot replay an uncertain command. External editor launches currently cannot carry line locations, so those actions are disabled/refused. Explicit reveal intentionally targets the file without claiming cursor placement.

Source evidence is pinned7982 `zkt`/`FR` and initial `yBi`: focus/hover discovery, platform modifiers, middle click, Enter and Space, preview single/double-click distinction, and conditional target/copy/reveal menus. The frozen7868 `03-conversations/11-final-response` and `02-composer/25-native-file-attached` show file references; `02-composer/15-file-mention-chip` is an unsent mention, not this native context row. No matched frozen capture of this new OMP context menu establishes pixel parity. The later preview-tab checkpoint covers ordinary-file replacement and promotion. Filetype glyphs, conditional HTML browser actions and the entire native target preference UI remain open.

Validation: 28 focused tests/163 assertions pass across the shared link renderer, native literal resolution, real WorkspaceState with controlled transport, and host opening with controlled launch adapters. Maintained hidden-Electron interaction acceptance consumes the recorded native generator/journal/worker history from the preceding checkpoint; its file-open, target and Save-as callbacks are explicitly controlled boundaries. No real external application, native save dialog, full App/main/preload, Work installation or macOS pixel claim follows from those checks. Production Markdown/scroll regression also passes, including exact file callback location and unchanged link/anchor errors. Ten maintained renderer checks/seven PNG+Chromium-AX pairs pass in `.data/file-mentions-ui-2026-09-07-r7/`, including pointer submenu selection, discovery retry, keyboard/middle/Command activation, controlled Save-as dispatch and action rejection without replay. Root and the independent reviewer inspected the new menu/error captures. The initial r4 discovered a hover/click toggle race; it was fixed and the identical pointer path passed. r5 used the superseded reveal label; r6 supplied interaction evidence before the four new menu/error captures were added in r7. Copy-path clipboard success, an actual OS Save-as dialog, real external launch and matched reference pixels are not established here. Typecheck, separate strict fixture checking and production build pass. Private logs are under `.data/file-reference-actions-2026-09-08/`. Prior runs and sealed reference bundles remain unchanged.


The Files panel uses the pinned Pierre editor over the existing host-owned workspace service. `WorkspaceState` remains the authority for file content, revisions, dirty buffers, recoverable conflicts and mutation receipts. The renderer does not write through a second editor-specific backend.

## Native auto-read references

The pinned OMP `FileMentionMessage` has `files[]` fields for path, consumed content, line count, byte size, skipped reason and optional image. It carries its own native identity and no originating user-entry ID. The transcript now projects this data as separate expandable reference rows. It does not infer an association from adjacency or repeated prompt text. The snapshot shows exactly what OMP retained, including native hashline/directory output; it does not reread a changed source file to reconstruct old context.

Projection copies and validates the complete file list, preserves ordering, and removes provider extras and image bytes from display metadata. Pending event snapshots preserve image hashes without retaining byte payloads. Missing/unsupported image data stays visibly unavailable. Retrieval uses the owning native journal entry and original `files[index].image`; non-image or invalid indexes reject. Sources includes native auto-read context and excludes skipped reads or unavailable image references.

File-reference labels wrap and use the existing session `openFile` action. Paths containing spaces, percent signs, hashes, colons and question marks stay literal filename characters. Unowned/outside-workspace references have readable labels without an open action; the host remains responsible for its realpath boundary. Open errors remain visible. The full literal path supplies the tooltip.

Pinned7982 source distinguishes the composer `atMention` node (`label/path/fsPath`) from generic rendered file references (`zkt`/`Jkt` in app-primary). Frozen `02-composer/15-file-mention-chip` and `25-native-file-attached` primarily show unsent surfaces; the latter also has inline file buttons in tool-produced completion text. Neither establishes a sent OMP context-to-user association. The current reference row is a bridge for OMP's separate context record, not full visual parity. Filetype icon selection, reference context menus, modified/middle clicks, preview-target semantics and native inline user mentions remain incomplete.

The maintained native test `apps/host/src/omp/file-mentions-native.test.ts` calls the actual pinned generator against disposable text/empty/directory/binary/PNG files, persists its output with SessionManager, and reopens through the real isolated worker. It checks paths/content/order, image index/hash and retrieval rejection, then repeats reopen. No model/provider calls occur.36 focused/regression tests pass420 assertions, including pending-to-durable identity, source provenance, literal paths, existing image HTTP retrieval and selected-text admission/restart regressions. Native evidence is `.data/file-mentions-2026-09-07/native-evidence-2/`; earlier evidence remains distinct.

The input-driven `scripts/acceptance/file-mentions-ui.ts OUTPUT NATIVE_MESSAGES_JSON [CONTROLLED_CWD]` renders recorded native history through production components. The file-open handler is deliberately a controlled callback, not a host-open acceptance proof. Image display, outside-workspace UI (absent from this fixture), native macOS/main/App and exact pixel parity are not credited by this run. Four renderer checks/three original PNG+Chromium-AX pairs pass in `.data/file-mentions-ui-2026-09-07-r2/`, including exact snapshot expansion, literal-path activation and a controlled open failure. Viewport, zoom, DPR, computed font/color and zero observed non-file network requests are recorded. r1 is earlier evidence from the first native-record wrapper; r2 uses the maintained native array. Typecheck, separate fixture TSX checking and production build pass. Independent integration review found no actionable correctness defect. Existing Work installations/runs and sealed bundles remain untouched.

## Selected-text history

A sent selection group uses the same chip and traversable preview as the composer, without Remove or a source-opening action. The chip sits above its authored message. An excerpt-only ordinary prompt displays the chip without an empty bubble. The pinned7982 user renderer imports app-primary `yo` (`bdr`), the same draft group, without an `onRemove` callback. Its attachment row uses reverse order, end alignment, 8px gap and 1px padding. No matched frozen screenshot exists for this sent-selection state, so this source trace does not establish pixel parity.

After OMP appends the attributable ordinary user entry, the host writes a non-model `agent-desktop.selected-text-binding` metadata entry with version, submission ID, context-entry ID and user-entry ID. Admission waits for that binding to flush. Missing or altered binding writes leave an uncertain outcome and retain the draft. Projection requires one matching context, one unambiguous binding and the correct branch order; it never joins records by nearby position, equal text or timestamps. Valid unlinked snapshots render as standalone saved-context chips with “Saved context · prompt not linked”; malformed or unknown-version context retains its ordinary native custom presentation. The native journal itself is unchanged by projection.

The maintained `scripts/acceptance/selected-text-history-native.ts OUTPUT` executes one real pinned-worker/local-provider prompt containing both a captured remote snapshot and an ordinary `@file` reference, records native/normalized history, and reopens the same session. It proves actual native file-mention conversion composes with the snapshot without reading its missing source path. This does not establish native skill/slash/steer or rewriting-extension combinations. Worker protocol31 introduces the binding admission requirement; public command6/schema8 remain unchanged.

Authenticated host tests also verify the exact bound user projection before and after restart, successful atomic draft consumption and lost-receipt retention without replay. Focused native, HTTP, lifecycle/image, projection and renderer tests pass62 tests/435 assertions. The initial bare-path Bun test invocation failed before worker readiness; both explicit `./` paths and the maintained `bun run test` wrapper pass the same test. No product workaround or skipped native test was introduced. Private native/test evidence is `.data/selected-text-history-2026-09-07/`.

The maintained `scripts/acceptance/selected-text-history-ui.ts OUTPUT NATIVE_MESSAGES_JSON` passes five renderer checks/four original PNG+Chromium-AX captures for the read-only sent chip, actual preview text, Escape, reload and narrow light collision bounds. It consumes the recorded native history without changing or hiding messages and records viewport, zoom, DPR and computed fonts/colors. Its observed non-file network request list is empty. Accepted evidence is `.data/selected-text-history-ui-2026-09-07-r2/`; r1 is superseded because its fixture altered production layout and hid native context rows. Typecheck, the separate fixture TSX check and production build pass; independent backend review found no attribution or snapshot-loss defect.

These renderer checks are separate from actual worker/HTTP acceptance. The later native-auto-read checkpoint above replaces the generic fileMention fallback; full reference-attachment parity remains incomplete. Installed App/main/preload, native macOS captures, graphical cross-host behavior and matched-pixel parity remain unverified.

## Editor selection and grouped composer chip

Editable Pierre source and editable/read-only rich Markdown expose a floating **Add to chat** action. Native editor coordinates determine the excerpt; DOM ranges supply geometry only. The action verifies the current document, raw buffer and range before adding. Hidden/archived routes cannot act. A selected excerpt does not save the file or send a model prompt. Workspace paths retain their tab's host and cwd; native skill files retain their actual source path.

The composer uses one pluralized chip for all file excerpts, with a traversable hover preview and group removal. Each repeated selection has its own identity. The draft keeps authored text, immutable raw snippets and an explicit empty list after removal. App send gating explains missing host support, native command/skill combinations, limits and active-response restrictions rather than silently dropping excerpts. Conflict details also show saved selected text.

Pinned7982 source chain: file editor `Tr` imports app-primary `Wo` (`_lr`), whose actions use `HZt`/`HH`/`FC`; Add is text-only. Grouping comes from app-primary `bdr`/`MNr`/`w3`; its icon import `$_ as R` resolves to app-initial `PKo`. The frozen bundle has no matched capture for this precise chip. The adjacent `04-panels/18-editor-find` accessibility capture records Add to chat/Edit but does not certify this candidate's pixels.

The maintained `scripts/acceptance/selected-text-ui.ts` runs production editors, chip and DraftController in hidden Electron with real keyboard/pointer input and an authenticated isolated host. Seven checks/seven PNG+Chromium-AX captures cover source and rich CRLF capture, later unsaved edit preservation/undo, reload, read-only Markdown, group removal and 390×844 light-theme placement. Source hashes remain stable during the accepted run. No sessions or prompts are created; only the disposable draft changes, and both fixture files remain byte-identical.52 focused tests/317 assertions, typecheck and build pass; independent integration review found no actionable defect.

Evidence: `.data/selected-text-ui-2026-09-07/run8/`. These are renderer composition/host-draft checks, not installed App routing, native macOS accessibility, cross-device UI or matched-pixel proof. Read-only Pierre still lacks the selection action. This earlier editor checkpoint left transcript, native skill/reference/steer composition, graphical conflict/offline behavior and installed acceptance open; the later history checkpoint above covers projection and ordinary @file composition.

## Selected-text native admission foundation

The shared/native foundation now preserves independently identified raw excerpts, remote source provenance and one-based UTF-16 ranges without reading the named file. Identical file excerpts remain separate ordered selections. A 400,000-character aggregate serialized snapshot bound is an app transport limit, not an upstream capability claim.

An idle ordinary prompt can carry this snapshot through worker protocol30 into pinned OMP18.1.10. The adapter writes one attributed native custom context record, verifies its exact content and metadata, flushes it, then invokes the ordinary prompt with its authored text. Only that prompt's actual native user-message object can produce acceptance; a custom entry or unrelated extension message cannot consume a draft. Missing/altered/duplicate context, disk failure and errors after an append attempt remain outcome-unknown. Native submission identity is checked both before startup and at append. Older workers are rejected rather than silently dropping context.

The real isolated WorkerRuntime/local-provider check exercises native context conversion, persistence, reopen and an excerpt-only empty authored prompt. Manager fault-injection tests separately cover admission failure boundaries. This is source/native-core evidence, not HTTP, desktop, installed or pixel acceptance. Public draft storage and command/controller transport are connected as described below. The visible editor/chip pass below covers selection actions and grouping/preview/removal. The later history checkpoint above covers transcript projection and ordinary @file composition. Graphical remote/offline checks and selected-context composition with skills, slash commands, steering and rewriting extensions remain incomplete.

## Selected-text draft and submission transport

`selectedTextAttachments` is now a sticky Draft field. Host schema8 prevents older services reopening selected-aware state, while `/v6/commands` prevents older endpoints from normalizing away snapshots. These versions serve separate storage and wire contracts. Clearing the last excerpt preserves `[]`; old writers omitting it are refused. Remote source provenance remains descriptive and never requires a file read on the receiving host.

DraftController captures and caches nested snapshots, compares them during conflicts, retains the format through conflict resolution and correlates consumption with the original command ID/revision. SubmissionController persists the exact snapshot and envelope before dispatch; restored retries cannot replace them with later edits or attach them to a new command. Missing ordinary native user admission stays uncertain. Main-process routing never falls back from v6 to an older endpoint. Active selected snapshots also hold automatic goal/question delivery, like other unsent content.

Actual authenticated HTTP tests exercise the pinned worker/local-provider path, native raw history, successful atomic consumption, conflicting clients, older endpoint refusal and a deliberately lost native acceptance reply followed by host restart and exact-ID retry. A subsequent test connects the production renderer draft/submission controllers and main endpoint selector to the same real host: save/send both use v6, one native context is appended, and the cleared selection format survives cache reload. These are controller/HTTP/SDK tests, not graphical desktops or hosted-provider proof. The private worker fixture clears its inherited environment before deferred SDK import, uses a disposable HOME/agent directory, and denies outbound fetch.

Validation:94 focused regression tests/648 assertions; three HTTP integration tests/59 assertions after adding the combined controller route; typecheck/build pass. Evidence: `.data/selected-text-public-2026-09-07/`. The first in-progress renderer test used the wrong consumed revision and is retained as a fixture failure; corrected tests check the actual subsequent save against revision2 with the newer excerpt. No installation, native UI handoff, sealed reference change or pixel credit belongs to this checkpoint.

## Editing and navigation

Each dedicated file tab is identified by host, workspace and relative path. Opening the same file again activates that tab in its existing dock; different paths and owners remain separate. Transcript links, Review file actions and directory choices use this route. The descriptor survives window-state restoration.

Dedicated files show a compact breadcrumb header and a host-owned recursive file-tree picker above one editor, without the directory browser or nested file-tab strip. Breadcrumb selection opens or reactivates a dock tab; it cannot change another tab’s file identity. The generic Files directory view remains available for navigation and legacy restoration.

Each open text file has a stable editor instance while its panel remains mounted. Switching files or temporarily viewing Changes preserves its undo history and selection. Hiding a dock keeps the editor mounted but inactive. Closing or moving a tab between dock regions remounts the editor; cached text survives, but native undo history and selection are not yet durable across those remounts. A line request waits for the owning file read and visible editor, then selects the requested one-based line or places the caret at its one-based UTF-16 column. Invalid locations show an error against the current buffer, including unsaved edits. LF, CRLF and CR line boundaries are supported.

Keyboard editing and find come from Pierre. UI edits autosave after three seconds of idle; Command/Control-S can save immediately through the same receipt path. The editor remains writable offline and during an in-flight save; the save callback respects connection, restoration and pending-command gates. Editor controls own their keyboard events before app shortcuts.

Command-L on macOS, or Control-L elsewhere, opens Go to line from the active file panel. The control closes Find and previews a centered line without taking focus from its input. Positive whole numbers clamp to the file range; negative numbers count from the end. Zero, unsafe integers and column/percentage syntax are invalid. Enter commits the line-start cursor and returns focus to the editor. Escape or Close restores the opening selection and scroll snapshot; clicking away retains the preview. Reopening starts with empty input, while repeating the shortcut in an open control selects its current value. Navigation changes neither file text nor host state. The current editor uses Pierre's unvirtualized `File`; this does not establish large-file performance.

Syntax highlighting uses the existing pinned review theme data. Editor background, code font family, size, weight and line height use the app's theme tokens. No additional dependency or runtime version was introduced.

## File preservation

- A save persists its original command ID before dispatch and includes the baseline revision. A lost receipt retains that ID for explicit recovery.
- Edits made after submission remain dirty when the saved revision is acknowledged.
- Each file has an in-memory generation. A read response from before a later edit, resolution or save receipt cannot replace that newer state or introduce a stale conflict.
- Offline text is cached under the host and workspace identity. Opted-in autosave drafts resume on reconnect with the original baseline revision; CAS or a host read preserves divergent local and host revisions as a conflict. Legacy manual buffers do not gain automatic save intent on upgrade.
- Choosing the host version retains the previous local text for recovery. Keeping local edits adopts the inspected host revision for the next save; opted-in autosave resumes after the normal debounce.
- Binary, oversized and unsupported-encoding files retain their existing non-editable states. UTF-8 BOM preservation belongs to the host write contract.

## Evidence and remaining parity

The frozen reference shows a Pierre-family source editor, highlighted source, find and undo. The pinned7982 source trace supports that library choice; its original exact dependency version is not recoverable. App Pierre1.3.5 remains an explicit tested pin.

The implementation is a partial Files checkpoint. Remaining Markdown rich features, installed app-exit save completion, remaining file-menu variants, complete selected-context composition and durable undo/scroll restoration still need their respective integration and acceptance. The generic Files directory list and its legacy nested file tabs remain visual differences; dedicated file tabs omit both. Generic Files retains its legacy Save/footer. Dedicated file tabs omit those healthy-state controls and show transient save status. Conflict controls and generic file-type icons remain visual/interaction differences, not claimed parity. The 48-point header and 384×320 picker use bounded reference measurements; they are not pixel certification. Hidden Electron checks exercise production renderer code against a real isolated authenticated host; they do not certify installed main/preload routing, native-window pixels or cross-machine behavior.

The focused regression passes43 tests/240 assertions, including real filesystem/Git saves and delayed-read cases. The shared skill source/rich regression passes13 checks/10 captures. General Files originals and receipts are under `.data/ui-acceptance/workspace-source-final-2026-09-07/`; source traces, scope, review notes and test/build logs are under `.data/workspace-source-editor-2026-09-07/`. Failed harness selectors from earlier attempts remain in their original private directories.

The per-file dock follow-up passes 38 focused tests/183 assertions. Its hidden Electron acceptance passes six checks/six captures, with two actual file writes, zero sessions, an unchanged seeded draft and stable source hashes. It records a 48-point visible header, 384×320 popup and 12-point UI-family breadcrumb text at 1440×1000 renderer size, zoom1 and DPR2. The old Files regression still passes nine checks/seven captures. Final originals and receipts: `.data/ui-acceptance/workspace-file-dock-final-2026-09-07-r4/`; private scope, reference measurements, review and failed-attempt qualifications: `.data/workspace-file-layout-2026-09-07/`. The acceptance composes the production dock components directly; full App routing remains outside its UI claim.

## Workspace tree

The dedicated editor header toggles a right-hand file tree beneath the shared header. The breadcrumb popup uses the same recursive tree without the filter field. Both navigate through the owning workspace and expand the active file’s ancestors. Tree navigation does not change the generic Files browser’s directory or save any editor text.

The pane filter inspects unopened descendants as well as visible rows. Online scans refresh each visited directory; failed reads retain any cached entries and report incomplete results. Offline searches are explicitly cached/incomplete. Scans yield periodically and stop scheduling new reads when hidden or superseded. Expanded folders retry once on activation or reconnection; persistent failures retain an explicit Retry control.

Arrow keys, Home/End, Enter and Space navigate the tree. The focused row scrolls into view, and queued focus is fenced against hidden/unmounted trees. The editor remains mounted when the pane opens or closes, preserving its current undo stack. Native file-type glyphs, sticky folder rows, compacted directory chains, symlink-directory navigation and large-tree performance still need their own checks.

Visibility is saved in window-local presentation state. Width is shared by file tabs during the renderer lifetime and starts at250 after restart, matching the pinned source lifetime. The pane has a200 minimum and60% parent maximum; dragging strictly below100 closes it, while100–199 snaps to200. The toolbar glyph is traced to the actual Toggle file tree control, distinct from the root-picker folder icon.

Tree acceptance: the final isolated production-renderer/authenticated-host run passes nine checks with ten captures, including exact200 minimum/60% maximum/strict-below100 close behavior, deep filtering, recursive breadcrumb navigation, same-editor undo across toggles and state retention across file-tab reopen. Exactly two file writes, zero sessions and the original revision1 draft are verified. Private result: `.data/ui-acceptance/workspace-file-tree-final-2026-09-07-r6/result.json`. Earlier attempts are retained: cross-remount undo and drag-reopen width assumptions were incorrect test expectations; pointer hit-testing was instrumented before accepting the actual production drag path. Injected Electron moves report buttons0 despite down/up sequencing, so physical pointer behavior is not claimed. Failed-folder reconnect remains source-reviewed rather than directly exercised. Generic file artwork and the filter input focus treatment remain visual differences.

## Autosave and closing

The general file editor now feeds actual Pierre content changes into the owner-scoped save queue with a three-second debounce. The native550ms potential-dirty marker precedes content extraction; our callback already contains the changed text, so there is no artificial additional550ms delay. New UI edits and explicit new-file creation persist autosave intent. Older version1 cached manual buffers remain manual. Offline opted-in text stays cached and can resume through CAS when its owner reconnects.

Autosave uses the existing durable command envelope and revision check. A newer edit made during a write survives the receipt and schedules another save. Confirmed failures, conflict, uncertain receipts and cache errors pause automatic saves; no new command is substituted for an unknown original. Manual save or a subsequent explicit edit can retry a confirmed failure. Original-receipt recovery remains explicit.

File-tab close waits for restoration and saves until clean. A failed close offers Continue viewing or Discard changes. Discard must persist before the close is approved; a storage failure retains the buffer. If a save is unconfirmed, its original envelope remains recoverable after discard. A persisted marker ties the discard to that command so eventual receipt recovery adopts actual host content clean; any later edit clears the marker and is retained. Checking an unconfirmed original can execute it if it never reached the host; discarding local text cannot cancel an already-dispatched save.

The healthy dedicated view omits the former Save button, footer and success notice. Its floating Saving…/Save failed status follows source-backed placement and sizing; exact glyphs, animation and native pixels remain unverified. The file-tab close hook and DockPanel await/save/modal route remain distinct from normal window shutdown, described below. Abrupt exit still relies on durable recovery.

The focused suite passes 48 tests / 251 assertions. The isolated dedicated-file renderer/host run passes 6 checks / 6 captures with stable source hashes: debounce, edits during a held write, offline host conflict, explicit failed-close decisions, dropped receipt and discarded-original recovery. `WorkspaceState.opened` stays undefined so this exercises dedicated file reads rather than the legacy browser refresh path. This run invokes the real close hook directly; it does not alone prove DockPanel button/removal behavior. Evidence: `.data/ui-acceptance/workspace-file-autosave-final-2026-09-07-r2/`; source trace, review, tests and limitations: `.data/workspace-file-autosave-2026-09-07/`.

The targeted DockPanel consumer follow-up passes three checks/one capture: Electron input closes a clean tab and later another clean tab; duplicate programmatic activations of the actual close button are suppressed; a held close drains newer text and removes the tab from its latest dock after keyboard movement. Exactly two actual file-write IDs and zero sessions occur, with original draft fields retained. The fixture renders the editor beside simplified dock content, so this proves the consumer’s asynchronous identity/removal behavior, not integrated App editor unmount or native window closure. Its settled capture has no Saving status after two animation frames. Evidence: `.data/ui-acceptance/workspace-file-autosave-dock-close-final-2026-09-07-r3/`. The original result’s “real double close click” wording is superseded by this input-method qualification; originals are unchanged.

## External Open on the owning host

Dedicated file tabs now have the reference split Open / Open options control. Discovery, file validation and launch run on the workspace's host. A file on another laptop never opens against the viewing client's directory or application catalog. The primary fallback is Finder/file manager; the menu offers installed supported editors, Default app, terminals and Open in folder. Linux hosts without a graphical desktop explain that external opening is unavailable. Discovery failures offer Retry, and selecting an application rechecks its availability on the host.

The editor matches the pinned caller's behavior: Open uses the current host file without forcing a save first or writing a preferred-application setting. Existing autosave continues independently. Open shares the durable workspace command queue; pending writes/actions can prevent admission and show an explanation. Lost launch receipts remain owner-scoped original commands. An uncertain outcome is never automatically replayed, and repeated original-ID checks cannot create a new launch.

The host resolves a catalogued project/session and a confined, existing regular file after target discovery, then checks workspace/file identity again. Executable/application candidates are fixed absolute paths with separate argv entries. macOS uses its native `open` launcher; Linux GUI processes detach after process admission without an application-killing timeout. A receipt proves launch admission, not that another app rendered the file. Applications disappearing before dispatch fail explicitly.

Discovery currently covers common macOS/Linux editor installations and supported terminal locations. Terminal actions open the containing directory; the native reference's configured `$EDITOR` invocation is still missing. Save as now has the separate client dialog and owner-host byte-copy path described below. Dynamic application icons, complete installation discovery, preferred-target settings and full pixel parity remain required work.

Validation uses injected availability/launch boundaries with real owner-host workspace commands and disposable filesystem bytes; it does not claim an actual OS app launch, installed main/preload behavior or physical cross-device execution. Private source trace and tests are under `.data/workspace-file-open-2026-09-07/`.

The Open menu uses the shared UI font, a170px panel and a28px split control. Default dark menu surfaces now use `#2a2a2a`; explicit theme-file/settings token overrides remain authoritative. This semantic token also affects other menus, without conferring new visual acceptance on those surfaces. Refreshed empty/unavailable menus retain keyboard focus so Escape remains usable after a selected application disappears.

The final focused run passes83 tests/496 assertions, typecheck/build and8 hidden-Electron checks/7 captures with stable source hashes. The authenticated host recorded6 Open deliveries/5 unique IDs,4 injected launcher invocations and exactly1 later autosave write. The unknown launch stayed uncertain after its duplicate original-ID check; no launcher replay occurred. There were zero sessions and the seeded text/project/model/revision1 draft was unchanged. The runtime checks caught and verified the empty-menu focus defect and a portal-event discovery retry storm. Evidence: `.data/ui-acceptance/workspace-file-open-2026-09-07-final3/result.json`. Hiding a panel during delayed discovery remains source-reviewed, not exercised in this bounded run.


## Save as on the viewing desktop

Save as… appears after Open in folder. The desktop opens its native save dialog with the source basename, sanitized like the reference. Cancellation returns without resolving the host or reading its file. After selection, main resolves the explicitly selected host once and uses authenticated, owner-fenced queries to copy raw bytes from the catalogued project/session. The destination comes only from the native dialog; renderer code supplies neither a destination nor file contents.

The copied data is the current host file, not an unsaved editor buffer. Save as does not force autosave, rename the editor tab, consume its draft, create a session, or change workspace ownership. It supports empty files, binary data and files above the editor’s text size limit. One MiB chunks bind to a revision derived from file identity and nanosecond metadata; the host rechecks its file descriptor, confined path and root for each read. The client rejects mismatched owner, path, revision, offset, size and invalid/truncated Base64, then rechecks source metadata before publishing the copy. A same-file shortcut applies only to the confirmed local host; equal remote and local path strings do not establish identity.

The selected destination is written through a temporary file in its actual parent. Normal transfer/read failures preserve an existing destination and remove temporary data. Source or destination changes observed during transfer abort the copy. New filenames publish without overwriting a concurrently created file. Existing and dangling destination symlinks retain the link and write its referent. Existing regular destination mode is retained; new files follow the process umask. Replacement changes the destination inode; extended attributes, ACLs and hard-link relationships are not preserved by this implementation.

Existing-file replacement is an ordinary rename after metadata checks, not an atomic cross-process compare-and-swap. A narrow check-to-rename race with other applications remains, as do ordinary filesystem parent/symlink races. Metadata revisions are not immutable content snapshots. Process death can leave a temporary file, and the copy has no durable restart receipt. These limits are not claims of exact native behavior: the pinned app streams directly into the destination; staging and change checks are deliberate protection against a truncated destination after remote failure.

The menu disables Save as offline or when the desktop bridge is unavailable. Duplicate operations in one viewing window are rejected by main; closing that window aborts transfer. Errors retain the editor state and show an actionable explanation. The destination dialog owns overwrite confirmation; its actual OS interaction and installed preload/main path still need native acceptance.

Automatic validation:76 scoped tests/485 assertions pass across main copying, host HTTP, external Open, workspace service and renderer state. Subsequent authentication-error and IPC-message corrections pass all17 main-copy tests/83 assertions; typecheck and production build also pass on that source. The copy tests include actual filesystem bytes and HTTP, cancellation before owner lookup, binary/empty/BOM/newlines, local versus remote same-path handling, source/destination changes, symlink retargeting, invalid chunks, wrong owner, transfer/closure errors and temporary cleanup. Native-dialog selection is an explicit test seam, not native OS verification. Private scope and reference trace: `.data/workspace-file-save-copy-2026-09-07/`.

The pinned source also invokes a best-effort macOS `launch-services-helper notify-download-complete` after a successful copy. Its native implementation and visible effect are unverified, and this follow-up is not implemented here; no Finder reveal or recent-document update is inferred from it.


Final hidden-Electron acceptance passes five checks/five capturePage originals with stable production/harness hashes. Four injected dialog invocations cover cancellation, successful export, real host revision failure and explicit recovery. Eight authenticated copy queries produce two exact exports and one rejected chunk; one later autosave is the only workspace mutation, with zero sessions and an unchanged revision1 draft. Offline/cancel paths send no copy query. The actual main copy helper runs behind a fixture IPC/preload, while native dialog choice is injected. Root screenshot review caught internal Electron error text; structured outcome serialization now leaves only the actionable message. Final evidence: `.data/ui-acceptance/workspace-file-save-copy-2026-09-07-final-outcome/result.json`. The menu is170px wide at a1440x1000/DPR2 content viewport; different catalog/focus/shell states prevent a same-state pixel-parity claim.

## Go to line evidence

The pinned7982 source supplies the whole-number parser, panel ownership, centering and explicit-cancel versus outside-close distinction. Frozen7868 capture04-panels/17 establishes the empty popup; it does not capture the tested transitions. The stable hidden-Electron run at `.data/ui-acceptance/workspace-file-goto-line-2026-09-07-final/` passes seven checks/five page captures with matching source hashes: far-line90 preview retains input focus, Escape restores the opening cursor/scroll, positive/negative clamps and Enter use character zero, Close restores the committed position, invalid values remain open, repeated shortcut selects the current input, and outside click retains line60. Cursor effects are checked with actual Electron insertion and undo, leaving exact editor/host bytes. Ten authenticated workspace reads, no attempted mutation, zero sessions and the original revision1 draft are recorded.

At renderer1440×1000/DPR2/zoom1, the popup is266×38, input200×28, close20×20, with16-point top/32-point end placement. The hidden native frame is1440×1032 at180/85; page rasters are2880×2000. These are Chromium accessibility/page captures, not OS-window captures. Frozen reference geometry and keyboard/key-window state differ; no pixel comparison passes are inferred. Source supports dynamic theme colors, text-change cancellation fencing and inactive-panel cleanup, but their transition-specific UI acceptance remains outstanding. Narrow-window geometry, physical Control-L on Linux and full App menu dispatch remain unverified. Source traces, failed harness attempts, corrected capture timing and comparison classifications remain private under `.data/workspace-goto-line-2026-09-07/`.

Seven parser/shortcut/file-link tests pass67 assertions. The existing shared skill regression passes13 checks/10 captures with stable source hashes, zero sessions and unchanged draft under `.data/ui-acceptance/workspace-goto-skill-regression-2026-09-07-r2/`. It caught the need to update the skill editor's direct-child flex selector for the retained editor wrapper. Existing installs, runtime pins and sealed Work evidence were not changed.

## Ordinary Markdown files

Dedicated `.md`, `.markdown` and `.mdx` file tabs now default to the existing editable Markdown view. View source / View preview sits in the shared file header. Both editors use the same host-owned document and remain mounted across mode changes. Switching first drains the current dirty file through `saveUntilClean`; offline, conflicting, failed or uncertain writes retain the current mode and buffer. A stale completion cannot switch a different file. Window-local dock state persists the mode under the same host/workspace/path tab identity; it does not change shared session or provider settings.

The underlying Markdown editor preserves raw source/frontmatter and writes task-checkbox changes through the same revision-checked save path. Line requests use CodeMirror selection and scrolling when preview is active. The existing generic Files directory view remains source-only. Mermaid, selection-to-chat and full scroll/undo handoff still need their own implementation or acceptance. Images and tables have scoped evidence below. File-link and offline reveal behavior is covered in the later checkpoint below.

Five hidden-Electron checks/five captures verify default preview, actual rich edit and host save before source mode, offline source-edit preservation and mode refusal with no write, reconnect/save before preview, and checkbox autosave. The host records three unique writes, zero sessions and the original revision1 draft. Final receipt: `.data/ui-acceptance/workspace-markdown-2026-09-07-checked/result.json`. Source and skill regressions pass7 checks/5 captures and13 checks/10 captures. Window-state tests persist both modes across store recreation. These tests do not certify installed App/main routing, physical input, clipboard, exact preview pixels or cross-device behavior. Frozen source traces, evidence qualifications and remaining differences: `.data/workspace-markdown-2026-09-07/`.

## Copy Markdown

Dedicated Markdown preview and source tabs expose the reference content-area Copy Markdown icon. It copies the active raw buffer, including frontmatter and unsaved offline edits, without saving or issuing a host request. Success shows the reference check glyph and Copied feedback for two seconds; clipboard rejection exposes a retry instead of claiming success. Buffer changes and unmounts invalidate stale feedback.

Nine hidden-Electron checks/eight captures pass against a disposable authenticated host, including actual system clipboard writes/readback from clean preview and dirty offline source, injected clipboard rejection and real retry. The harness snapshots clipboard items in memory and restores them before exit if still fixture-owned; the final receipt confirms restoration. Three host writes belong to the existing mode-switch/checkbox sequence; copying adds none, creates no sessions and preserves the revision1 draft. Typecheck/build pass. Evidence: `.data/ui-acceptance/workspace-markdown-actions-2026-09-07-final/result.json`; source trace and failure qualifications: `.data/workspace-markdown-actions-2026-09-07/`. This is renderer/system-clipboard evidence, not installed-window or matched-pixel proof. Same-document anchors, diagrams and selection actions remain open.

## Markdown link navigation

Unselected parsed Markdown links and angle-autolinks support completed left clicks and focused Enter activation. A cancelled press does not navigate. The editor handles link Enter before the normal newline command and leaves ordinary editing unchanged. Relative paths resolve from the Markdown file’s directory inside its owning workspace; local file URIs, encoded names, and native line/column/end-line syntax are supported. The shared host service still enforces realpath ownership on read. External HTTP(S) links use the existing desktop opener. Unsupported schemes, invalid locations and unavailable targets remain explicit errors. Pure document fragments do not invent an unverified anchor-scrolling behavior.

The owning file tab is reused and receives the location request. Cached file ranges can be revealed offline while a separate unsaved Markdown buffer remains intact. The dock’s pointer capture now stays on the tab button for ordinary clicks, moving to the drag container only after movement; normal tab activation no longer disappears into a drag operation.

Eight hidden-Electron checks/six page captures exercise actual RichMarkdownEditor, Pierre, WorkspacePanel and dock state against an isolated authenticated host. They cover cancelled presses, relative line-range selection through actual insertion/undo, file-URI tab reuse, pointer/keyboard URL dispatch, rejected schemes, real missing-file reads and offline cached-range navigation. The receipt records 23 reads, zero commands, zero sessions, unchanged original files and the revision1 draft. External opening is an injected recording boundary, not an actual OS-browser launch. Evidence: `.data/ui-acceptance/workspace-markdown-links-2026-09-07-final/result.json`; private source traces, failed attempts and comparison limits: `.data/workspace-markdown-links-2026-09-07/`.

Fourteen focused parser/file-link/dock-state tests pass67 assertions; the controlled dock regression passes seven checks. The shared skill editor regression passes13 checks/10 captures after the Enter-handler change. Typecheck and production build pass. Pinned7982 code supports the relative-file and range routing, while the frozen7868 preview captures do not cover these specific transitions. Captures are hidden-renderer pages with Chromium accessibility at1440×1000/DPR2/zoom1; no installed App routing, physical cross-host browser launch or same-state native pixel certification is claimed.

## Markdown Go to line

The editable Markdown view now uses the same app-level whole-line navigation behavior as the source editor. Command-L closes Find, snapshots the current selection and scroll, and opens an empty control. Valid input previews a centered line without taking input focus. Enter commits a line-start cursor; Escape or Close restores the prior selection and scroll after a preview. Outside click retains the preview. Mode changes dismiss the inactive control; document changes invalidate stale restoration. Read-only editors do not register it. Validation is announced only while invalid.

CodeMirror has a distinct appearance in the pinned reference: a340px card with20px rounding, a44px row plus border,96×24 input,24px close button and separator. It uses the secondary surface and a sticky12px top/end placement. The native stock CodeMirror dialog has a different grammar and is not the app control used here. The existing source editor keeps its Pierre-specific control.

Seven hidden-Electron checks/four captures pass against an isolated authenticated host at1440×1000/DPR2/zoom1. Actual input and insertion/undo prove the restored line10/column3 cursor, negative-line commit, explicit close and outside-click retention; the measured line90 center differs from its scroll viewport center by less than1.1 points. Captured dimensions are340×45 including border,96×24 input and24×24 close; explicit dark surface isRGB36. Nine reads, zero commands/sessions, original file bytes and revision1 draft are preserved. Evidence: `.data/ui-acceptance/workspace-markdown-goto-2026-09-07-final-a11y/result.json`. Source Go to line regression passes7 checks/5 captures after the accessibility correction; skill regression passes13/10 after the layout change, before that attribute-only correction; seven scoped tests/52 assertions, typecheck and build pass.

The detailed Markdown-control state is supported by pinned7982 source, not a matched native capture. These are Chromium page/accessibility captures in a hidden fixture; native-window pixels, installed routing, physical multi-host input, light/custom theme transitions, narrow-window behavior and restoration after remount remain unverified. Private source trace, first-run CSS/Enter-input correction and comparison limits are in `.data/workspace-markdown-goto-2026-09-07/`.

## Inline Markdown images

Ordinary Markdown file previews render owning-workspace images inline. Current-file-relative paths and owning-host file URIs, titles, alternative text, and full/collapsed/shortcut reference definitions are supported. Large images fit the available width; small images retain intrinsic dimensions. Selecting an image’s source exposes its Markdown; leaving it restores the image. External web/data images remain source markup, matching the pinned local editor’s path boundary. Skill-editor image resolution remains separate work.

Images stream through opaque Electron protocol grants backed by the existing authenticated host copy-info/chunk reads. Host credentials and machine paths stay out of image URLs. Grants belong to the requesting renderer and are revoked on disposal, navigation/reload, renderer loss or window close. Sequential chunks validate file identity, revision, offset and size; final identity is checked before releasing the final bytes. Unknown extensions let Chromium decode the content. No personal OMP executable or configuration changes are involved.

Disconnect retains already-mounted decoded images. A new offline acquisition uses the browser’s broken-image/alt presentation; reconnect refreshes image grants automatically without editing or resending a prompt. This is not durable offline image caching. The pinned reference only decorates local-origin images; using the owning remote host is an intentional extension of the agreed machine-owned workspace model.

Seven hidden-Electron checks/four captures pass with the actual production preload, editor, main image-grant/chunk modules and an isolated authenticated host. The fixture main IPC handlers invoke those modules; production main handler lifecycle wiring is source-reviewed, not exercised by this harness. A large SVG uses three actual chunks; file URI, reference definition, extensionless PNG, alt/title, size bounds, selection/disposal, modes, missing file and automatic reconnect are exercised. Ten workspace reads and35 image queries, zero mutation commands/sessions, original file/image hashes and revision1 unsent draft are preserved. Receipt: `.data/ui-acceptance/workspace-markdown-images-2026-09-07-final-clean/result.json`.

Fifty-three tests pass284 assertions; Markdown links (8 checks/6 captures), Markdown Go to line (7/4), and skill editor (13/10) regressions pass. Typecheck and production build pass. The CommonMark parser already used by transcript Markdown is now explicitly pinned as a direct dependency; its resolved package version is unchanged. Private scope, source traces, earlier runs and comparison limits are under `.data/workspace-markdown-images-2026-09-07/`. These are1440×1000/DPR2/zoom1 Chromium page/AX captures, not native window captures. No matched native inline-image reference exists in the frozen bundle; installed routing, physical remote-host input, hover tooltip pixels, skill images and full native pixel parity remain unverified.

## Normal window close and app quit

Normal Electron window close now requests correlated preparation from that renderer before allowing destruction. The app makes the main shell inert while opted-in online file saves finish and local file/skill recovery is persisted. Keep window open aborts that close drain; an already-dispatched save can finish, but the canceled drain cannot dispatch later edits. Offline and manual buffers are cached without a new host write. A failed save, unknown receipt or failed recovery storage keeps the window open. This does not claim persistence for every unsaved settings form.

App quit prepares every window. If one refuses, already-approved windows regain input; stale responses and renderer generations cannot authorize a different close. Host streams and notification delivery are disposed only at actual will-quit. Timeouts keep the window open. Forced termination, OS logout and installed full-App shutdown remain separate acceptance gates.

The authenticated isolated-host/hidden-Electron run passes six checks/five PNG+AX captures with stable source hashes: actual BrowserWindow close and app.quit, held-write cancellation, offline close/reopen, storage failure and multi-window quit refusal/success. Exactly one real file write, zero sessions and the original revision1 draft are retained. The fixture uses production preload, close hook, WorkspacePanel/state and main gate with fixture event wiring. Scoped controller/gate tests pass48 tests/260 assertions; the final explicit Node UUID import also passes the six gate tests. Normal Electron exposed an invalid receiver in the initial global crypto.randomUUID call that Bun tests missed; the real lifecycle run verifies the fix. Evidence: `.data/ui-acceptance/window-close-2026-09-07-r6/result.json`; scope and failed attempts: `.data/window-close-2026-09-07/`.

There is no matched Codex saving-on-window-close capture: the recovery status is an intentional durability affordance, not certified native visual parity. Skill close recovery is controller-tested, not exercised by this file-window fixture.

## Editable Markdown tables

Rich tables now follow the pinned editor’s live CodeMirror layout: equal-width cell spans, hidden pipe delimiters and zero-height separator lines,8×12 default cell padding,600-weight headers and subtle body-row borders. Text stays in the original Markdown document. Alignment syntax is preserved without inventing visual alignment; uneven rows use their actual parsed cells. Normal selection, editing, undo and links remain available. The dedicated source mode exposes raw syntax.

Cell marks have lower decoration precedence than inline formatting so a cell containing only emphasis remains an outer flex item. Screenshot review caught that nesting defect after an initial incomplete geometry check; the strengthened acceptance checks every row’s cell parent and width. The border is configurable through `--markdown-table-border`, with dark white4% and light foreground5% defaults; padding uses the existing spacing tokens.

The final isolated renderer/host run at `.data/ui-acceptance/markdown-tables-2026-09-07-final-scroll/result.json` passes seven checks/seven PNG+AX captures with stable source hashes. It covers table layout, pointer editing/undo, a cell link, one actual file save, mode switching, offline cache recreation and narrow/light/custom-token wrapping. It preserves all other Markdown/frontmatter bytes, zero sessions and the original revision1 draft. The shared skill and image regressions also pass. See `.data/markdown-tables-2026-09-07/` for source traces, failed attempts and evidence qualifications. Captures are hidden Electron pages/Chromium AX; no matched native table capture, installed routing or full pixel parity is claimed. The external opener and cache recreation have explicitly recorded fixture boundaries.


## Skill source/preview save guard

The skill editor now uses the same save-before-switch contract as ordinary Markdown files. A requested switch waits for the current write and any newer edits to reach the owning host. Rapid requests are coalesced by disabling the pending toggle. Dirty offline buffers, conflicts, failed recovery storage and unconfirmed receipts leave the current view and text intact. Inspect file can confirm an unknown save through a read; switching never silently replays that write. Leaving the active panel or replacing its controller aborts the delayed mode change. An already-delivered write can finish, while the canceled switch does not drain later edits.

The shared save waiter also protects normal close after connectivity drops: a previously delivered write settles before offline recovery is persisted. Close still retains later offline edits without sending them. The scoped controller/main close-gate suite passes28 tests/168 assertions, including Undo to the old baseline while an earlier write is pending. Typecheck and build pass. Independent Terra review found the disconnect-close gap; its fix was reviewed and regression-tested.

The final run at `.data/ui-acceptance/skill-mode-switch-2026-09-07-final-ready/result.json` passes15 checks/14 PNG+AX captures, eight actual skill writes and15 queries, with zero sessions and the original revision1 draft preserved. This includes the existing editor regression and real held-write/lost-receipt mode paths. Private reference/scope and comparison records live under `.data/skill-mode-switch-2026-09-07/`. The pinned7982 source establishes save-before-switch behavior; frozen7868 captures establish the two view modes, with no matched native capture for held/failed/offline transitions. These are isolated production-renderer and authenticated host checks, not installed routing or native pixel parity. The subsequent per-tab view persistence change below also restores the selected mode. A dirty offline document can continue editing in that restored view; changing views still requires resolving the save.


## Skill tab view persistence

Skill files reuse `DockTab.fileMode` and the existing per-window `WindowStateStore`. Mode is presentation state scoped to the validated host/ref/tab identity; it does not enter the shared document-recovery cache. The panel renders the stored mode, and successful save-guarded switches publish a new selection. An inactive/replaced panel cannot publish an old switch result. Existing snapshots without a mode default to preview, and invalid values are ignored.

The isolated run at `.data/ui-acceptance/skill-view-state-2026-09-07-r1/result.json` passes16 checks/15 PNG+AX captures. Actual disk state and IndexedDB restore a dirty source view after a full renderer reload, then a later reload restores preview. The same existing skill regression performs eight real writes; no extra save occurs from reload, zero sessions and the original revision1 draft remain.41 scoped tests/261 assertions cover file/skill restoration, native host/project/session scopes and independent window modes; typecheck/build pass. The fixture calls the actual disk store through its local proxy, so this does not claim installed main IPC/debounce or native pixel proof. Private scope/comparison: `.data/skill-view-state-2026-09-07/`.

Review also identified closed skill controllers remaining active. The close lifecycle change below addresses that separately from mode persistence.


## Skill tab close and controller lifetime

The shared file-close hook now accepts skill files through the same dialog and keyboard flow. Online close drains confirmed saves and newer edits before removing the tab. Offline unsent edits offer Continue viewing or Discard changes; Escape keeps the tab and its text. Discard persists a clean baseline in the existing cache, so reopening does not revive abandoned edits through legacy storage fallback. Storage failure keeps the original edits, and a newer edit during persistence keeps the tab open. A retained pending receipt cannot be discarded: return to the editor to inspect or explicitly check that save first.

App disposes and removes the controller only after the dock snapshot drops its tab. Failed/canceled close leaves the controller live. Disposed controllers cannot schedule autosave, perform new reads or send a command that was still waiting for local pre-dispatch persistence. A previously delivered command may settle and preserve its recovery result, but cannot trigger a later save.

Final isolated evidence: `.data/ui-acceptance/skill-tab-close-2026-09-07-final/result.json` passes19 checks/22 PNG+AX captures, with nine actual skill writes, zero sessions and the original revision1 draft. This includes the prior skill regression, actual close clicks, unknown-receipt refusal, offline Escape/discard/reopen, and a held online save before controller release.34 scoped tests/201 assertions cover cache failure, newer edits, late receipts and disposal before dispatch; the ordinary-file dock-close regression passes3 checks/1 capture. Typecheck/build and Terra review pass. Private scope/comparison: `.data/skill-tab-close-2026-09-07/`. Fixture lifecycle wiring mirrors App, so installed full-App behavior and matched native modal pixels remain unclaimed.


## Rich Markdown fenced code

Closed fenced blocks now hide the complete opening and closing marker lines while preserving the original document and newline positions. The first line displays the full language/info string at the top right; the first and last lines supply rounded corners. Body text remains editable with ordinary selection, undo and owner-host saves. Incomplete fences and indented blocks do not receive closed-fence decorations. The pinned rich editor does not configure language loading, so this surface does not add syntax highlighting.

Default geometry follows the pinned source:12px inline padding,10px effective corners and12px language labels, with the subtle secondary background. Dedicated theme tokens expose background, base radius and label size. The extended table acceptance run also covers backtick/tilde/empty/quoted fences, literal code content, editing/undo, a second real file save and custom code tokens. Final evidence at `.data/ui-acceptance/markdown-fences-2026-09-07-final/result.json` passes10 checks/10 PNG+AX captures with two actual writes, zero sessions, the original revision1 draft and stable source hashes.The shared skill-editor regression also passes19 checks/22 captures.20 scoped tests/77 assertions, typecheck/build and independent Terra review pass.

Private source traces and comparison qualifications live in `.data/markdown-fences-2026-09-07/`. These are isolated production-renderer/authenticated-host checks. The frozen bundle has no matched fence-state capture; native pixel parity, installed routing, Mermaid and broader editor coverage remain open.


## Native skill inline images

Skill file previews now use the shared Markdown image widget. Native OMP discovery binds the skill even when it lives outside the project; image metadata and chunks are read from its canonical parent directory on the owning host. Each read checks the skill identity before and after, while the image stream checks revision, size, offset and final path identity. Relative paths and file URIs inside the skill directory are supported. External web/data and outside-directory paths remain raw markup; durable offline image caching remains open.

Opaque grants reuse the existing Electron protocol and sender lifecycle. Mounted images stay decoded when disconnected; reconnect replaces the grant. Closing or hiding a preview removes its image widgets and releases their grants. A disposed controller releases a late acquisition. No credentials or host paths enter image URLs.

The first complete image run exposed a production admission issue: image requests exhausted the same four composer slots needed by reconnect's skill-file read. Images now have a separate bounded host budget, and each desktop serializes its skill-image reads per host, skipping canceled queued work before dispatch. Existing control/file queries keep their own budget. A regression test holds four image requests while a normal file read succeeds.

Final isolated evidence: `.data/ui-acceptance/skill-markdown-images-2026-09-07-r3/result.json` passes21 checks/24 PNG+AX captures with24 image queries, nine existing skill-editor writes, zero sessions and the unchanged revision1 draft. It exercises actual preload, production image/grant/transport modules and authenticated native-host reads, including a skill outside the project, SVG decode, missing-file presentation, disconnect/reconnect and the full prior editor regression. Ordinary Markdown images pass7 checks/4 captures.57 tests/312 assertions, typecheck/build and independent Terra review pass.

Private scope, comparisons and failed attempts: `.data/skill-markdown-images-2026-09-07/`. The fixture supplies main IPC wiring; installed main/App routing, physical Work input and matched native image pixels remain unverified. Existing Work runs and sealed reference bundles are unchanged.


## Skill Copy Markdown

Skill tabs now reuse the ordinary Markdown content-area copy control in both preview and source. It copies the active raw buffer, including hidden frontmatter and unsaved offline edits, without reading or saving the host. The content wrapper preserves editor scrolling and the shared26px button geometry; panel-wide toolbar styles exclude this control. Success/reset and failure/retry use the existing implementation.

The full skill regression at `.data/ui-acceptance/skill-markdown-copy-2026-09-07-r1/result.json` passes23 checks/27 PNG+AX captures, including actual system clipboard write/readback from preview and offline source, injected rejection and real retry. Prior clipboard formats were restored after verifying they remained fixture-owned. Copy adds no host call; the existing nine skill-editor writes, zero sessions and original revision1 draft are retained. Typecheck/build and independent Terra review pass. Private scope/comparison: `.data/skill-markdown-copy-2026-09-07/`.

The source audit confirms skill Open routes into the generic Markdown file editor. This does not establish a matched native skill-copy pixel comparison. The labeled owner-host Open control, per-mode scroll restoration and selection-to-chat remained open at that checkpoint; a file tree or breadcrumb is not inferred from the route's null working directory.


## Skill editor scroll restoration

Skill tabs preserve independent preview/source numeric scroll positions through mode changes and renderer reload using existing window-local dock storage. Hidden editors do not publish zero positions. Initial restoration waits for visible content; user input takes control. Scroll updates are coalesced, and tab/page lifecycle boundaries flush pending positions. Validation accepts only bounded finite numbers; raw document bytes and CodeMirror objects do not enter window JSON.

The pinned rendered Markdown snapshot is a raw in-memory CodeMirror effect, reused only when content matches. Its durable route does not serialize it. Our co-mounted editors keep live state; numeric disk restoration is an additional recovery path. Semantic anchor restoration across reflow is not certified by this change.

Isolated evidence: `.data/ui-acceptance/skill-scroll-2026-09-07-r1/result.json` passes24 checks/31 PNG+AX captures. Actual wheel input produces distinct positions; full renderer reload restores source, then preview restores independently without hidden-mode overwrite or added file writes. The full previous suite retains nine real skill writes, zero sessions, the original revision1 draft, clipboard restoration and stable source hashes.32 tests/173 assertions, typecheck/build and independent Terra review pass. Private scope/source trace/comparison: `.data/skill-scroll-2026-09-07/`. Installed full-App routing, physical Work input, immediate close timing and matched native pixels remain unverified.


## Native skill Open and Save as

Skill files now use the ordinary file editor’s labeled Open split control and application menu. Discovery and execution belong to the skill’s host, including skills outside the selected project. The host checks the native reference and canonical parent again after application discovery, then launches only a supported installed target with fixed arguments. An Open command shares the skill’s durable command queue; its controller stores the receipt before delivery and preserves that ID after an unknown response or failed acknowledgement storage. Choosing the same application checks that receipt; choosing another is refused until it is resolved. An explicit Check Open receipt action also works when the application has disappeared from the installed-app menu. Opening does not force a save of editor text.

Save as uses the existing main-process native save dialog and copy engine with an exact skill reference rather than a fabricated workspace. It reads the current host file, validates revision/chunks/final identity, and publishes to the local user-selected destination. Cancellation occurs before host reads; a closed window aborts the operation. Unsaved editor text remains intact. Production IPC shares the per-window exclusion with ordinary Save as.

Evidence: `.data/ui-acceptance/skill-open-2026-09-07-r2/result.json` passes26 checks/36 PNG+AX captures, including the full prior skill regression. Actual input/preload/query transport drives two host launches whose controlled adapter records the exact canonical path/argv/cwd. Withholding the second response, removing all apps from the controlled catalog, and checking its original receipt produces three command deliveries but exactly two launches. Save as writes a byte-exact local copy through the shared engine with a fixture destination chooser. Neither adds an editor write: nine previous skill writes, zero sessions, original revision1 draft, restored clipboard and stable source hashes remain.81 tests/406 assertions, a final32-test controller regression, typecheck/build and independent Terra reviews pass. Tests include path identity replacement during discovery, malformed/foreign options, failed storage before launch, lost receipt/reload/same-ID recovery, raw-byte copying and dirty-buffer preservation.

Private source trace/comparison: `.data/skill-open-2026-09-07/`. The controlled launch adapter and destination chooser do not prove native application launch or dialog interaction. Installed main/App routing, physical Work input, exact application icons and matched native skill pixels remain open. Selection-to-chat remains a separate missing editor feature.


## Composer clipboard and repeated sources

The main ProseMirror composer exports the pinned clipboard label/path escape grammar and at-mention HTML attributes. App HTML also carries its non-secret owner identity; pasting another host's file rejects atomically instead of remapping the absolute path. Native HTML uses fsPath or path and the selected host. Local absolute Markdown links restore file nodes; ordinary web/relative links remain text. Every pasted node gets a fresh identity. Explicit unsupported-host errors preserve the existing draft and clipboard. Native custom labels currently normalize to basenames.

Repeated sources require all nodes to carry UTF-16 text offsets. Publicv9 and its advertised repeatedSources capability retain the exact occurrences; schema11 prevents storage downgrade. Draft saves and clears use v9 while either the current or base draft contains repetition. Worker34 copies authored text length at the process boundary. The native generator reads distinct sources in first-seen order once, but serializes every link. Bindingv3 stores all occurrences and verifies a single owning host, exact user text, unique native snapshots and entry ordering; existing v1/v2 meanings do not change.

The authenticated host test caught and fixed a missing text-length argument in WorkerClient before delivery. Native HTTP, image/reopen, previous binding and disposal tests pass; actual packaged/upstream pins are unchanged. Ten isolated renderer checks/thirteen PNG+AX captures at `.data/composer-clipboard-ui-2026-09-07-r2/result.json` exercise production synthetic DataTransfer copy/paste, native keyboard undo/redo, reload and owner-host draft persistence with zero sessions. Source hashes remained stable. Typecheck, fixture-inclusive typecheck and build pass. These are component/native-backend checks, not installed, physical clipboard or pixel parity. Detailed scope, retained failure and limitations: `.data/composer-clipboard-2026-09-07/REPORT.md`.


## Standalone absolute file tabs

Composer file mentions can open without a selected project or when the file is outside the selected project/session cwd. The pinned7982 dispatcher supplies the literal host path and a nullable working directory; `/` and `~` use the standalone route. In-project references retain their existing workspace tabs.

Standalone tabs encode the absolute path in their host-qualified dock identity and expose only that basename to the editor. They reuse preview/pin, source/Markdown modes, offline buffers, revision-checked saves, Open and Save as. File trees and project breadcrumbs are absent. Linked Markdown images get separate explicit file targets; they do not grant access to the original file's parent directory. Parent-relative and absolute Markdown links resolve into a new exact file target.

The host accepts exact-file stat/read/Open/copy/write operations and rejects directory, Git, terminal, environment, settings and composer/plugin scope. Persisted window state rejects malformed file identities, mismatched basenames and non-file tabs. Recovery rejects another file's buffer or pending command before any replay. This adds no project/session rows and changes no OMP/Bun pin.

Scoped filesystem/controller and authenticated host tests pass. Four hidden Electron checks/five PNG+AX captures at `.data/standalone-file-ui-2026-09-07-r3/result.json` verify zero reads before composer pointer activation, exact-file rendering, disconnected renderer reload with the dirty IndexedDB buffer and no redundant read error/query, one Command-S host write, and missing-file presentation. Catalogs stay empty and source hashes remain stable. Fixture-inclusive typecheck and production build pass. Private evidence and qualifications: `.data/standalone-files-2026-09-07/`. Fixture routing is not installed full-App routing; native application/dialog input, matched reference pixels and cross-device installed behavior remain unverified. The broader milestone remains open.
