# Local project environments

Local environments remain incomplete. They are application features layered over OMP: reusable project setup/cleanup scripts and actions, selected in the composer and managed in settings. OMP's provider setup wizard does not supply this behavior. The app must give the resulting working directory and setup environment to the owning OMP session.

## Reference behavior

Pinned7982 provides an Environment selector separate from execution mode and starting state. It exposes loading/error, selected configuration, No environment, Work without environment, Set up project and Environment settings/repair. Selection is a project-owned configuration path, independent of branch selection. The editor supports a name, common and platform-specific setup/cleanup scripts, and named actions with optional platform and icon.

The native TOML schema has `version` (integer, default1), `name`, required `[setup].script`, optional `[cleanup].script`, and optional platform script overrides under setup/cleanup. A nonempty platform script replaces the common script; an empty override falls back to it. Actions contain name/command, optional platform, and a tool/run/debug/test icon. Configuration reads and saves carry a revision; an edit conflict must preserve the editor draft.

Setup executes after creating the worktree, in the configuration's project-relative working directory. Native pending creation preserves the new worktree and enters a failed state with an error and attention flag when setup fails; it does not advance to worktree-ready. A successful setup captures exported environment changes for later processes. Cleanup executes before worktree removal, and failure prevents that removal. Selecting a menu item alone does not run scripts.

Source evidence: pinned `worktree-environment-dropdown-2cca23d397d0.js` (`st`), `local-environment-editor-73dc2c51b8c0.js` (`Ke`, `Ze`, `ot`), native `worker.js` (`K4`, `K2`, `gde`, `Sde`) and the pending-worktree manager in `main-C5K7o1Hr.js`. Private excerpts and trace: `.data/environment28/`. These source facts do not establish measured UI parity or complete discovery/recovery semantics.

## Implementation boundaries

- Reuse the host's project ownership, revisioned drafts, command receipts and managed worktree lifecycle. Add an environment selection to the captured submission only through an explicitly compatible protocol; older clients must not silently drop it.
- Resolve and validate the selected configuration revision before side effects. Save the selected configuration identity with the managed worktree so later cleanup cannot use an unrelated project's selection.
- Record worktree creation and setup as distinct steps. Setup failure preserves the directory and original prompt; an unknown result must be reconciled before any explicit retry. Never replay a script because a client lost its response.
- Keep setup environment values private to the host. Apply the intended environment to subsequent owning-session and terminal processes without changing host-global credentials or another session's configuration. UI status and ordinary command receipts must not contain captured environment values.
- Provide setup progress/output, failure/recovery, revision conflicts and native editor controls. An inert selector, raw-file-only editor or source-only runner does not satisfy this feature.

Before completion, exercise real temporary worktrees and shell scripts for successful setup and exported environment, failed/cancelled setup, crash/receipt uncertainty, explicit recovery, cleanup success/failure, platform selection and named actions. Confirm the source repository remains unchanged, drafts persist through navigation/restart, and remote selection stays on its owning host. Pair the actual UI states with the reference bundle. Parent/subdirectory discovery and exact native recovery controls still need further tracing and implementation.

## Whole-chat Fork

Whole-chat Fork preserves the original managed conversation and creates a distinct native child. The current-workspace destination is labelled **Fork chat**, or **Fork chat in same worktree** when the source already belongs to a worktree. **Fork chat in new worktree** is offered only for an eligible project-owned source. A new worktree uses the existing preparation lifecycle and the source workspace's captured dirty tracked/untracked state, not an unrelated main checkout.

The owning host validates the source revision and idle admission, records durable intent, and flushes the existing worker without changing or retiring its native identity. A separate native worker uses `SessionManager.forkFrom`; the source worker never runs the identity-changing `AgentSession.fork` operation. Native child identity, the host binding, inherited private environment, empty child draft and successful receipt commit together. The original file and draft, including attachments, remain unchanged.

The operation is introduced by command16 `session.fork`, worker protocol57 and store format25. Older command endpoints reject it rather than silently dropping its destination. Lost acknowledgement is recovered through the read-only `getSessionFork` snapshot and stored binding: opening that child does not dispatch another fork. Unknown native outcomes block both a fresh attempt and recovery replay; they are not permission to recreate a child. This does not broaden external-session import or stock-CLI ownership guarantees.

The maintained actual-App scenario is `scripts/acceptance/session-fork-app/run.ts`. Its disposable host uses an explicit refusing Tailscale executable and verifies zero discovered peers before UI input. Native window captures are bound to observed CG/AX geometry, viewport, display scale, zoom, theme and window-manager state. These are controlled product checks, not matched-reference pixel or live-provider acceptance.

## Source28 configuration groundwork

`apps/host/src/local-environments/` now parses and serializes the traced configuration shape, lists individual configuration errors, selects nonempty platform overrides and performs revisioned saves. Authored files use `<project>/.agent-desktop/environments/*.toml` as an app-specific path choice; native discovery/import compatibility is not yet implemented. A matching revision can repair invalid TOML. Saves preserve file mode, return current and attempted edits on conflict, serialize canonical-project aliases, and recheck file bytes/identity before replacement. Descriptor reads refuse symlinks and nonregular files, enforce a1MiB bound and reject files that change during reading. The final check and rename are separate filesystem operations; this is not atomic compare-and-swap against arbitrary external processes.

The configuration module's nine real-file/parser tests pass40 assertions, and typecheck passes. Independent review found no remaining concrete blocker in that bounded module. `smol-toml`1.8.0 is pinned for parsing; the serializer uses reference formatting where it roundtrips and library escaping otherwise. Private review/provenance: `.data/environment28/`. No script executor, HTTP/settings/composer integration, environment propagation or native UI proof is included in this groundwork. The implementation and acceptance boundaries above remain required.

## Source28 host transport and settings work

Configuration reads/saves now use the existing authenticated workspace transport and command receipts. Queries resolve the owning project or session; the client cannot supply a root directory. The host advertises configuration support separately from execution. A real-host test covers unauthorized requests, foreign-project paths, revision conflicts, repeated command receipts and restart without reverting newer file contents. It creates no sessions or prompts and confirms saving does not execute the script.

The settings editor and sidebar now use this transport. Editor state stores per-host/project catalogs, unsaved edits and original pending save identities in the desktop cache; offline editing, stale writes and lost receipts have focused state tests. Git and environment settings routes now restore as their own pages, rather than Git reopening as OMP. The focused combined checks pass50 tests with266 assertions, and typecheck passes. A hidden Electron component check exercises native pointer/text input, save, reload with unsaved edits, and invalid-TOML repair against the actual authenticated host and files. It observes two distinct successful saves, zero sessions and no script execution. Native Select All supplies the raw-repair replacement; keyboard-shortcut coverage is not inferred. An ownership-specific read API allows bounded repair of invalid TOML without passing absolute configuration paths through the relative workspace-file API.

The editor uses the reference breadcrumb position,768-point form,288-point name field and128-point script fields; setup and cleanup have independent platform tabs. Its five controlled captures and production-App regression (16 groups,13 captures) pass. These are component/controlled-transport checks, not installed-window or registered pixel parity. The settings sidebar inventory, project landing, action/icon controls, typography, colors and focus/hover states still require native comparison. Variables help now uses a native popover with an explicit CSS anchor; a sixth controlled capture verifies right alignment, a4-point gap, viewport fit and Escape dismissal without shifting the form. Native paired appearance is still unverified. Private evidence: `.data/environment28/editor-layout/` and `.data/environment28/app-regression-final/`.

The sourced-script runner has real shell tests for successful exported-environment capture, explicit exit, pipeline failure, nonzero exit, output bounds, and timeout/abort of owned process groups including background children after the parent exits. It does not enable worktree setup by itself. Setup/cleanup lifecycle receipts, configuration selection in submissions, private environment propagation to OMP/terminals, named actions and native UI comparison remain required.


## Source28 lifecycle storage and private worker environment

The preparation ledger accepts the owning HostStore database so receipt and preparation writes can share a transaction. It preserves the exact configuration revision and private setup result, rejects stale transitions, and only explicit startup reconciliation marks dispatched operations unknown. Reconstructing an accessor does not reconcile a still-running operation. The public projection omits scripts, output and captured environment values. Actual SQLite coverage includes rollback of a preparation transition together with a command receipt.

`WorkerRuntime.create/open` now accepts a separate host-private environment argument applied before spawning the worker. It never enters the native init/IPC payload. A real no-provider worker check verifies create/reopen receive allowed exports and unsets while ordinary sessions and discovery remain unchanged; host-owned profile/home/worktree variables stay protected. These checks do not establish authentication isolation or provider behavior. The runner, ledger and environment checks pass17 tests with93 assertions.

The storage/worker checkpoint initially preceded route integration. The subsequent host route results below establish creation/reopen/removal behavior; composer recovery, terminal propagation and named-action execution remain unfinished.


## Source28 real worktree lifecycle and rollback compatibility

The host-owned preparation store now shares SQLite transactions with command receipts. Its first persisted preparation raises the database to schema5; earlier schemas remain unchanged until that feature is used. Interrupted dispatched operations become unknown once at host startup. A private session lookup restores captured exports for the owning session only. Packaging declares schema1–5 compatibility; an actual schema5 preparation database is refused by older artifacts without modifying its bytes, mode or modification time.

The lifecycle coordinator validates exact configuration bytes before creating the managed directory, records dispatch before Git or shell effects, and preserves failed setup for an explicit revision-checked retry. Reobserving the original preparation identity never reruns setup or worktree creation. Cleanup uses the captured configuration even if its source file changes, and a failed cleanup retains the directory and Git registration. Actual Git/shell tests cover successful setup/cleanup, failed setup and explicit retry, stale configuration rejection, and a real worktree creation followed by an injected lost receipt. The lost-receipt case records unknown, creates no session and performs no second Git dispatch. Source index bytes, HEAD and files remain unchanged.

These coordinator and store tests establish effect ordering in disposable local worktrees. They do not yet establish the server create/resume/removal routes, composer behavior or native UI parity. Those integration gates remain open.


## Source28 captured environment draft protocol

Environment selections now carry their project, configuration path and exact revision through host drafts and the desktop cache. Explicit No environment is stored as null and retains the new format. Protocol5 rejects older endpoint delivery and prevents old writers or consumers from dropping the saved selection. Conflicting edits remain recoverable; consuming an older prompt cannot clear a newer environment choice. Project changes clear a selected configuration to explicit null.

Pending worktree submissions capture the exact environment and draft revision before delivery, restore them after restart, and retain the original command identity after uncertain delivery. Local-mode creation sends no environment script selection but retains protocol5 for subsequent draft consumption. The draft checkpoint advertised configuration support only. The subsequent route integration below accepts explicit protocol5 environment requests; the composer selector remains gated until its recovery flow is connected. The combined host, transport, renderer draft/submission and existing attachment/permission/worktree regression checks pass66 tests with419 assertions; typecheck passes. These tests include an actual authenticated host save/restart with zero sessions and no script execution, alongside controlled state tests; they do not prove environment execution or native UI parity.


## Source28 native host creation, recovery and cleanup

Authenticated protocol5 worktree creation now validates the captured saved draft, project, model, permissions, starting state and environment revision before effects. It records each preparation phase, runs setup, and only then creates an actual OMP worker with private exports. A successful native identity, session row, preparation binding and command receipt commit together. Replaying a completed request returns that receipt. A post-commit state-publication failure cannot replace the already-durable success with a failure response. Setup failure instead returns a redacted preparation receipt; a distinct revision-matched resume command may retry a known phase. Unknown outcomes remain inspection-only and cannot replay native creation or setup.

The owning host exposes read-only preparation status under its existing authentication. Native worker reopen restores the bound private environment, including after failed cleanup. Managed removal checks registration/ownership, reserves the worktree, runs captured cleanup before Git removal and records successful removal. Cleanup failure prevents removal; later removal requires a new explicit command. Setup is aborted when the host stops. No script output or exported values enter ordinary status/command receipts.

The targeted route/lifecycle/store/workspace/compatibility/transport checks pass67 tests with611 assertions. A subsequent actual-host regression passes46 assertions and additionally injects one state-publication failure after native commit. It uses actual Git, sourced shell scripts and production OMP workers with outbound fetch blocked, proving one failed setup, one explicit retry, one native session, restored exports after host restart, failed cleanup retention, explicit successful removal and unchanged source HEAD/index/README. The newer unsent draft remains revision2; no user prompt or provider response is involved. This is local source-runtime evidence, not installed Work or terminal propagation proof. Private records: `.data/environment28/environment-routes-final.log` and `environment-post-commit-notification.log`.

The composer selector now sits between execution and branch controls and uses a separate host/project catalog of names, paths and revisions. Its offline cache omits scripts and actions. Loading/error/stale states preserve the selected revision; cached choices remain available offline, and sending requires an exact current revision. Settings roundtrips retain the original choices and text. The host advertises explicit commandVersion5 execution support separately from configuration support.

A preparation card observes status through an authenticated project-owned query. Observation never starts setup or sends input. Known setup failure offers an explicit retry; unknown effects remain inspect-only. Pending storage preserves the original create/resume/prompt envelopes, captured draft and preparation identity. Monotonic observations can recover a committed session without creating another. Polling before create success or recorded failure cannot discard the original ownership envelope; lost prompt receipts retry the same input. A newer draft stays independent.

The focused catalog/submission suites pass29 tests with245 assertions. The owning-host query additionally rejects unauthenticated and foreign-project reads. A hidden Electron production-App acceptance exercises real pointer/text controls, selected environment/settings return, an actual shell setup failure, a newer edit, renderer reload and explicit retry. Actual host/Git/OMP evidence shows one create, one resume, one original prompt, one managed worktree, two setup attempts and unchanged source HEAD/index/README. The original prompt is delivered once to an in-process deterministic provider; the newer editor draft remains unchanged. This proves controlled App delivery behavior, not a hosted provider, credential isolation, installed macOS window or registered native visual parity. Private evidence: `.data/environment28/composer-runtime/`, `catalog-and-recovery-final.log`, `composer-host-tests.log` and `composer-scope.md`.

Setup output/cancellation, named actions, native configuration discovery/import, explicit reconciliation after uncertain effects and paired native UI acceptance remain open.

## Source28 private terminal environment

Both terminal adapters resolve setup values from the owning session’s preparation record. The private argument is separate from the public create options and is checked against the canonical terminal working directory before launch. Project-owned terminals receive no session setup. Protected host/profile keys and terminal transport variables retain their existing ownership.

The raw-PTY path builds one child environment. Native tmux uses a0600 launch script inside its0700 private store, applies the original allowed set/unset operations and owned-directory aliases, and executes the configured shell. Applying the original operations matters when an adopted tmux server has older global values. Export values never appear in process arguments or public catalogs, and no global tmux environment is changed. The script deletes itself before exec; cleanup cannot assume that the CLI’s launch acknowledgement means the shell has already opened it. A crash between writing the payload and persisting its terminal record can leave a private orphan; startup-wide deletion would race another launcher and is deliberately absent.

The same31 tests/327 assertions pass against actual macOS and Linux bundles, including authenticated session/project terminal creation before and after host restart, explicit unsets, unusual values, unchanged daemon/server environments and native adoption. Deckbox’s installed service remains unchanged. This proves source-runtime propagation only; installed UI and paired native acceptance remain open. Private source hashes, logs and service-continuity receipts are under `.data/environment28/linux-terminal/`.

## Source28 live setup output and cancellation

Setup and cleanup now share an owning-host run controller. Each dispatched run records its preparation revision, bounded stdout/stderr, truncation, cancellation request and completion in a separate private SQLite table. Output has its own sequence and does not advance the preparation revision. An authenticated, project-owned `environment.output` query reads it explicitly; ordinary preparation projections, draft caches and host snapshots still omit output and exported environment values. Script text deliberately printed to stdout/stderr remains visible in this output view.

`session.environment.cancel` requires the owning project, preparation identity and exact running revision. It uses the command receipt ledger but bypasses the queue containing its original create operation, just as an interrupt must. Only the live owner can abort; stale or restarted/unknown operations cannot be cancelled or replayed through this path. Known cancellation settles as failed setup with a cancelled result, preserving the worktree and original submission for explicit recovery. Completed duplicate cancellation returns its original receipt, including after host restart.

The composer offers More details/Less details and Cancel while the setup or cleanup script is running. Failure expands details; output reads never dispatch work and are not saved in desktop caches. Lost cancellation replies retain their command identity for an explicit Check cancellation. The owning-host capability gates these new controls on older hosts. Native source confirms disclosure, failed details and cancellation behavior; no matched pending/failure screenshot exists in the frozen bundle. Worktree checkout/fetch output and percentages, cancellation outside the script phase, native Auto-fix and exact surface parity remain open.

The initial hidden Electron App run passes10 checks/9 captures: live stdout/stderr, one real cancellation, preserved original/newer drafts through reload, one explicit retry, one OMP session and one original prompt. Source HEAD/index/README stay unchanged. This is controlled App/source-runtime evidence with a deterministic local provider, not installed or native pixel parity. The initial HTTP fixture omitted its draft execution mode and was correctly rejected before setup; the corrected22-test suite passes276 assertions, including ownership/revision/authentication rejection and durable output/receipt after restart. Private evidence: `.data/environment28/composer-cancellation-01/`, `output-host-tests.log` and `output-host-final.log`.

The final candidate repeats the10-check/9-capture App flow after the post-exit cancellation race was fixed. macOS source checks pass31 tests/304 assertions; the portable HTTP assertion additionally passes34 assertions. Deckbox passes26 tests/280 assertions with the same final host source and unchanged installed service/runtime. Its initial assertion expected no shell-added termination text; the corrected check preserves that native diagnostic and matches it to the stored setup result. Final proof: `.data/environment28/composer-cancellation-final/`, `output-final-tests.log`, `output-cancel-portable.log`, and `linux-output/verification.json`. Capability names are explicitly `scriptOutput` and `scriptCancellation`, leaving whole-worktree cancellation unclaimed.


## Source28 configured action execution

The environment card's Actions menu and primary Run control now use authenticated host-owned action catalogs. Catalogs expose names, icons, original action indices, platform availability and configuration/selection revisions; script bodies stay in the configuration service. Each canonical workspace directory remembers an explicit environment or No environment through revisioned host metadata. A missing or invalid selected configuration stays visible as an error instead of silently switching. Projectless sessions resolve their own cwd; managed sessions retain their captured source environment. Catalog discovery is still limited to the app's project environment directory.

Run accepts only an exact catalog reference, validates the assembled input before creating a terminal, and reserves the workspace during startup. Its command identity is persisted through the existing version5 ledger, so repeated delivery returns the same receipt. Old routes refuse the new intent. First persisted use raises the database to schema5, preventing an unaware older host from adopting action restart state.

A private action identity associates host, target, canonical cwd, configuration path and original index with one native terminal. Explicit Run replaces that pane's configured shell process and submits the current command in its owning directory. It carries only that session's private setup exports/unsets; other sessions and project terminals are unchanged. Existing viewers detach and attach to the new generation of the same terminal. A definitely closed action may reopen on a new native pane with the same terminal identity. An interrupted/lost restart acknowledgement remains unknown across host restart and never triggers an automatic replay. Inspection, close and forget are required before a new action can replace that uncertain identity.

Native viewer recovery now ignores late failures from an invalidated attachment and services the queued refresh after its earlier operation settles. This corrects the expired-attachment banner observed after Run without retrying user input. The controlled App harness checks actual shell marker counts and visible xterm output, not command echo or hidden accessibility text. It exercises menu placement/focus, Escape restoration, menu Run and primary Run; four captures show the second output in the same terminal with no recovery banner. The initial premature second click and later transient-error captures remain preserved as superseded evidence.

Source references establish the environment header controls, icon paths, stable action terminals and explicit shell restart. The frozen bundle provides action-editor/icon/hover/result states, but the current controlled fixture does not match their content, outer-window geometry or native capture method. Its six checks establish real App interaction and shell execution only; full native screenshot comparison remains unverified. Evidence: `.data/environment28/actions-scope.md` and `actions-ui-1788700202434/`.

The integrated macOS action/runner suite passed66 tests/508 assertions; a later close/reopen check passes19 tests/208 assertions, and final renderer/transport regressions pass21 tests/157 assertions. The same final action/native-terminal source passes28 tests/224 assertions on Linux. All515 transferred source files match their Home originals and the installed Deckbox service/runtime remain unchanged. Typecheck passes. The runner separately preserves multiline exported values and no longer overwrites legitimate script variables while capturing its EXIT result; those real-shell cases are included in the integrated suite.

Remaining work includes native discovery/import and subdirectory selection rules, worktree Git configuration selection, action recency/primary ordering and keyboard shortcuts, setup-action entrypoints, offline action catalogs after desktop restart, whole-worktree progress/cancellation, Auto-fix and uncertain-effect reconciliation. Native/installed acceptance and exact theme/hover/icon/pixel comparison remain separate gates. Release28 was subsequently frozen at `cf18cef`, validated and handed off for isolated native verification; it has not replaced the installed services.

## Source29 existing project configuration compatibility

The existing configuration service now also reads direct `.codex/environments/*.toml` files in the selected project. They appear beside app-authored `.agent-desktop/environments` files in settings, composer selection and the environment action catalog. Explicit editing updates the selected file in place, with the same revision conflict handling and permission preservation. New app-authored files still use the app directory; no migration or automatic copy occurs. Default filenames sort before other filenames, then by path. Malformed files remain visible as errors.

One shared resolver handles both namespaces and equivalent project aliases. It canonicalizes the project ancestor without following either storage directory or the file, then revalidates the captured project and rejects storage symlinks. Catalog/read/save do not execute scripts. Actual setup, captured cleanup and actions continue through their existing ownership and command receipt paths; this adds no new persisted execution semantics or schema.

The final source checks pass27 tests/168 assertions covering both namespaces, authenticated catalog/edit, stale saves, aliases, symlinks, actual Git worktree setup/cleanup, private exports, unchanged source repository and no replay after an uncertain Git receipt. Real native terminal checks separately cover both namespaces, repeated actions in one terminal, lost reply recovery and restart persistence. Typecheck and the focused independent resolver review pass. The initial HTTP assertion used the temporary path's alias instead of the host's canonical path; the corrected fixture uses the canonical project path. This is source/runtime evidence, not installed or native visual parity. Private evidence is under `.data/environment29/`.

The pinned source trace establishes a broader remaining contract: ancestor discovery through the first Git boundary (at most50 directories, no descendants), an inherited settings group, native default file/name generation, and worktree-local configuration materialization/selection. The native setup call runs at the worktree Git root; the catalog also carries a relative directory field whose remaining consumers need tracing. Parent choices are not exposed yet because the current workspace service requires a Git-root project and preparations capture that project's root. Managed configuration copying and worktree Git selection also remain open. Root-only compatibility is a completed subset of this work, not full discovery parity.

## Source29 ancestor catalogs and project cards

Ancestor catalogs now scan both configuration namespaces from the selected project upward, including the first directory containing a Git file or directory, and stopping at that boundary, the filesystem root or50 directories. There is no descendant search. Default filenames sort first globally, then directory distance and path. The resolver only accepts configuration paths in that discovered set and rechecks the boundary for edits. Concurrent saves through parent, child and aliased projects serialize by the actual configuration directory, so a stale edit becomes a conflict. New files remain in the selected project; explicit inherited edits update the original ancestor file.

The settings overview now uses project cards instead of a second project sidebar. Each card shows its own configurations and a collapsed inherited group with the source folder and filename. The project header opens its default configuration, and its plus button starts a new configuration with the native project-derived initial name. Project editor state, pending receipts and unsaved edits remain separately owned. The notebook icon comes from the pinned settings caller. New-file path generation still uses the app's existing naming convention and remains a parity gap.

Source and authenticated-host suites verify discovery boundaries, both namespace formats, inherited conflict handling, error entries and existing lifecycle behavior. Three real native-terminal cases cover app, native and inherited configurations, including repeated action execution, original-receipt recovery and host restart. A hidden Electron run exercises two project cards, inherited expansion/edit/save, unsent editor restoration, malformed-file repair and cross-project draft preservation. Its11 captures and real host file checks prove three distinct saves, the ancestor updated in place, zero sessions and no setup execution. Typecheck and independent review pass. Evidence is private under `.data/environment29/`, particularly `inherited-settings-final/`, `inherited-integration.log`, `inherited-native-actions.log` and `nested-lifecycle-boundary.log`.

These are controlled component/source-runtime checks, not installed native acceptance or exact pixel parity. The reference bundle has a project-list capture but no matched inherited-state capture; the inherited interaction is supported by the pinned source. Settings sidebar colors and spacing, summary-versus-edit navigation, remaining editor controls, native filename generation and a complete native comparison remain open.

Local projects can use inherited action configurations now. Creating worktrees from nested project directories remains unsupported by the existing Git workspace service. A real Git regression verifies that this request is rejected before preparation storage, managed-directory creation or script execution, leaving the source index unchanged; that rejection proves a boundary, not feature completion. The next integration must distinguish repository root, selected project directory and managed session directory, then implement worktree configuration materialization and persisted selection. No incorrect execution directory or new persisted schema is introduced by discovery alone.

## Source29 saved summary and edit navigation

Opening a saved configuration now shows its environment name, setup and cleanup summaries, and saved actions. Edit reloads a clean file from its owning host before entering the form; a failed read leaves the summary visible and does not enable an overwrite. Unsaved and offline edits retain their original revision and text. The project breadcrumb returns to the saved summary without discarding those edits, and the root breadcrumb returns to project cards. A completed save returns to the summary unless newer edits arrived while the request was pending. View state survives desktop reload, including recovery of older editor caches.

Setup and cleanup each have independent Default/macOS/Linux/Windows tabs when any script is configured. An empty platform override falls back to the default with an explicit notice. Script snippets use selectable highlighted code and Copy code; action rows show the first command line and disclose the original multiline command. Viewing, editing and saving do not run scripts. The owning-host catalog refresh updates clean cached summaries while preserving dirty drafts.

The pinned7982 source supports these controls and navigation rules. The frozen7868 bundle has overview/editor captures but no matching saved-summary, fallback or multiline-action capture. A controlled hidden Electron run produced17 content captures through real pointer/text input and authenticated disposable host APIs, including three file saves, inherited editing, malformed-file repair, cross-project unsent edits and reload recovery. It created no sessions and executed no scripts. Typecheck and26 renderer/state/Markdown tests pass. Evidence is private at `.data/environment29/summary-settings-03/`, `summary-reference.md`, `summary-tests-final.log` and `summary-typecheck-final.log`.

This remains source/runtime evidence, not installed native acceptance or exact pixel parity. Copy clipboard delivery was not exercised in that run. Invalid-file repair still uses the app's revision-guarded raw TOML editor, whereas the pinned source initializes a replacement form. Global settings appearance, remaining editor control details, native filename generation and nested-worktree materialization/selection remain open. The immutable reference bundles and preserved Work installations/runs were unchanged.

## Native release28 checkpoint and source29 follow-up

The Work verifier completed the single authorized release28 v2 run at frozen `cf18cef`: actual setup details, one model-invoked README read, two project actions, two session actions carrying the private setup value, and one separate setup cancellation. Repeated actions retained their terminal identities; refresh and panel hide/show did not increment execution counters. Cancellation retained the original draft and both worktrees, with the source Git bytes and refs unchanged. These are the verifier's scoped native results; provider/account coverage and whole-app visual parity are not implied. The separate release28 delivery is sealed:21 native PNG/AX pairs,90 files,89 recorded hashes and11,642,683 bytes. Home independently verified every recorded hash and the checksum-manifest digest. Its gallery is `.data/codex-screenshots-refresh-release28-2026-09-06/index.html`; through27 remains immutable.

Two native qualifications remain: the session's primary Run appeared only after opening Actions, and a previously observed DONE line was missing after the interval containing terminal fitting from120×40 to145×8 and recovery actions, while the preceding output remained. The samples are278.717 seconds apart and include prompt settling, refresh and hide/show; they do not isolate resize causation. Read-only copies of the verifier's action/recovery evidence are in `.data/environment29/work28-readonly/`. Source29 addresses initial action-catalog connectivity separately. Simple Bash/Zsh resize regressions do not reproduce the Work output loss; they cannot certify that defect fixed.

## Corrected nested-workspace reference contract

The deeper pinned7982 trace supersedes the earlier interpretation that all setup runs at the worktree Git root. The selected source project directory supplies Git's relative prefix; the new session uses that prefix under the new worktree Git root. Separately, the selected environment's owning folder supplies its own prefix, and setup, cleanup and configured actions execute at that environment-relative directory. Source/worktree path variables identify the selected workspace directories. A project such as `repo/apps/web` can therefore run its session under `new-worktree/apps/web` while an inherited root environment runs setup at `new-worktree`.

At the initial reference checkpoint the implementation supported root worktree projects only. The source29 integration below now records these distinct directories and materializes selected configuration according to branch/ignore state before enabling nested sessions. The corrected static trace is `.data/environment29/nested-reference.md`; execution evidence is separate.

Primary Run discovery now waits for the owning `WorkspaceState` connection, rather than relying only on the desktop connection prop. The catalog also reloads after reconnection when a previous value is present. A mounted production-component regression delays owner readiness against an actual authenticated host: the previous effect leaves Run absent with no host query, while the corrected effect exposes it without opening Actions and refreshes it on reconnect. The companion App/native-tmux run still executes two actions in the same terminal. These source checks do not retroactively change frozen28's native result.


## Source29 nested directory foundation

The workspace service now exposes an explicit canonical Git root and selected-project prefix, plus a separate root-scoped service preserving the existing host limits. It rechecks source-directory resolution and keeps the original file and Git boundaries intact. A directory context maps the selected workspace and inherited/local configuration folder independently into a future managed checkout. Resolution uses the existing configuration discovery and no-symlink read rules. Post-checkout verification rejects missing, symbolic or non-root targets; it does not create a substitute directory, fall back to the Git root or establish host ownership by itself.

Real Git tests exercise root, nested and linked-worktree resolution, redirected roots, inherited/local configurations, unchanged source index/HEAD/status, a branch omitting the project, and a target directory redirected through a symlink. They do not create OMP sessions or execute setup, and no native appearance claim is made. Existing lifecycle tests continue to require rejection of nested requests before effects. No schema or production lifecycle dispatch changes are introduced by this foundation.

The next integration must capture these paths behind a new schema fence, use the selected workspace for session cwd and path variables, use the configuration folder for scripts/actions, and retain the full managed Git root for ownership and removal. Configuration materialization and worktree-local selection must precede enabling the path. Managed-session binding and terminal environment validation currently assume equal Git/session/script directories and must be updated together; simply removing the Git-root guard would be incorrect. The final37-test suite passes272 assertions and typecheck passes. Evidence: `.data/environment29/nested-context-final.log`, `nested-context-scope.md` and `nested-reference.md`.

## Source29 integrated nested worktrees

Version2 preparations retain the original source selection and a validated directory context. Their first write atomically promotes host state to schema6; existing version1 records keep their prior semantics. Worktree creation resolves the containing Git repository, verifies registered managed ownership, materializes the selected configuration, and records its exact effective snapshot and worktree-local Git key before setup. A selected tracked config absent from the starting branch becomes no environment; exclusive copies and retained-source fallbacks follow the checked ignore rules. Missing nested project directories retain an uncertain preparation and checkout without running setup or substituting the Git root.

The actual OMP session uses the managed nested workspace cwd. Setup and cleanup use the selected configuration's logical owner, while injected source/worktree variables continue to identify the selected workspace. Cleanup reparses the current menu selection, checking its selection revision before dispatch. Git controls explicitly use the containing repository; ordinary file and terminal operations retain their selected-workspace boundary. Configured actions receive separate checked Git-root authority, record their actual script cwd, and preserve nested workspace variables. Their reservation covers the managed root, and locked or unrelated managed checkouts are rejected.

The integrated Git/SQLite/runner/native-session/compatibility suite passes77 tests/537 assertions. The subsequent cleanup and authenticated host regression passes9 tests/162 assertions, including newly selected nested cleanup, restart recovery and duplicate receipts. Actual bundled tmux action/manager checks pass33 tests/222 assertions without skips; final action-ownership refinements pass11 tests/34 assertions. Authenticated native-terminal action checks pass3 tests/96 assertions across both namespaces and inherited configurations, including host restart and durable renderer retry. No provider prompts are needed for these checks. Initial failures preserve the old test expectations: OMP's title slot precedes its semantic session header; inherited actions now write at the config owner; copied configs remain dirty files after cleanup. Corrected assertions verify actual identity/locations and preservation rather than bypassing these rules.

Remaining work: later menu selection changes currently persist in SQLite but are not mirrored back to the worktree Git key. Copied untracked configs prevent clean-only removal until explicitly preserved or cleaned; this integration does not discard them automatically. Installed/macOS UI and Linux execution of this new schema6 batch, matched visual comparison and the broader milestone gates remain open. Frozen28 and the separate runtime-owned/schema1–5 artifact remain unchanged. Private source evidence is under `.data/environment29/nested-*`.

Final full-source regression:750 tests passed,29 platform/bundle cases skipped, zero failures,26,039 assertions across147 files. Typecheck and independent lifecycle review pass. The separate explicit-bundle runs above exercise the native manager/actions and authenticated terminal cases; the full-suite skip count is not presented as platform acceptance. Final full-suite evidence: `.data/environment29/nested-full-source-final.log`.

Linux source verification of committed274caf8 passes93 tests/821 assertions across13 suites with zero skips or failures, including actual idle OMP sessions, authenticated host routes and bundled native tmux. The source files were hash-checked in a separate Deckbox temporary directory before and after execution. The installed host remained active with the same PID and runtime hashes; no installed update or provider prompt was needed. Evidence: `.data/environment29/linux-nested/verification.json` and `tests.log`. This closes Linux execution for that commit, not installed desktop appearance or later changes.

## Source29 durable menu selection

Later environment selections now use the managed Git root as their shared identity, including ordinary same-worktree sessions. Catalog, selection and action admission serialize on that root. A selection acquires the host mutation reservation before its SQLite CAS; SQLite records the desired value before the worktree Git key is mirrored and verified. Explicit no-environment writes `__none__`. Matching Git values require no write, and existing source/common config values are preserved.

SQLite and Git are not presented as one transaction. A failed mirror reports an uncertain result with the desired selection retained. Catalog/reopen repairs that value without advancing its revision; action admission rechecks the current selection before dispatch. Cleanup reads the same managed-root key. Existing version1/project choices keep their cwd identity. The new schema6 path remains unreleased; prior installed schemas are unchanged.

Three added service tests use real Git and SQLite with session-metadata fixtures, covering shared-target CAS, stale requests with unchanged Git bytes, config-lock failure/reopen repair, root reservation and common-config preservation. They do not create native OMP sessions. The combined action/config suites pass22 tests/88 assertions with one bundle-dependent case skipped. A separate actual bundled-tmux run across actions, config, lifecycle and authenticated action routes passes34 tests/286 assertions with zero skips/failures. Independent read-only review found no durable-selection or dispatch blocker. Native app/pixel acceptance remains separate. Evidence: `.data/environment29/selection-real-service-02.log` and `selection-native-final.log`.

## Source29 managed deletion and recovery

Explicit managed deletion now snapshots tracked changes and nonignored untracked files into `refs/agent-desktop/snapshots/<sha1(canonical-worktree-path)>` before cleanup. Copied environment configs are preserved in that snapshot rather than blocking removal. Cleanup failure retains the worktree. A successful cleanup is followed by receipt-checked `git worktree remove --force`; no filesystem-delete fallback follows a Git error. The UI's Delete control uses this path. This is source behavior, not a new native pixel claim.

The snapshot uses a private index and verifies the actual checkout/registration identities. Source and worktree indexes, source files and HEAD remain unchanged. Dirty submodules, hidden index flags and unsupported text/encoding conversion setups fail before deletion rather than receiving a false preservation claim. Ignored files are outside the Git snapshot, as in the reference's untracked-file boundary. Full automatic restoration UI and those unsupported conversion cases remain parity work; do not represent the snapshot as a general filesystem backup.

Immediately before Git dispatch, the host stores a schema6 removal intent bound to the exact command hash, project, source, worktree and snapshot. Successful removal atomically finalizes that intent, marks a successfully cleaned preparation removed, archives linked sessions, and records the command's success. Notifications follow the commit. A post-dispatch failure reports `OUTCOME_UNKNOWN`. Restart reconciliation only observes the retained ref, registration and filesystem; it never repeats cleanup or deletion. It can finalize a proven removed target, while an existing target or missing/changed evidence remains unresolved. Individual unavailable sources or reconciliation failures do not prevent the host starting.

The implementation deliberately retains a worktree when snapshotting fails, and never recursively deletes after failed Git removal. The pinned source can proceed after a targeted Settings snapshot failure and can remove the directory after a force-removal error. These stricter failure semantics are explicit differences. Linked sessions here archive only after successful removal, so cleanup failures leave their previous archive state intact.

Focused actual Git/SQLite/host checks pass39 tests/386 assertions, including a real deletion followed by an injected metadata-transaction failure, unchanged linked state on rollback, exact-command reconciliation after reopening, and generic managed worktrees without an environment preparation. A separate authenticated lifecycle check proves pre-cleanup content and copied configs survive in the snapshot. Evidence and the exact static reference trace remain private under `.data/environment29/`; frozen28 and prior verifier runs remain untouched.

Final raw-file review also reproduced and corrected symlink snapshot handling: valid and dangling links retain mode120000 and their exact link text without following target contents. The real-Git service suite passes27 tests/226 assertions; this supplements the removal-receipt checks rather than implying installed UI acceptance.

The integrated final source passes73 tests/715 assertions across8 files on macOS with the bundled native terminal runtime. The same final host sources pass129 tests/1111 assertions across15 files on Deckbox, with all564 staged source hashes verified and the installed17 PID/runtime hashes unchanged. Initial evidence retains the broad run that had loaded pre-fix symlink code and the Linux test-transfer omission; neither is credited as passing. Typecheck passes. Private final receipts: `.data/environment29/current-final-focused.log` and `linux-final02/verification.json`.

## Source30 action icon popup

The action editor now uses the reference's Tool/Run/Debug/Test popup instead of a generic select. Existing SVGs, a48-point trigger and compact160×122-point menu use28-point rows with16-point glyphs. Null remains stored as null until explicit selection and displays the native Tool fallback. Arrow/Home/End navigation, Escape restoration, Tab/Shift+Tab exit, outside dismissal and viewport collision handling are implemented.

The maintained component passes19 controlled Electron captures at1440×1000/DPR2/zoom1, including actual pointer/keyboard selection and persisted `icon = "debug"`; no script marker or session is created. Typecheck passes. Initial screenshots exposed oversized36-point rows and18-point glyphs; the corrected capture uses the compact dimensions above. Evidence is private under `.data/environment30/icon-picker-compact-1788726190/`. This is source/controlled-component proof, absent from frozen29 and not a registered native pixel comparison.

The same source30 editor now follows the native vertical action-card structure: uppercase NAME, inline icon/name row, ACTION SCRIPT below and single-choice PLATFORMS beside Delete. Pinned source confirms one selected platform, so this does not invent multi-platform storage. The controlled card measures768×280.78 points with12-point padding/gaps, a742×36 name row and305.15×24 platform group. Twenty controlled captures verify pointer selection, native keyboard order, persisted revised name/Debug/macOS, zero sessions and no setup marker. Evidence: `.data/environment30/action-card-1788726604/`; surrounding page placement, script copy and registered native pixels remain separate work.
