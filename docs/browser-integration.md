# Native OMP browser integration

## Follow-up to independently exercised release 18

The Work verifier isolated a navigation-only failure: counter clicks and typing succeeded, while both third-party and same-origin link clicks reached the destination but lost their acknowledgement. Read-only preview refresh restored history controls. The pinned patch now retries only transient post-click CDP observation for up to two seconds, revalidating the exact native owner/tab/target on each read. It never replays the click, substitutes another target, or treats an unverified result as success. Real isolated native-browser contract tests cover this patch; the independently launched release 18 remains unchanged and does not contain the fix.

The source panel also fits the native page to its measured viewport on initial display and local panel-size changes. It does not resize again merely because another client changed the native viewport. Fit can be switched off, and explicit Resize remains available. An uncertain resize is not automatically replayed. Controlled Electron geometry tests and real OMP viewport/JPEG tests verify these behaviors; the next signed pair still requires the Work verifier's navigation and layout pass. Browser annotation, zoom and other unmatched reference controls remain open parity work.

Source investigation, isolated feasibility evidence and source implementation, 2026-09-05. **Source 18 includes a native viewport panel and typed navigation/input controls; full browser parity remains incomplete.** The initial investigation read installed package source, the preserved Codex archive, and official Electron/CDP documentation. The later bounded experiment below opened only its own disposable native browser/profile and localhost page. Neither stage inspected live Codex or other apps, changed installed settings, accessed existing browser profiles, or made provider requests.

## Release 17 metadata transport

The maintained project now selects a small, pinned Bun patch for OMP 18.1.10. `listTabsForOwner` returns immutable snapshots from the native supervisor's actual tab map, filtered by the native session's exact identity. Its original source hash and isolated real-browser proof live under `patches/omp-18.1.10/browser-metadata/`; the selected package patch is `patches/@oh-my-pi%2Fpi-coding-agent@18.1.10.patch`. Host packaging includes the selected patch in its hashed file inventory so a frozen production install reproduces it. Global OMP installations and release 16 bundles are unchanged.

Worker protocol 6 adds a read-only metadata request. `GET /v1/sessions/:id/browser-metadata` uses the existing host authentication and an explicit expected-owner header. Observation never starts a missing native worker. Responses distinguish not-started, unavailable and a running worker snapshot, and name the native target IDs, backend, title, URL and viewport. Desktop parsing validates those fields and their owner/protocol before exposing them. Older route-level 404 responses remain an explicit compatibility case.

The exact-path native test creates a real provider-free OMP worker and reads its empty tab list through the production route class, checking the returned worker PID. A separate isolated test opens a real native browser tab and verifies its target, owner isolation and immutable metadata. Together these establish the metadata seam; they do not establish a renderer stream, input delivery, real tab transport over Tailscale, or a browser panel. The next implementation must still attach to that exact target, keep detach separate from close, and prove shared document state. Worker startup failures from Bun's filename-filter test mode are documented in [test runtime](test-runtime.md), not treated as an authentication limitation.

Installed release 17 verifies the exact patched supervisor hash on Home, Work and Deckbox. Home and Work return actual running worker snapshots with zero tabs; wrong-owner/stale requests fail with 409. Home additionally verifies unauthenticated rejection and no-store responses. Deckbox runs the exact-path HTTP/native-worker tests against isolated archive sources and installed dependencies: five tests, 18 assertions, no provider calls. These checks do not establish nonempty tab streaming across Tailscale. Evidence: `.data/ui-acceptance/home17-installed-contract.json` and `installed-hosts-release17.json`.

## Source 18: native viewport and human controls

The Browser dock now renders periodic JPEG captures of the exact tab held by the owning OMP worker. It uses periodic JPEG captures, with native tab selection, refresh, pause/resume and a stale-image label. Navigation, back/forward/reload, clicks, scrolling, edit keys and direct text input now have typed native operations. Downloads, browser creation, streaming and broader browser behavior remain required work. The Browser action is available for native sessions. Closing a viewer never closes its native tab.

`captureTabViewportForOwner` extends the selected native patch. It resolves the exact creator/name/target, captures only that Puppeteer page's current viewport without activation/navigation/resize, and revalidates ownership before returning. At most eight distinct captures can run, with one per target. Timed-out native operations remain counted until they settle. This is deliberate: releasing a slot while its screenshot still runs would amplify retries. Pinned Puppeteer 25.3 uses a FIFO screenshot mutex and OMP configures a 60-second CDP protocol timeout; queued captures can retain slots for several minutes during a stalled browser. Target/connection closure rejects underlying calls, and worker disposal provides the final process bound. The caller receives its 10-second timeout without closing or detaching the agent’s tab. A narrower cancellable observation connection remains future work; no unbounded retry or forced tab closure is used. Worker protocol 7 requires the selected worker PID. Shared projection bounds JPEG bytes and decoded dimensions and removes fields outside the frame contract. cmux remains explicitly unavailable for this preview.

`BrowserFrameHttp` coalesces simultaneous same-target reads, uses only an existing worker, and rechecks the session/handle after capture. It returns no-store data through the existing authenticated HTTP transport; frames are never command events or journal entries. The main-process reader bounds the body, verifies response owner/session/worker/target and image dimensions, and exposes only the reconstructed frame through preload. The renderer sequentially polls only visible active panels, rejects stale responses, clears pixels on owner/target replacement and retains an explicitly stale frame on network failure. Opening another tab, hiding the dock, pausing, and unmounting do not start overlapping reads.

The actual native pipeline proof runs with:

```sh
bun ./scripts/acceptance/native-browser-preview.ts .data/ui-acceptance/native-browser-preview-new
```

The verified run is `.data/ui-acceptance/native-browser-preview-run1/result.json`, with `initial.png` and `remounted.png`. It starts an isolated real OMP worker and native Chromium tab through a test-only extension, then uses authenticated Bun HTTP routes, the production main transport functions, a minimal acceptance preload and the actual BrowserPanel. Electron decodes a real 8,224-byte 640×480 JPEG. Hide/unmount stop polling; remount retains the native target, URL, cookies and unsent input. Native session disposal reaps the worker and stops that page's heartbeats. All recorded source hashes remain unchanged. This proves the local production preview path; it does not prove physical Tailscale viewing or installed/Linux browser packaging. No provider, existing profile or installed service was used.

Separate controlled renderer tests preserve deferred-selection/pause/worker/owner error cases and actual image decoding at wide/narrow/150% sizes in `.data/browser-panel-acceptance-source18-root/`. The production App check additionally verifies that hiding its actual Browser dock stops polling (`.data/app-dock-acceptance/browser-source18/`). Controlled fixture pixels do not count as native-browser evidence. The native worker and isolated patch proofs are maintained under `apps/host/src/omp-workers/browser-frame-native.test.ts` and `patches/omp-18.1.10/browser-frame/`.

## Same-target navigation and input

The selected OMP patch exports `performTabHumanActionForOwner`. It binds each operation to the exact owner, target and captured main-document loader ID, CSS viewport size and scroll position. Navigation may change scroll; pointer/key/text operations reject stale scroll or document context. Capture validates its context before and after taking the JPEG. Native browser runs and human mutations exclude one another synchronously; a human action waits for an existing viewer capture instead of failing because its own preview is reading. Pending work remains counted after a caller timeout. Typed text uses native `Input.insertText`, and modifier releases run in `finally`. Human actions do not fabricate agent tool or approval entries.

Worker IPC 8 and the authenticated `/browser-control` route expose only bounded typed operations. Each host process creates a control epoch returned with frames. A request carries that epoch, a fresh-frame timestamp and an action identity; concurrent retries share one ephemeral receipt, changed payloads cannot reuse an identity, and requests from a prior host process or expired frame are rejected. Receipts are bounded in memory and retain no typed input payload. No uncertain action is automatically replayed. Native preflight refusal is `rejected`; errors after possible side effects are `unknown`.

The viewer maps rendered image coordinates to the captured CSS viewport. A focused offscreen textarea captures direct text and composition; a bounded serial queue preserves edit-key order and uses completed receipt context for following actions. Address drafts survive preview refresh. A synchronous halt prevents queued input from crossing an uncertain result; unsent input moves into an explicit recovery area and cannot resume silently when polling succeeds. This local recovery is not a persistent browser-input journal. Actual OS IME, clipboard, drag/select, rich browser keyboard shortcuts, native dialogs, uploads/downloads and physical remote viewing remain separate acceptance work.

The isolated native candidate passes six groups at `.data/temp/omp-browser-control/candidate-check-xbyNLr/run/result.json`, including navigation, exact document state, stale requests, native/human exclusion, capture sequencing and retained timeout state. The production HTTP/main transport checks distinguish duplicate, rejected and uncertain requests. The control-specific Electron fixture in `.data/browser-controls-root-compact/` uses actual `webContents.insertText` and key events to verify serial typing, address preservation and unknown-result recovery. Earlier failing runs remain recorded: an edit event crossed an uncertain response before React committed its disabled state; the synchronous halt fixes that race. These controlled checks do not establish native or physical browser interaction by themselves.

Run the combined real-worker/HTTP/main/renderer path with `bun scripts/acceptance/native-browser-preview.ts OUTPUT --controls`. It verifies native counter changes and unsent input by reading the page back through OMP after the desktop closes its viewer. The final run passed in `.data/ui-acceptance/native-browser-controls-final/result.json`: four UI phases, 14 authenticated HTTP requests, four completed native actions and three actual renderer captures. OMP independently read counter `1` and the exact appended unsent input from the original document. Viewer hide/unmount stopped polling; session disposal reaped its worker and native page. Source hashes stayed unchanged. This is local source acceptance, not installed or physical cross-device browser acceptance.

The final frozen source suite passes **499 tests / 24,313 assertions**, with 17 skips and no failures across 102 files (`.data/source18-controls-full.log`); all 427 source hashes match before/after. The fresh external frozen install/build and exact native source reproduction pass in `.data/temp/source18-browser-controls-repro-agent-desktop-source18-controls.jfP6hl/result.json` without changing root dependencies.

A subsequently available private reference bundle adds 66 browser captures, indexed at `.data/browser-parity-scope-2026-09-05.json`. Its helper baseline is build 7868, while the pinned archive is 7982. None is yet paired as a matched visual acceptance result. The loaded-page reference makes remaining structural work explicit: native browser tabs belong in the dock tab strip, the idle address is centered, history buttons expose their true enabled state, and the page uses the panel viewport. Current viewport preview and generic Browser dock tab are intermediate behavior. Follow the project-local verification skill for per-file geometry and reference-build differences; do not count the native fixture as visual parity.

## Recommendation

Use **the exact browser target OMP controls**, on the session's owning host, and display that target in the Electron panel through a host-side CDP frame/input bridge. The panel is a new viewer/controller for the same live document, cookies, page history, and JavaScript state. A second Electron view loading the URL creates another document; it does not meet this requirement.

OMP supplies browser automation and ownership machinery, not an embeddable desktop browser widget. Its native backends are Chromium/Puppeteer, an existing browser/Electron CDP endpoint, the Chrome relay extension, and cmux WKWebView surfaces. The first vertical slice should join **native OMP-created Chromium tabs** to the panel, prove actual shared target identity and input, and then add existing-browser backends through explicit capability gates. cmux has a different transport and must stay visible as an unsupported live-stream backend until separately implemented. These are proposed slices, not exclusions from the goal. [O1, O2, O7, O10]

The important correction to an earlier assumption is that **our SDK workers do not currently get OMP's shared browser broker automatically**. Native `registry.ts:176–184` selects that route only for a compiled OMP binary or a declared self-dispatching CLI worker entry. `pi-utils/src/worker-host.ts:11–29` explicitly excludes ordinary SDK embedding; our worker entry does not declare one. Therefore the current default path is a per-process launch with a temporary profile. Adding the declaration alone would be wrong: that entry must actually dispatch all native worker selectors. [O2, O5, A1]

## Verified source baseline

`@oh-my-pi/pi-coding-agent@18.1.10`, release commit `f241301c83726afe75a847e919b89977a54dafbe`. All **32 files** in installed `src/tools/browser.ts` and `src/tools/browser/**` were byte-compared against that exact git commit. This extends the earlier [release provenance](../.reference/omp-18.1.10-release/README.md); it does not substitute the preserved main snapshot.

| Source | SHA-256 |
| --- | --- |
| `src/tools/browser.ts` | `bcfaad44c5d0fdf2cba89a22e0d01ae5799556394c5ffc65db433d3e4515c7f1` |
| `src/tools/browser/registry.ts` | `b55fe741a5887199d4b43c358fb10ebd82c0f050add1caa4db69e73d313c376b` |
| `src/tools/browser/tab-supervisor.ts` | `9215d5bed2a0f1069216db5d4691a2e72b1bd599914d46854c7cfaba7f1c3f9e` |
| `src/tools/browser/tab-worker.ts` | `5e1fd0c13ebf6012b09a0ef65519574b8bf6c1f3e0a7351e39d433561207f9e6` |
| `src/tools/browser/shared-daemon.ts` | `89360653b5cf8eacd72eaeac285b630343b30a52dd61129b34814b5378918324` |
| `src/tools/browser/relay/bridge.ts` | `544ca39666e5466bd47f794c52c03286d451c985e3ad4692419651e3056a6116` |

## Native behavior and the useful seam

| Area | Exact pinned behavior | Consequence for this app |
| --- | --- | --- |
| Agent API | An enabled Eval prelude named `browser`; actions `open`, `close`, `run`, `call`. JavaScript and Python helpers include navigation, ARIA/observations, screenshots, extraction, selectors, keys, scrolling, dragging, uploads, and evaluation. `tab.run` exposes real Puppeteer `page`/`browser` and Bun/Node/tool access. It is not sandboxed. | Keep the native prelude and declarations. Do not replace it with a second tool whose tabs differ from the panel. [O1:48–152; O6:227–325; O14:1–30] |
| Backend resolution | Explicit `app.cdp_url`, then `app.path`; requested/configured relay, configured `browser.cdpUrl`, available cmux, then default Chromium. Relay and cmux environment switches can override their settings. | Show the resolved backend, including configured-but-unavailable errors. Changing a preference must not silently move an existing named tab to a different browser. [O1:100–138,207–213; O10; O11] |
| Native settings | Seven keys: `browser.enabled=true`, `browser.cdpUrl`, `browser.relay=false`, `browser.relayUrl`, `browser.headless=true`, `browser.cmux=true`, `browser.screenshotDir`. | Already covered by the 484-setting descriptor UI. These settings do not constitute a browser panel, durable tab catalog, or site-permission UI. [O12:4480–4557] |
| Default Chromium in this SDK | One module-local browser registry per session process; default headless handle launches Chromium. Explicit temporary user-data directory is removed on disposal. Tabs in the same process share the browser's default context. | Present this as temporary browser state until a deliberate native persistent-profile lifecycle is implemented. Browser cookies are separate from OMP model OAuth/auth-broker credentials. [O2:79–95,176–196,339–373; O5:460–495; O8:704–721] |
| Native CLI shared mode | One broker-supervised headless/headed Chrome per canonical project directory; stable profile under `daemonRuntimeDir(projectDir)/omp.browser.{headless,headed}.profile`. Process clients attach; last client/broker cleanup governs the daemon. Actual registry caller fails when shared acquisition fails, despite the helper's outdated fallback comment. | Reuse a supported cooperating native broker entry if adding this lifecycle. Do not silently fall back to private Chrome after claiming shared/persistent mode. This is a **browser process broker**, distinct from the provider-account broker. [O2:396–447; O4:1–9,66–134; O15] |
| Named tabs | Module-local `Map<string,TabSession>`; default name `main`. `TabSession` has browser handle, exact target ID, ready info, state and creator `ownerSessionId`. Creator is retained across reuse. Acquisitions serialize per name; overlapping runs reject as busy. | Scope IDs to owning host/session/worker generation, not name or URL alone. A duplicated `main` on another session or host is another tab. [O1:48; O3:72–158,210–220,494–520,736–761] |
| Native tab internals | `acquireTab`, `getTab`, `runInTab`, `releaseTab`, `releaseTabsForOwner` are exported internal modules. Bulk enumeration is only `getTabsMapForTest()`. `WorkerInitPayload` distinguishes creating a target from attaching an exact target; `ReadyInfo` includes `targetId`. No panel stream/subscription operation is in `WorkerInbound`. | Add a small maintained native lifecycle/metadata export, not a production dependency on a test accessor, guessed tool logs, or URL matching. Keep the bridge inside the existing owning worker. [O3:206–220,759–808; O7:45–137] |
| Target creation/attachment | Default Chromium creates `about:blank` through `Target.createTarget`, records its ID before initialization, then returns ready. Existing-browser selection uses the first URL/title substring match, or a visible/usable page when no selector is supplied. | The panel must address the actual returned ID. Do not attach by `app.target` substring when exact identity is required; it can be ambiguous. Initial import of existing user tabs must be explicit. [O8:704–721,1085–1117; O9:239–297] |
| Disposal | Native `AgentSession.dispose()` releases creator-owned browser tabs. Headless-mode close closes the real page; attached-mode close disconnects and leaves it. Shared-browser handle release disconnects without stopping sibling sessions' browser. Spawned apps may be killed with `kill:true`. | Viewer detach must release only its viewer transport. Session/host disposal currently destroys owned page state. Saved URL/profile cannot promise restoration of an unsent page form or live JavaScript heap. [O3:618–756; O8:2184–2193; O2:334–393; O13:4435–4448] |

### Other backends

**Connected/spawned browser:** `cdp_url` accepts HTTP discovery, not a `ws://` debugger URL. Native attachment has no default viewport override and may adopt a user-visible page; `open({url})` navigates it. Spawned `app.path` can point at an Electron executable; reuse detection refuses an already-running application without a reusable CDP endpoint instead of starting an isolated impersonation. The app should expose exact, explicitly selected targets, with detach and close distinguished. A CDP viewer can in principle observe these targets, but lifecycle, dialogs, focus and permissions need separate proofs. [O2:154–215,256–305; O9:179–220,239–297; O14:25–30]

**Relay:** the native Chrome extension uses `chrome.debugger`; the loopback server exposes `/json/version`, `/json/list`, `/cdp` and `/ext`. Default URL is `http://127.0.0.1:9224`. The machine-global relay daemon auto-starts only in supported CLI-worker mode; SDK embedding requires an already-running endpoint. `/cdp` rejects browser Origin headers; `/ext` accepts extension origins and checks a token only when configured. Discovery/auto-attach can inspect multiple eligible user tabs, even though explicit drive claims govern grouping. The bridge forwards page commands, suppresses `Browser.close`, acknowledges `Browser.setDownloadBehavior` without implementing a download path, and rejects browser-context creation. Do not assume every Chrome domain is available through `chrome.debugger`, or that another debugger/DevTools cannot interrupt it. Relay screencast, selection and reconnect behavior remain untested here. [O2:216–253; O10; O11:17–40; O16:57–108; O17:530–571,640–750]

**cmux:** selected through `CMUX_SOCKET_PATH`, optional native socket password, and `PI_BROWSER_CMUX`/setting state. This uses cmux RPC and WKWebView surfaces, not a Chromium CDP target. It can create or attach a surface; native cleanup closes only one it created. Its screenshot API captures the viewport, and explicitly reports full-page/element-crop limits. Native upload reads files on the session owner and transfers file data to that surface. A Chromium stream implementation cannot honestly claim cmux support; a bounded screenshot/control adapter would be a separate lower-frame-rate backend until a native streaming API is established. [O10:183–199; O3:420–488,648–679; O18:552–578,715–724]

## Electron and remote viewing options

| Approach | Same real target? | Assessment |
| --- | --- | --- |
| Host attaches CDP to OMP's exact target; streams frames and sends typed input | Yes, provided target identity is validated and reconnect never creates a replacement | Recommended first experiment. Works with a headless Linux owner and several viewing laptops; the page's `localhost` and files remain those of its owner. |
| Local `WebContentsView` loads the URL | No | Good for a separate app-owned browser design, but does not adopt arbitrary Chrome/CDP targets. The documented constructor takes an Electron `WebContents`; only one view can display that object at once. Remote viewers would still need a stream. [Electron WebContentsView](https://www.electronjs.org/docs/latest/api/web-contents-view) |
| Electron itself owns a dedicated browser WebContents, exposes it to OMP through a scoped CDP adapter, and streams it remotely | Potentially, with exact target adoption | Viable larger alternative if native local browser interaction is prioritized. It adds an owner Electron runtime/GUI dependency, a new lifecycle/profile owner, and a target-scoped bridge so OMP cannot accidentally select app chrome. Ordinary headless Bun hosts do not already provide this. |
| Electron offscreen `paint`/shared texture | Only for WebContents already owned by that Electron runtime | Produces bitmap or GPU texture output from Electron windows; it is not an API to import an external Chrome page. GPU texture consumption adds native integration. [Electron offscreen rendering](https://www.electronjs.org/docs/latest/tutorial/offscreen-rendering) |
| Copy DOM, iframe the URL, or proxy site HTML | No | Duplicates or changes page/security/state semantics and does not meet the shared-tab requirement. |

CDP `Page.startScreencast` emits compressed frames with metadata/session IDs; `Page.screencastFrameAck` acknowledges them. The method is experimental. Use one host-side producer per target and bounded latest-frame fan-out, so one slow viewer cannot stall every client. Probe actual browser/protocol support rather than relying on newer tip-of-tree options. A screenshot fallback must be labelled and measured, not reported as a live stream. Navigation/back/forward/reload can use target-scoped Page methods. [CDP Page](https://chromedevtools.github.io/devtools-protocol/tot/Page/#method-startScreencast)

Translate explicit pointer/key/text/composition events against the **accepted target viewport and frame generation**. CDP has mouse/key dispatch and experimental text/IME commands; that does not establish complete clipboard, selection, accessibility, drag/drop, native menus, audio, downloads or WebAuthn behavior. Two viewers cannot independently resize the same page without changing each other's document geometry. Prefer one shared viewport, scaled/letterboxed locally, plus an explicit revisioned resize control. [CDP Input](https://chromedevtools.github.io/devtools-protocol/tot/Input/)

## Ownership, permissions and transport

1. **Owner is the existing app session host.** `(hostId, sessionId, nativeTabName, workerGeneration, nativeTargetId, browserInstance)` must resolve server-side. Browser targets and profiles stay there. A saved catalog identifies actual tabs; it must not claim a dead target is still attached because a URL happens to match. Parent-owned source currently has no browser transport contract; the existing worker API and renderer window are the integration points. [A1, A2]
2. **Renderer reconnect is different from host restart.** Persist tab associations, selected tab, order and last-known URL/title, but revalidate exact live identity before sending input. Renderer detach/reconnect should preserve all document state while the owner lives. Current native session disposal closes its owned tabs; preserving exact tabs through a host restart needs a deliberate cooperating lifecycle/broker change. Profile persistence can preserve site storage, not an arbitrary live document. Native orphan records are cleanup records keyed by process/target, not a resumable session-tab database. [O3, O4, O13, O19]
3. **Native agent approval stays native.** The browser prelude declares `approval:"exec"`. `invokeEvalPrelude` resolves live `tools.approvalMode`/`tools.approval`, waits for the real UI Approve/Deny selection when required, and rechecks replacement/disable races. Do not call the private handler to bypass this path. Direct human navigation/input needs an explicit UI action receipt and ownership checks; it must not fabricate an agent tool call or permission approval. [O1:141–152; O20:77–132]
4. **OMP does not supply Codex-equivalent per-origin policy here.** The seven browser settings and execution approval do not define separate site-history, upload, download or full-CDP consent policies. Raw native `tab.run` can reach Bun/Node and arbitrary Puppeteer operations; a UI-only domain allowlist cannot honestly enforce those agent restrictions. Such restrictions require enforcement in the actual native execution/network boundary. Current launch defaults also include `--no-sandbox` and optional proxy/certificate flags; do not describe them as a Chromium security sandbox or silently change the configured policy. [O12; O14:18,54; O5:436–457]
5. **Keep CDP/relay private to the owner.** Reuse the app's existing authenticated loopback and same-user Tailscale transport. Browser page JavaScript must not receive host bearer credentials, debugger endpoints, relay tokens, raw CDP send methods, filesystem paths, or the desktop preload bridge. The current app rejects Origin on remote requests, authenticates the socket peer, and its own renderer is sandboxed with isolated context and denied window navigation. The new page stream crosses main/preload as bounded data, not remote HTML inserted into app chrome. [A2:415–440; A3:430–440; O16:57–108]
6. **Same-tab actions need conflict behavior.** Native `runInTab` currently rejects a second run while busy. A raw second CDP controller could otherwise interleave with native agent automation. Start with truthful native-busy rejection for competing mutations and a small revisioned active-controller lease for human input; observation may continue. Implement human takeover only once cancellation of the specific native browser operation is proved. Do not release a tab or abort an entire conversation as a hidden substitute. Unexpected transport loss after a click is uncertain; never replay clicks/typing automatically. [O3:494–520,618–646]
7. **Page-origin actions remain page-origin actions.** Preserve normal site navigation, redirects and subresource behavior on the owner. Treat `http://localhost` as owner-local. Download/upload and external protocol/file URLs need typed owner-side operations and bounded transfers; do not route arbitrary renderer file paths or relabel desktop-local files as owner-local. Web permissions, popups and JavaScript dialogs need surfaced state, including native agent auto-dialog policies when configured. No browser state clearing or credential sharing is implicit in opening a panel. [O1:69–78; O8:1092–1107; O18:715–724]

## Small implementation contract and ordered proofs

Proposed implementation names below are **not existing APIs**:

```ts
// Pure shared metadata. All native handles/endpoints remain in the owner worker.
type BrowserTabInfo = {
  id: string; hostId: string; sessionId: string; name: string;
  generation: string; revision: number;
  backend: "headless" | "spawned" | "connected" | "relay" | "cmux";
  state: "opening" | "ready" | "busy" | "closed" | "lost";
  url: string; title: string;
  viewport: { width: number; height: number; deviceScaleFactor: number };
  capabilities: { stream: boolean; pointer: boolean; keyboard: boolean;
    text: boolean; composition: boolean; resize: boolean };
};
// Worker adapter: list/subscribe native tabs; attach/detach a viewer;
// typed bounded navigation/input/resize, revision checked; binary frame delivery.
// Opening/closing use native operations and emit actual lifecycle acknowledgments.
```

Keep a single small pinned adapter in `apps/host/src/omp-browser/**`, narrow additions to the existing session worker protocol/runtime, and a host-owned `browser-http.ts`. Add pure shared browser types, existing main/preload transport bindings, `BrowserPanel.tsx` and its state/CSS, and small App panel integration. Persist catalog metadata through the existing store only when persistence behavior is defined; no frames, page contents, keystrokes or cookies in the command/event journal. Existing old hosts should return an explicit unsupported capability, with authentication/network/backend failures retained as failures. No general CDP proxy, new orchestration framework or page-state replication service is needed.

The smallest upstream change is an explicit owner-filtered tab enumeration/lifecycle subscription and exact-target attachment/control seam. It must include agent-created tabs, title/navigation updates and disposal, rather than only tabs opened through the UI. Prefer that maintained patch/export over production use of `getTabsMapForTest`, method monkeypatching, or intercepting textual tool output. Native browser execution remains the authority. The exact SDK `ToolSession` is assembled inside `createAgentSession`; the experiment successfully invoked the existing `session.getToolByName("eval").execute(...)` path and its real browser prelude without reconstructing it. That provides a real native open/action path for a first adapter; it does not supply a typed desktop catalog, lifecycle subscription, streaming or input contract. [O3; O7; O21:1787–1940; O13:5034–5041]

| Slice | Load-bearing actual acceptance |
| --- | --- |
| 1. Native tab seam | Temporary native profile and controlled local HTTP page. Open through the real browser prelude, prove exact native target ID/name/owner appears in the adapter, and human controls affect the same page state read back through native automation. No provider inference required; do not mock the browser or synthesize its history. Test duplicate tab names in two native session processes. |
| 2. Actual Electron viewer | Real headless Chrome owner plus the production Electron panel. Verify first frame, coordinate mapping, scroll, keys/text/IME, back/forward, title changes, page reload, native-busy behavior, visible errors and no app-chrome target exposure. Capture actual viewport, not a blank loading fixture. Measure frame latency and bounded queue behavior. |
| 3. Two viewers and Tailscale | Two actual desktop viewers of one target. Edit an unsent form from each in turn, verify the other sees it, compare a page-generated random identity and owner-side DOM. Disconnect/reattach one viewer without navigation or target creation. Verify stale-generation input rejection, one accepted viewport, owner offline state, and preservation of unrelated tabs. |
| 4. Ownership lifetime | Renderer quit leaves the page alive. Native session close has exactly its documented effect. Crash/host-restart must report lost targets until real native persistent ownership/adoption is implemented and tested. Verify browser death, worker death, native orphan cleanup, profile locks, canonical cwd, and sibling-session survival. No URL-reopen masquerading as original state restoration. |
| 5. Backend and browser parity | Explicit connected target, spawned app and relay extension in isolated test profiles; actual relay DevTools conflicts/reconnect and screencast availability. Separate cmux proof. Then dialogs/popups, uploads/downloads, clipboard/select/copy, drag/drop, accessibility, browser history/search/address completion, find, zoom, devtools, permissions and required persisted settings. Each unsupported capability remains explicit until proved. |

Packaging must include the exact native tab worker entry and dynamically loaded browser dependencies. Native package pins `puppeteer-core` **25.3.0**; OMP resolves/downloads a browser on first use, with `PUPPETEER_EXECUTABLE_PATH` taking precedence. macOS prefers isolated Chrome for Testing; other platforms prefer detected system Chrome before the managed fallback. The native cache uses `getPuppeteerDir()`. Prove packaged launch without relying on a developer checkout or pre-populated cache, bounded download failures and missing Linux libraries. Pin/test actual Chrome/CDP versions because screencast is experimental; do not assume the latest online protocol matches the launched browser. [O5:150–247; O3:1101–1112; O22]

## Static Codex reference, not live UI evidence

These archive entries were byte-compared against preserved `.reference/codex-26.901.41600/app.asar` and its SHA-256 inventory. Offsets below are zero-based Unicode character offsets in minified JS, not source-map lines. No archive executable code was run.

| Exact archive entry | Static observation |
| --- | --- |
| `webview/assets/webview-ac07f87f04cd.js`, offset 199 | Component accepts adoption lease, adopted WebContents ID, bounds, browserTabId, conversationId, hostKind, visibility/paint state and scale. Evidence for owned page adoption and separate view lifecycle, not proof of OMP interoperability. SHA `f30f9d81d3e373b0f102972b58330a822f7cbdcc86ee95215b8a1f1841ef04a5`. |
| `webview/assets/hidden-browser-use-webview-host-9e31be6229df.js`, offsets 394,1989 | Conversation-associated hidden browser tabs can bootstrap while hidden and without painting. SHA `362a5fc244d7888ede6b7c4e2311ff37c0758ade8c6e9cc9cee961b37910226f`. |
| `webview/assets/cloud-browser-side-panel-a767a7034b67.js`, offsets 9759,10077,18326,26465 | Separate cloud path has observe/control modes, clipboard/control-lock messages, WebRTC video/data transport and a user control-activation affordance. This hosted transport is not available merely by embedding OMP. SHA `2996634b2cae8d55696f561b7d8b82327f532c217fe391de80d49fb48110ec74`. |
| `webview/assets/browser-use-origin-state-queries-561ff2db0353.js`, line 1, symbols `P`, `L`, `he`, `ge` | Distinct origin/history/upload/download/full-CDP policy controls, managed constraints and configuration write paths. These are meaningful reference functions absent from native OMP browser settings. No OpenAI authentication/probe logic is part of the proposed implementation. SHA `51479e9435923a24b4a5ee2613fde2e38c8d9afd43a3f6b9220ec6758a011a26`. |

Static component metadata cannot establish actual menu geometry, timing, OS integration, feature-flag availability or visual parity. The first panel should reuse existing typography/theme/layout tokens; exact browser chrome remains a separate source-and-own-app acceptance task.

## Actual isolated native/CDP feasibility result

The maintained [native-browser.ts experiment](/Users/twaldin/dev/agentic-app-expiriment/scripts/acceptance/native-browser.ts) passed **seven actual native/browser phases** on macOS arm64 using Bun1.3.14 and the already-existing Chrome for Testing **150.0.7871.24** executable. No download was needed. The final verified run took approximately 2.7 seconds from parent invocation to completed native cleanup. Evidence is private under [.data/native-browser-acceptance/2026-09-05-verified/result.json](/Users/twaldin/dev/agentic-app-expiriment/.data/native-browser-acceptance/2026-09-05-verified/result.json), with source/binary fingerprints in [invocation.json](/Users/twaldin/dev/agentic-app-expiriment/.data/native-browser-acceptance/2026-09-05-verified/invocation.json).

```sh
bun scripts/acceptance/native-browser.ts .data/native-browser-acceptance/new-run
# An existing compatible Chrome for Testing executable can be supplied as argument 2.
```

The parent spawns an isolated Bun child with temporary HOME/XDG/TMPDIR, no inherited credential variables, an explicit existing browser executable, and external fetches rejected. The child creates a **real native SDK session**, obtains its actual Eval tool, and calls the native browser prelude. It uses `getTabsMapForTest` solely to locate the native-created tab for this labelled experiment; that accessor is not an accepted production seam. No browser or native SDK function is mocked, no provider is called, and no artificial user/tool transcript entries are inserted.

| Phase | Actual evidence |
| --- | --- |
| Native open | Native browser prelude opened the localhost fixture through a real tab worker. Creator session ID matched the real native manager; backend was the expected SDK process-local headless browser with a profile under the isolated tree. |
| Second CDP observer | A separate Puppeteer/CDP connection discovered the **exact native target ID**, then read the same URL, page-generated random identity, cookie and unsent field value. The proof selects by target ID, not title/URL matching. |
| Native action → observer | Native `browser.tab(...).click` changed the counter from 0 to 1, observed through the second connection. Native and observer PNG screenshots were byte-identical at each stable state, and the hash changed with the counter. |
| Observer input → native | Raw `Input.dispatchMouseEvent` press/release on the observer CDP session changed the same counter to 2. Native browser evaluation independently asserted the original document identity and new counter. Every native Eval cell was required to complete with exitCode0. |
| Native frame stream | Actual `Page.startScreencast` produced a PNG frame with 800×600 native viewport metadata; `Page.screencastFrameAck` succeeded. The saved frame was visually inspected and showed the actual counter2/unsent field. This establishes a working native producer, not stream latency/backpressure or multi-view acceptance. |
| Observer detach/reconnect | Disconnecting the observer left native browser control working. Native clicked again to 3; a fresh observer connection found the same target, original random document identity, cookie, unsent field and Chromium process. Four native/observer screenshot pairs for counts0–3 were identical within each pair. |
| Native cleanup | Real `session.dispose()` removed its tab registry entries, disconnected the native browser, exited its owned Chromium process with code0 and removed its temporary Chrome profile. The isolated child also exited0; the parent removed the remaining isolated profile tree. No manual kill was needed. |

Final experiment source SHA-256: `f90608c85606a79803a62d46c7387cd459f93dcef6c278034ea290367c03d294`. Package version and four critical native browser file hashes were asserted against the exact release before and after execution; the experiment source was also unchanged during the run. Earlier first/final runs remain separate evidence, before strict per-cell status and source-stability assertions were added. Focused typecheck and diff checks passed.

This makes the **same-target CDP approach experimentally viable**. It also confirms the SDK temporary-profile/disposal caveat. It does not prove a production Electron panel, two viewing desktops, Tailscale streaming, concurrent native/human input arbitration, persistent host-restart adoption, Linux packaging, relay/cmux support, WebAuthn, uploads/downloads, clipboard/IME or full browser UI parity. Those remain the ordered proofs above; source files and installed services were unchanged except this maintained acceptance script and document.

## Source index

`O` links are immutable upstream release files; line numbers identify the inspected region. The matching local root is `node_modules/@oh-my-pi/pi-coding-agent/`, except `pi-utils` entries.

- O1 — [tools/browser.ts](https://github.com/can1357/oh-my-pi/blob/f241301c83726afe75a847e919b89977a54dafbe/packages/coding-agent/src/tools/browser.ts#L48).
- O2 — [browser/registry.ts](https://github.com/can1357/oh-my-pi/blob/f241301c83726afe75a847e919b89977a54dafbe/packages/coding-agent/src/tools/browser/registry.ts#L79).
- O3 — [browser/tab-supervisor.ts](https://github.com/can1357/oh-my-pi/blob/f241301c83726afe75a847e919b89977a54dafbe/packages/coding-agent/src/tools/browser/tab-supervisor.ts#L72).
- O4 — [browser/shared-daemon.ts](https://github.com/can1357/oh-my-pi/blob/f241301c83726afe75a847e919b89977a54dafbe/packages/coding-agent/src/tools/browser/shared-daemon.ts#L66).
- O5 — [browser/launch.ts](https://github.com/can1357/oh-my-pi/blob/f241301c83726afe75a847e919b89977a54dafbe/packages/coding-agent/src/tools/browser/launch.ts#L174), [pi-utils/worker-host.ts:11–29](https://github.com/can1357/oh-my-pi/blob/f241301c83726afe75a847e919b89977a54dafbe/packages/utils/src/worker-host.ts#L11).
- O6 — [browser/declarations.d.ts](https://github.com/can1357/oh-my-pi/blob/f241301c83726afe75a847e919b89977a54dafbe/packages/coding-agent/src/tools/browser/declarations.d.ts#L227).
- O7 — [browser/tab-protocol.ts](https://github.com/can1357/oh-my-pi/blob/f241301c83726afe75a847e919b89977a54dafbe/packages/coding-agent/src/tools/browser/tab-protocol.ts#L45).
- O8 — [browser/tab-worker.ts](https://github.com/can1357/oh-my-pi/blob/f241301c83726afe75a847e919b89977a54dafbe/packages/coding-agent/src/tools/browser/tab-worker.ts#L704).
- O9 — [browser/attach.ts](https://github.com/can1357/oh-my-pi/blob/f241301c83726afe75a847e919b89977a54dafbe/packages/coding-agent/src/tools/browser/attach.ts#L179).
- O10 — [browser/cmux/rpc.ts](https://github.com/can1357/oh-my-pi/blob/f241301c83726afe75a847e919b89977a54dafbe/packages/coding-agent/src/tools/browser/cmux/rpc.ts#L183), [browser/relay/daemon.ts](https://github.com/can1357/oh-my-pi/blob/f241301c83726afe75a847e919b89977a54dafbe/packages/coding-agent/src/tools/browser/relay/daemon.ts#L54).
- O11 — [browser/relay/kind.ts](https://github.com/can1357/oh-my-pi/blob/f241301c83726afe75a847e919b89977a54dafbe/packages/coding-agent/src/tools/browser/relay/kind.ts#L17).
- O12 — [config/settings-schema.ts](https://github.com/can1357/oh-my-pi/blob/f241301c83726afe75a847e919b89977a54dafbe/packages/coding-agent/src/config/settings-schema.ts#L4480).
- O13 — [session/agent-session.ts](https://github.com/can1357/oh-my-pi/blob/f241301c83726afe75a847e919b89977a54dafbe/packages/coding-agent/src/session/agent-session.ts#L4435).
- O14 — [prompts/tools/browser.md](https://github.com/can1357/oh-my-pi/blob/f241301c83726afe75a847e919b89977a54dafbe/packages/coding-agent/src/prompts/tools/browser.md#L1).
- O15 — [launch/paths.ts](https://github.com/can1357/oh-my-pi/blob/f241301c83726afe75a847e919b89977a54dafbe/packages/coding-agent/src/launch/paths.ts#L12), [pi-utils/dirs.ts:957–978](https://github.com/can1357/oh-my-pi/blob/f241301c83726afe75a847e919b89977a54dafbe/packages/utils/src/dirs.ts#L957).
- O16 — [browser/relay/server.ts](https://github.com/can1357/oh-my-pi/blob/f241301c83726afe75a847e919b89977a54dafbe/packages/coding-agent/src/tools/browser/relay/server.ts#L57).
- O17 — [browser/relay/bridge.ts](https://github.com/can1357/oh-my-pi/blob/f241301c83726afe75a847e919b89977a54dafbe/packages/coding-agent/src/tools/browser/relay/bridge.ts#L530).
- O18 — [browser/cmux/cmux-tab.ts](https://github.com/can1357/oh-my-pi/blob/f241301c83726afe75a847e919b89977a54dafbe/packages/coding-agent/src/tools/browser/cmux/cmux-tab.ts#L552).
- O19 — [browser/orphan-registry.ts](https://github.com/can1357/oh-my-pi/blob/f241301c83726afe75a847e919b89977a54dafbe/packages/coding-agent/src/tools/browser/orphan-registry.ts#L1).
- O20 — [eval/preludes.ts](https://github.com/can1357/oh-my-pi/blob/f241301c83726afe75a847e919b89977a54dafbe/packages/coding-agent/src/eval/preludes.ts#L77).
- O21 — [sdk.ts](https://github.com/can1357/oh-my-pi/blob/f241301c83726afe75a847e919b89977a54dafbe/packages/coding-agent/src/sdk.ts#L1787).
- O22 — [native package dependencies](https://github.com/can1357/oh-my-pi/blob/f241301c83726afe75a847e919b89977a54dafbe/packages/coding-agent/package.json), [browser cache directory](https://github.com/can1357/oh-my-pi/blob/f241301c83726afe75a847e919b89977a54dafbe/packages/utils/src/dirs.ts#L700).
- A1 — [our worker entry](/Users/twaldin/dev/agentic-app-expiriment/apps/host/src/omp-workers/entry.ts:6), [native runtime interface](/Users/twaldin/dev/agentic-app-expiriment/apps/host/src/omp/runtime.ts:34), [worker process boundary](/Users/twaldin/dev/agentic-app-expiriment/apps/host/src/omp-workers/runtime.ts:69).
- A2 — [desktop window](/Users/twaldin/dev/agentic-app-expiriment/apps/desktop/src/main/main.ts:415), [renderer CSP](/Users/twaldin/dev/agentic-app-expiriment/apps/desktop/index.html:6).
- A3 — [host request authentication](/Users/twaldin/dev/agentic-app-expiriment/apps/host/src/server.ts:430), [Tailscale transport contract](tailscale-transport.md).
- R — [preserved Codex archive inventory](../.reference/codex-26.901.41600/asar-file-list.json), [reference methodology](reference-findings.md).


## Native creation and individual browser dock targets

The original native Browser dock action requested one native resource using the session's live OMP browser configuration. The local New tab flow described below now defers that request until address submission. A fresh owner-bound metadata ticket authorizes admission; observation alone never starts a worker. Explicit creation may resume an inactive session worker. Host admission reserves the request identity before awaiting native work, preserves identical retries, rejects conflicting/stale requests, and retains its eight-operation limit until native settlement. A desktop timeout never automatically retries or releases that underlying-operation bound.

Worker IPC 9 returns the exact native target and whether OMP created a page/surface or adopted a configured connected/relay target. The dock persists worker PID, name and target ID alongside host/session identity. Viewer close is separate from native resource disposal; a missing/restarted worker cannot substitute another page. Existing legacy generic descriptors stay readable without inventing native identity. CMUX targets remain discoverable even though viewport capture is unsupported.

The real native pipeline in `.data/ui-acceptance/native-browser-create-title-final/result.json` exercises production DockPanel and its hook, metadata/create/frame/control HTTP endpoints, main transports and BrowserPanel against an isolated OMP worker. It verifies one creation, navigation, title propagation, viewer close, persisted exact-target restore, original-page cookies/input and final owner disposal. Five screenshots are controlled hidden Electron captures, not native window originals matched to the reference. No provider or installed service is involved. The periodic JPEG viewport and existing toolbar remain visibly different from a complete Codex browser; streaming, native context/permission/file menus, annotations, find/print, history surfaces and remote installed acceptance remain open.

Reproducibility requires both the isolated native proof and `browser-create/run-frozen-install.ts`. Bun 1.3.14 misapplied a prior zero-context deletion even when Unix patch succeeded. The contextual patch is byte-verified after fresh frozen installation; see the candidate README for the exact reproduction command.

## Browser toolbar, native history and explicit viewport size

Worker IPC 10 adds the native CDP navigation entry and Back/Forward availability to each document context. Native history changes, including same-document entries, participate in stale-context checks. An explicit resize is serialized with other human actions, preserves the existing native viewport's device scale/mobile/touch attributes, and rejects a missing emulated viewport before dispatch. It does not resize a connected operating-system window. Older contexts without navigation remain usable for existing actions but cannot authorize history or resize operations. No client automatically resizes a shared page when its own window changes.

The desktop uses the native availability flags, a 34-point toolbar with 28-point controls, dedicated browser icon variants, an idle host label/full editable URL, and Escape restoration. Local development addresses retain their route in the idle label. URL/search classification uses pinned `tldts` 7.4.9 with private suffixes enabled, matching the reference's documented parser family; the extracted notices contain multiple parser versions, so exact embedded-list identity is not asserted. The package's [official parser documentation](https://github.com/remusao/tldts/blob/master/packages/tldts/README.md) describes that option. Host-local file paths produce an explicit unsupported message instead of becoming search queries. The options menu exposes actual preview refresh, pause/resume and explicit page-to-panel resizing. External-open uses the complete URL on the current desktop; an external-open error does not halt native page input.

Evidence remains separated:

- `.data/ui-acceptance/native-browser-toolbar-first/result.json`: actual isolated OMP worker, authenticated HTTP and production desktop transport/dock/browser UI; 39 requests and five captures. Real resize changes the JPEG and document dimensions; Back/Forward traverse native history with correct button states. Native page identity, original input/cookies and owner disposal remain verified.
- `.data/browser-toolbar-controls-fourth/result.json`: four controlled Electron groups, including focus/full selection/Escape, external URL, menu keyboard/dismissal, 34/28-point geometry and unknown-input recovery; two viewport captures. The earlier focus checks failed because the hidden web contents had not been focused; the final runner explicitly focuses the web contents rather than fabricating focus events.
- `.data/browser-toolbar-panel-third/result.json`: seven controlled owner/target/offline/pause groups and three viewport captures. `.data/browser-toolbar-app-final/result.json`: six production-App workflow groups and four viewport captures. Both retain source fingerprints.
- The selected native patch passes zero-fuzz real-browser checks, independent runtime validation, a fresh external frozen install with exact patched bytes, and the actual WorkerRuntime regression. Evidence: `.data/temp/omp-browser-control/candidate-check-dE5IhQ/run/result.json` and `.data/temp/browser-history-frozen-final-2/result.json`.

These are source/native/component checks. Paired native-window originals and AX snapshots at the reference conditions, full browser menus, site information, annotations, live streaming, browser file flows and installed remote acceptance are still required. The source checkpoint is not installed release 17.


## Automatic fit and independent Work checks through release23

Later source enables automatic page-to-panel fitting by default, retaining an explicit Fit option. Each viewer reacts to its own measured layout and keeps the last requested dimensions so another viewer’s viewport update does not create a resize fight. Fit-off preserves the native viewport when the dock changes size. Work21 verifies actual navigation receipts without replay, counter input, native keyboard input and both Fit states. This supersedes the earlier explicit-only toolbar checkpoint above.

A remaining Work21 observation is separate: after typing into a narrow preview, the image retained14/13-point bands until options/blur. The release23 renderer candidate marks fit measurements stale whenever a control starts, then remeasures after pending status clears before permitting auto-fit. Controlled acceptance records392.5→364.5→392.5-point viewport height around a300ms action and no transient resize. Old source also passed this synthetic timing test, so the candidate is not considered a proven fix for the Work observation until independent packaged retesting.

The first-frame503 error is another open observation. The pinned native capture reads context before/after its screenshot and rejects document transitions; unlike post-action observation, capture does not retry recognized transition errors inside that request. The host maps its exception to BROWSER_FRAME_FAILED, and normal renderer polling can recover on the next second. This is consistent with Work21’s observed automatic recovery, but the exact native exception was not captured. A future retry must be bounded, read-only, limited to recognized transition failures, and revalidate the same owner/target on every attempt; persistent failures must stay visible. Release23 does not change this native capture path.


## Source24 — bounded capture transition recovery

A real-browser regression now schedules one actual `page.goto()` between the capture's initial context read and Chromium screenshot. The old source rejects it with `The browser document or viewport changed during capture`. The candidate discards those raced pixels and repeats only read-only observation of the same owner-bound target. Each attempt rechecks the registered owner/tab/target, and the capture reservation remains held across retries. The retry window is two seconds; the existing ten-second outer capture timeout still bounds slow operations. Closed targets, ownership changes, payload bounds and unrelated errors remain failures. No page input is replayed.

The actual native worker test passes a single transition with exactly one page load, a stable new document in the same target, persistent real navigation that produces a bounded failure, and a successful subsequent capture after navigation stops. Focused frame/control contracts pass11 tests with85 assertions, and a separate fresh native rerun passes31 assertions. Typecheck passes. Evidence is private in `.data/browser-frame-transition24/`: `red.log`, `green.log` (the earlier single-transition case), `contracts.log` and `recheck.log` (including persistent-transition recovery). The fixture controls scheduling by wrapping the real page screenshot once, or repeatedly for the bounded-failure case; it does not fabricate screenshot bytes, document contexts or exceptions.

This reproduces a native failure mode consistent with the Work21 first-frame observation. It does not establish which exception that particular Work request raised, and source24 is not yet a packaged or independently exercised UI fix. Work23 remains frozen and unchanged.

## New-tab lifecycle still required

The pinned app separates a pristine New tab presentation from a native web page at `about:blank`. Its replacement/disposal checks also account for address drafts, navigation/history, loading, agent ownership, capture and other browser state. Removal is tied to specific paths: a singleton tab during Step workspace layout, an address-bar action/tab suggestion, and cleanup when leaving a browser conversation. Ordinary task-tab selection is not a general blank-tab deletion trigger.

Current OMP metadata and the renderer's blank-page appearance do not establish that a native target is disposable. The local New tab flow below separates acquisition from opening; its draft lifetime and owner-qualified presentation still need integration with the required Step workspace layout and cleanup triggers. Do not close an OMP browser or session based only on its URL, or count always retaining empty tabs as completed parity. Presentation removal and native target disposal have different ownership requirements.

Private source windows and a forty-case extracted-predicate diagnostic are in `.data/empty-browser-tab-lifecycle-2026-09-09/`. This is preparatory source evidence only: no application/browser/native runtime was launched, no UI behavior changed, and no native or appearance acceptance is claimed. The existing launch hold remains in effect.

### Initial navigation acquisition

The source now accepts `initialUrl` on a browser creation request. The desktop sends that form to `/v1/sessions/:id/browser-open`, which requires the URL; ordinary `/browser-create` still accepts only no-URL creation. This prevents an old host from silently dropping the new field and acquiring a blank target. Both routes use the same owner, ticket and request-ID admission map, whose input hash includes the exact URL. A duplicate shares its receipt; a changed URL or changed operation under the same identity rejects. Uncertain acquisition is retained as unknown without automatic replay.

The private worker protocol is 38. The native adapter requires `BROWSER_TAB_CREATE_INITIAL_URL_VERSION=1` before forwarding an initial URL to the patched OMP acquisition function. That function uses its existing acquisition/navigation deadline and live session settings, including relay/CDP/cmux/headless selection. It reports the actual target disposition and resulting URL, which may reflect redirects. It does not promise a fresh page for adoption backends or establish that a target remains unused. No browser-control navigation is issued afterward.

The maintained OMP patch and isolated patched source copies carry this change; installed dependency files were left unchanged during the launch hold. An older loaded package rejects the new operation before acquisition. Focused pure tests cover parsing, admission, deduplication, uncertain outcomes and the transport path with controlled fetch. An extracted native adapter experiment verifies URL forwarding and preflight validation using a controlled acquisition dependency. These are source/controlled checks, not actual IPC, browser-mode, network, packaged or UI acceptance. The deferred shell, address-draft persistence, unknown-result UI reconciliation and all three cleanup triggers remain required. Evidence and separate source-review scope: `.data/browser-initial-navigation-2026-09-09/`.

## Address-focus command

The renderer installs `focusBrowserAddressBar` through the shared command keymap. It focuses the visible browser that owns focus, or the side browser before the bottom fallback when focus is in the main chat. Hidden/inactive, different-owner and unrelated editor surfaces do not become targets; the command never creates, navigates or closes a browser. A clean address selects the full URL, while an unfinished address retains its selection. The former unconditional browser-local Cmd/Ctrl+L handler is removed, so saved overrides and clears apply.

The browser page-input bridge offers keys to the same app shortcut listener before forwarding native page keys. A handled event is consumed once; composition, popup, explicit editor ownership and editable-key restrictions remain. App dispatch eligibility is separate from installed keymap support. Pure controlled document/listener tests and source compilation cover this wiring; mounted App/React address expansion, command-menu dismissal, real page input and native keyboard/focus acceptance remain required. No new runtime was launched under the hold. Source review/evidence: `.data/browser-address-focus-2026-09-09/`.


## Local New tab and initial acquisition

Opening Browser for an existing session now inserts a local `NEW_TAB_PAGE` with a unique window tab ID. It performs no metadata, frame, worker or acquisition call, including when the owning host is offline. Existing native inspection remains a separate explicit action. A legacy browser descriptor without a native target is not silently reinterpreted, and a native `about:blank` page is still a web target.

The address field reuses browser normalization and records its draft in the existing window-view state. An explicitly empty edit is retained; Escape discards an idle draft. Enter obtains a current ticket and sends one initial-navigation request to the captured host/session. Pending work blocks a second submission. Completed receipts replace only the initiating extant descriptor, preserving its stable ID, dock placement, hidden state and other tabs' drafts. Closing before the ticket returns prevents acquisition; closing after dispatch suppresses presentation replacement without disposing the OMP session. Search includes local tabs by their saved title without treating an unsent address as an observed URL; metadata observation starts when the stable tab acquires its native target.

Definite rejection permits deliberate retry. Transport failure or uncertain receipts retain the original request as unknown and prevent a fresh submission. A saved pending request restores as unknown without replay; a pending ticket lookup without a request restores idle. The per-window checkpoint below now waits for the existing save acknowledgement before remote admission. Explicit unknown-result reconciliation is described below; its native acceptance remains required; the host journal below addresses host history separately. Those failure paths and the three separate layout/address-suggestion/prior-conversation cleanup contracts remain required. No cleanup is authorized from a blank URL or stale metadata.

Source checks cover the controller, actual dock hook under a controlled React dispatcher, temporary-file window-state reopen and pure search helpers. The existing App and native-browser acceptance fixtures now explicitly submit an address, but only their strict compilation has run during the launch hold. No actual App/DOM, native acquisition, focus, appearance or installed acceptance is claimed for this flow.


## Read-only creation observation

`POST /v1/sessions/:id/browser-creation-status` observes the durable admission journal with the original parsed request in its body. It does not create or resume a worker, look up a live handle, reserve a request, wait for native settlement or change receipt retention. Only the owning route's unresolved promise returns `pending`; completed history returns `settled` plus the original receipt. A prior pending record without that live promise returns settled inspect-only unknown, including across host epoch changes. Wrong input for an existing identity fails. Missing history alone returns `unavailable`, which must not authorize replay. A previously completed receipt is historical acquisition evidence, not proof of current target liveness or disposability.

The trusted main/preload `getBrowserCreationStatus` path validates outer and nested ownership and reuses creation's completed-target validation. It makes one observation request with no create/open fallback; HTTP/transport/validation failures remain errors. The request body keeps the original URL out of the query string. Existing authenticated server routing still owns access; this source slice does not newly exercise a listening server or physical connection.

Pure route and controlled-fetch tests exercise the pending/settled/unavailable contract and zero acquisition on observation. Renderer recovery use, native save-barrier acceptance, physical host restart acceptance and all three cleanup contracts remain required; this primitive alone does not finish recovery. No runtime or installed acceptance under the launch hold.


## Durable admission record primitive

`HostStore.browserCreations` stores exact owner/session/request hashes and pending/settled receipts in the existing SQLite metadata table. The first successful claim atomically establishes schema16, so older host code refuses the new durable state. A valid legacy default device policy is materialized with identical semantics inside that transaction to avoid introducing missing-policy corruption on reopen. Reads do not migrate; already-fenced missing/corrupt policy is refused. The host artifact descriptor now declares schema16 support.

Identical claims return the existing record; changed input or conflicting settlement fails. Raw initial request URLs are hashed rather than stored; a completed native receipt retains its allowed observed URL/title. Records are not removed on age or epoch changes. Observation of an interrupted epoch produces inspect-only unknown without mutating the record; a completed receipt remains historical evidence, never a claim of target liveness or permission to dispose it.

The HTTP consumer below uses this API before acquisition and acknowledges settlement only after durable finish. Renderer recovery UI and all three cleanup rules remain required; the pre-dispatch checkpoint below is separately source-tested. Pure SQLite/file-reopen and metadata packaging tests support only the record and downgrade-fence scope; no host process, physical restart, native browser or installed acceptance is claimed.

### Durable HTTP consumption

`BrowserCreateHttp` now requires the owning HostStore journal. Admission commits before native handle lookup; completion is returned only after its receipt commits. A previous exact request returns its historical receipt even after in-memory retention or host epoch changes. Pending journal entries without this route's live promise return inspect-only unknown, never reacquire. Observation reads durable history without native lookup or write; storage failures remain explicit and cannot become completion or an absent-history fallback. The process-local map retains only waiters/recent outcomes, not authoritative deduplication.

Pure Request/Response + temporary SQLite tests cover route recreation/reopen and storage failures. They do not prove a running HTTP server, IPC, native browser or installed artifact. Native renderer checkpoint acceptance, unknown recovery UI and cleanup remain required; the journal and observation prerequisites have separate pending source reviews.

### Local save acknowledgement boundary

The existing window-state writer flushes the temporary file, renames it and flushes the containing directory before reporting success. Failure at directory acknowledgement remains an error even if the new file is already visible; it does not claim rollback. This strengthens the immediate parent-directory boundary only, without claiming power-loss proof or durability of newly created ancestors. The exact-request renderer barrier below consumes this acknowledgement; an effect-scheduled save by itself is insufficient.

### Renderer request checkpoint

Each App owns a `BrowserWindowCheckpoint` shared by its browser launchers and window persistence hook. Enter obtains a ticket and schedules the pending request, then waits for the exact tab/host/session/instance, parsed request and draft to appear in both the committed view and an acknowledged validated save projection. If validation drops the initiating tab, the checkpoint rejects rather than acknowledging the raw renderer view. The controller does not save a captured whole-window snapshot. Save failure, removal/replacement, changed committed request or controller disposal cancels the wait; live/connected state is checked again before native dispatch. Beforeunload reads the last committed view, excluding abandoned renders. Saved pending requests still restore unknown without replay.

Controlled controller/hook tests and actual temporary WindowStateStore writes cover the boundary; they do not establish React/Radix/native scheduling or physical crash behavior. The native-browser acceptance fixture uses actual WindowStateStore via synchronous IPC, but is compiled only under the launch hold. The explicit observation UI below remains separately source-tested; all three cleanup triggers and native acceptance remain required.

### Explicit unknown-result recovery

An unknown launcher offers **Check creation status**. Each deliberate check sends the original request to the read-only observation endpoint once. Pending/unavailable/errors remain unknown, with the address and request retained; there is no create/open fallback or automatic polling. A definite rejected receipt permits a deliberate new submission.

A historical completion is followed by read-only metadata for the original host/session. Only one matching alive name/target ID from the same worker PID/backend/kind may materialize the extant launcher, using its current title. Missing, dead, replaced, ambiguous or foreign targets remain unknown. Closing or disconnecting across either await suppresses attachment. This does not close or adopt unrelated native targets.

Controlled source tests and server-rendered button availability support this behavior, not mounted UI/native/installed proof. Durable journal/consumer/checkpoint scopes retain their separate review gates, and all three cleanup triggers remain required.

## Step workspace layout and local launcher cleanup

The distinct `stepWorkspaceLayout` command is installed/configurable as CmdOrCtrl+Shift+B. In a session with no active right content it opens a local New tab in split view and clears full/restore-full flags while preserving saved split width. Closed or full retained content returns to split; split content hides and focuses the current Chat. A right singleton can instead be removed when it is this conversation's canonical app-local idle launcher with no request, native target or address draft (even an empty draft is retained). A changed title is protected because the current descriptor owns its only persisted copy; this additional guard is an app adaptation, not a different reference predicate. Bottom tabs stay independent.

Queued edits are evaluated in the dock's functional state update. Removed local controllers are disposed after the snapshot commits. This is presentation cleanup only: no OMP page/worker/session close is issued, and native WEB about:blank is not a disposable local launcher. Materialized targets, pending/unknown requests and other owners are retained. Settings/plugins omit dispatch while the installed command stays editable in Keyboard shortcuts. The existing no-session browser capability, address suggestion replacement and previous-conversation cleanup remain separate required work. Source tests do not establish native keyboard/focus, appearance or the pinned app's non-unified feature-flag branch.

## Leaving a browser conversation

A committed window route change now cleans only the previous non-null host/session's initialized pristine local launchers in right and Bottom. BrowserNewTabPanel marks its original controller observed in a layout effect; controller allocation during render is insufficient. This is a committed app-local presentation observation, not an OMP snapshot or painted-visibility assertion. Same-conversation tab selection, Settings/plugin visibility and host catalog loading do not change the cleanup key.

Current descriptor and controller state must both remain eligible: no address draft (including empty), request, pending/unknown/rejected state, native target, different owner/instance or custom title. Functional updates recheck queued changes. Other windows and tabs remain independent; existing window persistence saves the resulting local projection, and committed removal prunes only local controllers. No OMP page, worker, session or host close is issued. Native pristine/adoption/use/capture cleanup and address-suggestion replacement remain separate required work, as does native/installed acceptance after the launch hold.


## Browser before the first conversation

This remains unimplemented. The current App command, dock launcher, controller and worker require a native session. The pinned frontend instead exposes Browser from the global command/app-menu path on the home draft scope; it deliberately omits Browser from the generic new-tab launcher. Enabling an extra inert launcher row would not satisfy that behavior.

Draft layout identity, browser page ownership and native OMP session identity are separate concerns. The worktree finalizer restores a separately captured panel snapshot under the created conversation; its Browser callback keeps the app tab ID and observed URL, without passing the source transfer fields. Ordinary local Send migrates durable tab-route descriptors, including Browser storage/tab IDs and restore URL, then requests page restore under the target conversation. These paths establish presentation/restore intent, not transfer of a native page or OMP worker. The separate thread-handoff transfer call must not be substituted as their implementation.

OMP's internal acquisition context needs cwd, effective settings and an owner identity, but the installed public adapter requires AgentSession. Native tabs are creator-owned in a process-local registry. Current Send always creates a fresh worker, and prepared-worktree creation supplies the final managed cwd and setup environment. Session initialization loads extensions/tool policy; even SessionManager allocation writes native directory/breadcrumb state. Creating a hidden session before Send, rewriting only renderer IDs, or recreating a page is not an established equivalent.

The next integration must bind browser-owner lifetimes to admitted durable drafts, first-Send routing and honest cleanup/recovery before exposing this path. The browser-only worker prerequisite below does not yet implement that admission or transfer/restore flow. Private preparation distinguishes the reference and current OMP boundaries in `.data/browser-draft-owner-2026-09-10/` and `.data/browser-first-send-owner-2026-09-10/`; it does not provide native, mounted UI or installed acceptance.


### Explicit native acquisition prerequisite

The maintained OMP18.1.10 patch now exports `createBrowserTabForOwner` with `BROWSER_TAB_OWNER_CREATE_VERSION = 1`. Its input supplies an owner ID, cwd, effective native settings and an AbortSignal. It does not construct AgentSession or SessionManager, load extensions, create a conversation, or select a model/account. The host must establish and retire that owner, preserve its configured settings and clean up its successfully acquired tabs; this primitive does not implement that lifetime or first-Send routing. Aborting the signal cancels acquisition, not a previously completed owner's entire resource tree.

Both this API and the existing `createBrowserTabForSession` use the same native configured-backend acquisition, create-only names, exact initial URL validation and acquisition deadline. The session adapter retains its live session-ID/disposal checks. If ownership ends before acquisition returns, cleanup can release only the exact returned tab still registered under its name; a same-name replacement is left alone. Backend errors propagate without another acquisition or a forced-headless fallback. Public declarations cover both APIs, while the existing session request/result shapes remain unchanged.

The controlled acceptance fixtures execute the selected native adapter with injected acquisition/registry dependencies. They cover metadata projection for the five existing backend kinds, preflight, in-flight retirement, same-name replacement and the existing session path. They do not execute any configured backend, SDK import, worker, browser, provider or UI. The maintained patch and declaration are separate from the installed package bytes; neither dependencies nor the lockfile were changed or installed for this source check. Actual host ownership, authenticated admission, durable recovery, first-Send restoration and native acceptance remain required before enabling draft Browser.


### Browser-only owned worker prerequisite

Worker protocol39 adds a separate `browser` initialization mode and `WorkerRuntime.createBrowserOwner`. The daemon starts it with the existing app-owned Bun/entrypoint/environment handling, captures the admitted owner ID and canonical cwd, and requires an exact owner response with no session snapshot. The child takes this path before importing OmpRuntime. It loads read-only native Settings and the explicit browser acquisition API, failing closed if that patch/version or owner-release capability is absent; there is no session fallback. This does not use SessionManager, model discovery, extensions or authentication discovery. Native browser configuration remains host-local and is read at initialization.

The worker exposes the existing browser create/metadata/control/frame operations under its original owner ID and worker-PID fences. Both session and browser handles share the parent operation implementations. Disposal first retires admission and aborts acquisition, waits for initialization and all already-started creations, then releases that native owner's tabs. Release failures remain visible through retained disposal and the existing acknowledged child shutdown. Initialization failure has no acquired resources to release. Client detachment alone is not owner disposal. The caller still must establish authorized durable owner identity, limits, lookup/reconnect, request journaling, retention and first-Send routing before any UI can use this API.

Controlled tests cover the actual lifetime class with an injected native backend and the selected actual parent WorkerRuntime class with a controlled client/path resolver. They exercise held setup/acquisition, retirement, malformed/foreign results, cleanup failures and explicit browser initialization/PID checks. The first lifetime fixture stalled because its pending-rejection assertion preceded gate release; that run/source and its exact test-process termination are retained. The repaired fixture records the outcome and asserts after releasing the gate. Main child code, dynamic imports, native settings/cleanup and IPC have source/typecheck evidence only; no worker/SDK/browser/provider was executed under the launch hold. The installed package still lacks the new acquisition API, so a current runtime launch is neither performed nor implied by this prerequisite.


### Durable draft browser identity prerequisite

`HostStore.draftBrowserOwners` stores a separately tagged draft resource identity, not a synthetic session. A new claim requires the actual saved draft revision/project and, for project drafts, the same owning-host project path inside the immediate SQLite transaction. The record captures the original binding; exact retries return it even after later text/model changes. Reusing the ID with different input fails. Returned objects cannot mutate the stored identity. This namespace does not alter the draft or session tables.

First claim atomically raises schema19 and preserves the valid legacy device policy. Reads do not migrate. Explicit retirement persists intent once and never frees an ID for reuse; it is not proof that native cleanup completed. Neither missing nor retained history grants worker liveness or authorizes replay of a native create request. The host packager now declares schema1–19 support; older artifact checks refuse a schema19 database.

This is a persistence prerequisite. A future authenticated admission service must resolve/validate the actual filesystem identity and projectless default directory, bind the durable record to the browser-only worker, retain exact native request history, and define first-Send/cleanup/recovery behavior. The record API is internal and must not accept a renderer-supplied path as a capability. No HTTP endpoint, worker acquisition, UI enablement or native transfer was added here. Disposable SQLite/reopen/failure checks and packaging metadata fixtures do not establish physical restart, installed artifact or native behavior.

### Draft directory admission

`DraftBrowserAdmissions` consumes the actual host store with a host-configured absolute default directory. Its request carries only host/owner/draft IDs and the saved draft revision. A new admission resolves the project from that saved draft, verifies its owning host and canonical catalog path, or resolves the configured default for a projectless draft. Caller-supplied cwd/project fields are not consumed. Missing, non-directory or symlink-substituted project paths fail before claim. Existing exact records preserve their original binding after later draft edits; retired or differently bound IDs cannot be admitted.

The returned admission captures directory device/inode identity and provides `assertCurrent` for the future worker owner to call immediately before use. It rereads the durable retirement/binding, catalog path and actual directory. An observed failure permanently invalidates that admission, even if the path later returns. The initial post-claim check can fail with history already saved; it does not roll back that identity. Filesystem checks are samples, not an atomic filesystem lease, and an unobserved replacement or physical restart has no identity-continuity proof. A fresh admission result is distinct from a historical record (`fresh: false`); history must not cause automatic worker recreation or native request replay.

This host-internal directory/record consumer is still unwired. HTTP authentication, worker registry/lifetime and native request journaling must consume it before a client can use draft Browser. No worker, endpoint or UI was started or enabled. Tests use real disposable directories, catalog/SQLite mutations, rename/symlink controls and an after-claim substitution; they do not prove native or installed behavior.

### Host-owned draft worker registry

`DraftBrowserWorkers` connects admitted draft identities to `WorkerRuntime.createBrowserOwner`. One host registry reserves the entry before asynchronous setup, joins exact callers and checks the returned ID/cwd/PID before exposing browser operations. Each operation samples the original admission before dispatch and after completion; observed retirement or directory/worker failure suppresses late results and retires live access. This does not turn a dispatched operation into a known non-event. Native create deduplication and durable outcome observation must still be provided by the request journal above it.

Workers belong to the host, not client connections. No disconnect/refcount auto-close is installed. Explicit retirement persists owner intent and drains setup/subscription/worker cleanup; a persistence failure still drains but returns an error. Host shutdown stops admission and waits for every owned cleanup, aggregating failures. Cleanup errors remain observable on repeated calls. Successful cleanup frees registry capacity while saved identity history remains. The default limit is32 owned/closing entries, checked before a fresh durable claim.

Read-only inspection starts no worker. Saved identities lacking a current entry are unavailable and cannot recreate automatically, including after setup failure or a new registry instance. Deliberate new identities are distinct requests, not retries of old creation. This registry remains internal and unwired to server routes; authentication, durable native request journal, client transport, first-Send restoration and UI are still required. Controlled worker handles with real disposable draft/catalog/SQLite state test its lifecycle; no actual worker/IPC/SDK/native browser or configured account was invoked.

### Draft browser creation history

`HostStore.draftBrowserCreations` records requests in a separate `draft-browser-creation.v1` namespace with explicit draft owner identity. It reuses the existing parsed browser request, exact hash and native metadata rules, while keeping session receipts unchanged. A new claim requires a real non-retired durable draft owner inside the same SQLite transaction. The owner already establishes schema19; this companion namespace adds no migration or new schema number. Older schema19 code has no draft creation consumer and must not gain one without this journal contract.

Request IDs retain the exact URL/ticket/intent hash and never become fresh again. The initial URL is not stored as a request field; completed metadata may contain the observed URL. Settlement validates host/owner/request, PID, native tab name/state and backend disposition, and preserves the first exact result. Returned object mutation cannot change stored history. Missing/corrupt owner or request records fail closed rather than repairing identity.

Read-only observation does not acquire a worker, refresh admission, settle storage or create a target. Same-epoch pending remains pending; an interrupted prior epoch is projected as unknown without changing its saved record. Retired owners can still observe or settle an already-claimed request, but cannot claim new requests. A historical completion is not proof the target remains alive. The future authenticated HTTP consumer must claim before native dispatch and persist settlement before reporting completion, with no recreation/replay on unavailable history. That consumer, renderer routing, first-Send and native/physical acceptance remain required. Tests use actual temporary SQLite connections/reopen/failure triggers and existing session-journal regressions; no browser/worker/SDK/IPC/native launch occurred.

### Draft browser HTTP ownership and creation

The host now routes `/v1/draft-browser-owners/:ownerId/{acquire,status,retire,create,open,creation-status}` behind its existing local bearer-token or authenticated/allowed-tailnet gate. Every operation is POST with the browser owner header and a bounded body containing draftId/draftRevision; creation actions additionally carry the existing parsed creation request. Client cwd/project fields are rejected. `acquire` explicitly establishes the admitted worker, `status` only inspects it and supplies a current ticket, and `retire` explicitly persists/drains it. There is no renderer caller or advertised draft Browser capability yet.

`create`/`open` claim a durable request before looking up the existing admitted worker. Lookup never creates a replacement. Same-request concurrent calls join; persisted results survive HTTP-handler recreation, while pending records without an in-process operation return unknown. Exact input conflicts remain conflicts. Epoch/age and eight-in-flight limits apply before new claims. Known native preflight rejection is recorded as rejected; lost response, retired/changed owner or invalid completion is unknown. Completion is returned only after journal settlement succeeds. Status checks never replay creation or probe native metadata.

Host shutdown closes admission immediately, drains owned workers and all active request settlements before store close, and retains cleanup failures in the host aggregate. Startup failure also drains these owners. Existing session browser routes reuse the unchanged bounded body reader. Source/typecheck establishes placement behind the existing authentication/device policy gates; Request/Response tests do not invoke that live server, socket authentication, IPC or native worker. Tests use actual disposable registry/store/journal with controlled worker handles and cover no-replay, claim/finish failure, blank/open intent, late shutdown settlement and concurrency. Draft client transport, metadata/control/frame routing, first-Send restoration and native/installed acceptance remain required under the unchanged launch hold.

Browser-only worker failures carry their captured browserOwnerId to the host callback. The draft registry retains its own failure subscription; those failures do not fall through into the server's model-discovery error state. Initial setup errors still reject the owner request. This classification is checked with the selected actual parent runtime and a controlled client callback, not a native crash or live model-picker test.

### Draft browser metadata and viewport reads

The existing draft owner handler also accepts POST `metadata` and `frame` operations with the original draftId/draftRevision. Frame input adds the exact workerPid/name/targetId. Both remain behind the existing server authentication gate and browser owner header; neither acquires a worker, creates a tab, refreshes durable admission, or fabricates a session identity. Responses explicitly carry ownerKind `draft`, hostId and ownerId. An absent owner reports not-started metadata; saved owners without a live entry remain unavailable. Frame reads require the existing matching worker.

Native metadata is projected through the existing tab parser, bounded to1000 unique tab names, and checked against the admitted worker PID. Viewports reuse the existing bounded JPEG/target parser. Eight distinct metadata/frame reads may be pending per handler; exact concurrent owner/PID/target reads join the same operation, and settled reads are removed rather than cached. The original handle and admission are checked after each read. Retirement, observed directory/worker failure and shutdown suppress late results. HTTP shutdown still drains active reads even if worker cleanup finishes earlier. These checks cannot prevent an unobserved external filesystem change between samples.

Request/Response tests use real disposable store/admission/registry code with controlled metadata/frame callbacks, including malformed results, retirement, shutdown and missing-registry recovery. They establish no actual SDK/IPC/capture, server socket/authentication, renderer transport, first-Send, UI or native parity. The HTTP/server integration and registry prerequisites retain their separate review statuses. Controls and client consumption remain required; no draft Browser capability is enabled by these read routes.

### Draft browser actions

POST `control` on the draft owner route accepts the original draftId/draftRevision and the existing parsed browser control request. It uses the existing128KiB/5s action-body reader so supported16,384-character text is not cut down by the smaller creation-body bound. Navigation, reload/history, pointer, scroll, resize, text and key actions retain the shared target/document/action validation and native dispatch path. This is server source integration only; no draft action UI or client capability is enabled.

Session and draft routes share `BrowserControlRequests` for ephemeral admission and receipts. Each instance owns its receipt table and a fresh control epoch. Draft metadata/frames now expose that control epoch, while status creation tickets keep the distinct existing creation epoch. Recreating a draft HTTP handler cannot accept an old control request against its retained worker. Clients must keep the original request immutable for retries and use a new request identity for a deliberate new action after refreshing. Session receipts keep sessionId; draft receipts instead carry ownerKind `draft` and ownerId.

Exact concurrent requests join before asynchronous lookup; contradictory input is rejected. The existing60s capture-age rule,4096 receipt limit and120s settled eviction remain. There is no disk action journal or automatic replay. Typed action payloads are not copied into receipts; returned context/URL/title remain bounded, untrusted page data. The age/eviction policy uses the existing host wall clock, not a new clock-rollback guarantee.

Original owner binding is checked before draft receipt retrieval; action dispatch requires the existing matching worker. Owner state is checked after initial lookup and again after the final post-action lookup. A lost owner after dispatch is unknown even when the old worker returned success. Known native preflight rejection remains rejected; other errors or malformed output are unknown. Active draft controls join the existing HTTP shutdown drain; client detachment does not itself cancel or replay an admitted control.

Controlled Request/Response tests use actual disposable owner/store/registry code and fake native callbacks. Shared session controls are verified by the existing five non-socket tests, with its socket transport test explicitly excluded under the launch hold. A private same-fixture comparison of the initial extracted engine versus the corrected engine reproduces a false completion during the final lookup; the correction returns unknown. This is intermediate-source evidence, not an old whole-app baseline or real page input. HTTP85f, observation a5eb and this action delta retain separate review scopes. Authenticated socket/IPC/native controls, renderer transport, first-Send restoration and installed/physical appearance remain required.

### Main-process draft browser transport

`DraftBrowserTransport` captures the original host endpoint/credentials and draft owner reference on construction. Its owner, creation/observation, metadata, frame and control methods submit one POST each to the explicit draft route; there is no session fallback, implicit acquire, response-driven replay or automatic redirect. Request parsers capture the exact input before awaiting the network. Client transport failure leaves mutation outcomes unconfirmed; callers must inspect retained status and preserve original request identity, not treat an exception as permission to repeat a side effect.

Responses require the owning-host header and matching protocol/host/owner without a sessionId. Owner status validates the saved record's original draft binding and retirement state, but returns no filesystem path record to the caller. Creation uses the same pure receipt projection as the host journal, now exported from the shared protocol; journal storage semantics/schema remain unchanged. Metadata validates up to1000 unique native names, frame/control enforce exact worker and target identity, and native content is projected through the existing bounded parsers. Response limits are64KiB for owner status,32KiB for creation/control,64MiB for the worst-case escaped1000-tab metadata list, and the existing8MiB JPEG plus envelope allowance for frames. Error bodies are capped at16KiB. Calls have explicit20s/60s timeouts and do not retry.

Controlled-fetch tests drive actual Request/Response handlers, SQLite owner/journal/admission/registry code and fake native callbacks. They check original endpoint/input capture, pending/settled/unknown status, malformed/foreign response rejection, bounded reader cancellation, and lost-response no-replay. The bearer header is a fixture assertion, not server authentication proof; no socket/IPC/Electron/worker/browser/provider ran. The main/preload/renderer consumer is still unwired, and acquisition/adoption on first Send remains a separate required ownership step. HTTP85f is separately source accepted; observationa5eb/controlscf40 retain their own pending reviews. This transport does not enable a draft Browser capability or establish native/physical/visual parity.

### Draft browser main/preload bridge

The production main entry now registers eight explicit draft-owner IPC handlers, and the isolated preload exposes their typed `draftBrowser` group. Every call requires the original ownerId/draftId/revision and an explicit hostId. The renderer supplies neither endpoint credentials nor a directory. Binding validation is shared with the transport; parsed creation/control/frame input and the draft reference are copied before asynchronous host lookup. Main uses the existing trusted-sender check before lookup and again afterward, rejects a different resolved host, then invokes the existing bounded transport once.

Preload contains only the channel adapter. Registration itself performs no host request. There is no global mutable selected owner, default-local/session route, automatic acquire/retry/retire or close-driven cleanup. Once submitted, a host operation retains its existing ownership even if a client disappears; callers must preserve original request/unknown state and explicitly inspect it. A bridge exception does not establish that a native mutation was rejected. These APIs are transport support, not a host capability announcement or permission for the renderer to enable draft Browser before its durable intent/lifetime/first-Send contracts are implemented.

Controlled tests execute the maintained preload adapter and registered handlers together, with a supplied trust predicate, host resolver and fetch responses. They cover all eight channels, explicit host routing, input capture during held lookup, invalid/untrusted admission, lookup-time caller loss, foreign/unavailable hosts and lost-response no-replay. They do not execute Electron's actual sender validation or context bridge. Existing transport tests separately use real disposable SQLite/host handlers and controlled native handles. The initial new fixture incorrectly called the existing wire creation field `request`; its failed run is retained, and the fixture was corrected to the unchanged `creation` field. This is not a production regression pair.

Observation a5eb has now received its separate source PASS/PASS receipt; the earlier transport paragraph's pending observation statement is historical. Controls cf40 and transport dd4 remain separate pending reviews at this bridge handoff. No renderer caller, capture, actual IPC/socket/native/provider, installation or first-Send restore/adoption ran or gained acceptance. The launch hold and incomplete whole-app/physical/visual gates remain unchanged.

### Window-local draft browser recovery intent

Window views can now retain a bounded `draftBrowserOwners` list containing only version1, hostId and the original ownerId/draftId/revision. This is recovery knowledge, not worker readiness, a page receipt or dispatch permission. The parser rejects malformed/extra fields, duplicate host/owner identities and more than64 entries instead of silently dropping entries or evicting earlier owners. Original references survive profile save/reopen and remain separate between window slots. The host's existing32-worker capacity remains separate; a larger local recovery list does not increase it. Older saved views without the field retain their prior shape. No schema19 host migration changes in this batch.

App's current view-save projection preserves the restored list across route/layout changes; it does not yet produce or mutate entries. `DraftBrowserOwnerCheckpoint` provides the future acquisition owner with an exact-reference barrier over the existing committed/saved callbacks. It waits for both committed presence and acknowledged persistence, rejects replacement/removal after presence, dropped/corrupt saves, errors and cancellation, and never writes an old route snapshot. Disposal rejects pending/future waits; failure clears a previously acknowledged view. References are copied on input. A new reference may wait through a pre-commit render gap, but no dispatch caller is wired yet. Host/draft/connection/action eligibility must still be revalidated by that caller after the barrier; this class does not authorize acquisition or browser adoption.

Controlled checkpoint tests and real temporary WindowStateStore saves/reopens cover these persistence rules. A same-final-test comparison selects the actual captured old parser with the actual unchanged store implementation: the old parser drops the reference list; the new parser retains it. That is a parser/Store cutover, not an old whole-App/React/native baseline. App passthrough is source/typecheck only, and a synchronous filesystem save is not power-loss or physical restart proof. No worker creation, bridge calls, native/UI/provider actions or first-Send transfer are performed. The hold and full completion contract remain unchanged. Shared desktop bridge41b814 and transportdd4 remain separately pending; cf40 controls now has its separate source PASS/PASS receipt, superseding only the earlier pending-status prose.

### Draft browser acquisition controller

`DraftBrowserOwnerController` consumes the real owning DraftController, explicit committed host/draft/connection/action context, the typed acquire/status/metadata bridge, and the window-intent checkpoint. It remains unmounted in App. Host identity comes from DraftController rather than a separately supplied transport default. `ensureSaved` persists a pristine empty draft to a positive host revision when needed, without consuming prompt/model/attachments or creating a session. A fresh explicit acquisition captures that acknowledged draft binding, publishes one new owner ID outside render, and waits for its window acknowledgement before invoking acquire once.

Observed route/action/connection loss and DraftController project/conflict transitions invalidate in-flight work, including loss-and-return between awaits. Readiness is not published until the response matches the original host/owner, reports a valid PID/ticket, and a guarded lookup-only metadata read reports that same worker. Status projection is captured before awaiting metadata. A fresh explicit inspection invalidates earlier queued readiness guards even if the same worker returns; repeated unchanged context alone does not. These guards require actual App committed-context delivery in the future consumer, including first-Send/action eligibility; the class does not intercept submission globally.

Lost acquisition responses retain the original window intent as unknown. Calling acquire again cannot resend an unknown request; explicit inspect only calls status and, for reported ready owners, existing-worker metadata. Starting/unavailable/retired states never create replacements. Only an explicit observed absence permits a later deliberate acquisition of the same saved owner ID and revision, after another window checkpoint. It does not mint a different ID or silently rebind a now-stale revision. Resolving stale/retired ownership into a new browser after first Send, explicit retirement and full window-owner coordination remain required next consumers. Construction/restoration issue no request. Disposal cancels unsent checkpoint waits and unregisters the draft observer; it does not retire or kill already-admitted host work.

Controlled tests use the actual DraftController/save-envelope path and actual checkpoint with supplied bridge callbacks. They cover empty-draft revision establishment, prompt/model preservation, save failures, original-ID recovery, all five held stages, project/action/route changes, conflict, wrong metadata PID, disposal and queued-guard lifetime. These are not authenticated server/IPC/native/React/first-Send tests. An intermediate-source pair isolates the earlier queued guard incorrectly becoming eligible again after a fresh inspection; the maintained final regression and regular import-only selected modules preserve that history. Existing DraftController and window-intent tests remain separate parts of the reported test count. Transportdd4 is now separately source PASS/PASS; bridge41b814 and window intent31b2 remain pending at this handoff. No draft Browser UI has been enabled, and the launch hold/full-goal gates remain unchanged.

### Window-owned draft browser lifetime

App now retains a DraftBrowserWindowOwner beside the existing terminal request owner. Its live intent list replaces the restored-list-only save projection, and the existing window observer delivers committed, saved and failed values. Construction and commit do not instantiate per-draft controllers, write drafts, inspect owners or acquire workers. Only explicit acquire/inspect methods create a controller against the actual committed DraftController. Browser buttons/panel creation still do not call these methods; enabling that path requires the draft-page creation/control adapter, host capability handling and first-Send restoration policy.

Recovery records remain in this window across host, route, settings and session transitions. All observed eligibility losses reach retained controllers; begin-Send and environment resume synchronously invalidate browser admission before their first await, and committed submission/busy state keeps it disabled. There is no implicit retirement, new owner after submission, session binding change or adoption. Conflicted drafts retain the lower controller's save guard. Reusing a same-ID replacement DraftController cannot rebind an existing owner. Multiple historical owners require an explicit original owner selection for inspection, never arbitrary fallback; identities from other hosts/drafts stay untouched.

The window owner counts pending identity reservations as well as saved records against the local64 cap, while failed saves with no allocated identity release the reservation/subscription. Concurrent attempts cannot dispose another attempt while its draft save is pending. Original references publish before checkpoint admission. A queued readiness guard also requires current committed and acknowledged exact references; storage failure or an observed record drop invalidates the guard without deleting recovery knowledge. Unmount disposes local subscriptions/checkpoint waits only, using App's existing deferred unmount pattern; host work remains retained. React/native scheduling and durable host retirement are not established by that source wiring.

Eleven new controlled tests use actual DraftController, acquisition controller and checkpoint through the window owner, with held saves/bridge calls and explicit commit/save events. The combined suite includes13 existing controller tests and8 window-intent tests; no whole-App mount or new Electron/native/HTTP call was run. The original controller30e5 and window-intent31b2 freezes are pending separate reviews; transportdd4 and desktop bridge41b814 now have separate source PASS/PASS receipts. No prerequisite approval is inferred from this batch, and all old freezes, launch hold and full GOAL requirements remain unchanged.

### Explicit draft page creation and recovery

DraftBrowserPageController carries a version1 page intent with a stable local instance ID, the complete original saved draft-owner binding and the existing launcher address/request state. It is separate from a session DockTab: it never invents a session or routes a draft through session browser methods. It reuses the maintained URL decision, launcher restoration parser, creation-ticket/request parser, draft receipt projection and native-tab projection. Restored pending requests become inspect-only unknown; a pristine launcher and an explicit empty address remain local.

A deliberate URL submit requires a captured original-owner readiness guard, reads that owner's status/ticket, captures its worker PID, allocates one page request ID, publishes the complete page intent and awaits exact window acknowledgement before one create call. The checkpoint and queued materialization callbacks are mandatory future consumers; the existing WindowView parser has not yet been extended to save this page-intent shape, so this controller is not App-enabled or durably integrated. After completion it also checks lookup-only metadata for the original worker and exact native name/target/backend/kind. It supplies a guard to queued replacement and requires positive acknowledgement, rather than assuming a scheduled setter succeeded. Observed connection/action loss and the upstream original-owner guard invalidate each await and queued admission; reconnection cannot revive the old attempt.

A sent request with a lost response, mismatched target or refused replacement stays unknown with its original address/request. Explicit inspection calls only creation history and, for a completed receipt, current metadata. Pending/missing history never creates, refreshes tickets or allocates a new owner. Confirmed rejection allows a later deliberate edit/submit with a new page request. Closing cancels an unsent checkpoint and suppresses late local publication without closing/retiring host work. This is not native first-Send ownership transfer or automatic cleanup.

Thirteen controlled tests exercise the actual page controller with held status/checkpoint/create/history/metadata/publication stages and supplied original-owner generation guards. No actual WindowStateStore, DraftBrowserWindowOwner, React/IPC/server/native call is made by these new tests. A same-final-fixture intermediate-source pair isolates mutable status-PID rereading: the earlier controller accepts a different completed worker after its retained response object is changed; the final controller captures the PID before awaiting and retains unknown. The old pair stops at the wrong publication; later fixed controls are not old proof. Ordinary typecheck covers maintained sources, not mounted/App/native behavior. Page-intent persistence and dock producer, actual owner-guard composition, host capability handling, BrowserPanel/frame/control consumption, first-Send native adoption/cleanup and installed/physical acceptance remain required. Window intent31b2 now has its separate PASS/PASS receipt; controller30e5 and window owner170c remain distinct pending reviews. No UI activation, native/retained-instance action or hold change.

### Persisted draft page requests

WindowView now retains a bounded draftBrowserPages list separately from session DockTab descriptors. The intent codec is shared between the page controller and window parser, with the controller's existing exports preserved. Each page must match an exact retained draftBrowserOwners entry. Duplicate host/page identities, a request shared by two local pages, invalid references and more than100 page records reject the entire save; no record is pruned to make a save succeed. The list cap follows the existing local window tab bound, not a claim about native browser capacity. Legacy views without the field retain their old shape. Pending requests restore as unknown with original URL, address, request and owner; restoration does not create a worker or page.

WindowStateStore now checks the serialized UTF8 document against its existing512KiB read limit before creating/replacing a file. This covers the complete document, including other panels and geometry, so a valid individual list cannot acknowledge a document the store would refuse on reopening. Oversize failure leaves the old in-memory acknowledgement and file intact. This is a new pre-write size guard; the existing fsync/rename/directory-sync contract is unchanged. It is not a power-loss or downgrade guarantee, and old window parsers still do not retain the new page field.

DraftBrowserPageCheckpoint observes the current window's committed and saved page lists, including exact parent owner/request/address. The original local page must already be committed; its new request can wait through the request's pre-commit gap. After the request appears, removal/clearing/replacement cannot revive it later. A dropped/corrupt/failed save, abort or window disposal rejects unsent acquisition; failed saves clear older acknowledgements. It writes no captured route and issues no bridge call. App now forwards the new field from restoration into its next save and delivers existing committed/saved/failed callbacks; it does not yet publish a live page list or connect page controllers to this checkpoint.

Eight new tests use the actual window store/parser and checkpoint, including the actual draft page controller with a real temporary file save and controlled browser callbacks. That flow proves no create before acknowledgement and inspect-only journal recovery from the reopened pending request; it does not run the real server, IPC, native browser or mounted App. Other tests cover owner/request conflicts, snapshots, windows, oversized saves and acknowledgement invalidation. Separate same-final-fixture comparisons isolate the old parser (current Store both sides) and old Store size admission (current parser both sides). The old parser acknowledges then loses the page list; the old Store acknowledges oversize. Their later assertions are fixed-only, not old reopen/retention evidence. Main/window-view and prior page-controller tests are included separately in the total count.

Page-controller class body is unchanged by moving its codec. The62faa page controller,30e5 owner controller and170c window owner remain separate pending source reviews;31b2 owner-intent persistence is independently PASS/PASS. Actual live page/dock publication and receipt persistence, owner-guard composition, UI/frame/control, host capability handling, first-Send/native adoption/cleanup and installed/physical acceptance remain required. Launch hold, retained instances and historical seals remain unchanged.

### Committed owner invalidation correction

Every DraftBrowserWindowOwner committed projection now reaches its checkpoint, including lists the manager rejects for dropping or rebinding a retained owner. Malformed lists also clear the manager's current attachment projection. A committed-list error remains latched across later save acknowledgements; saved values cannot rehabilitate admission while the current committed list is invalid. Recovery requires a valid committed list followed by a successful save. Ordinary save failures with an unchanged valid committed list keep their existing retry behavior. This preserves new-intent pre-commit waiting and never removes an original recovery record or changes its host, owner ID or draft revision.

Eight controlled regressions use the actual manager, acquisition controller, checkpoint and DraftController. A same-final-fixture comparison changes only the manager: the rejected170c body lets late saved(A) enable status-absent and acquisition despite a dropped, missing, malformed or rebound committed list. The correction blocks both entry points and permits fresh explicit inspection and same-ID acquisition only after valid commit/save recovery. Pending unsent acquisition cases verify cancellation, retained identity and no replay. Old failures stop at their first mismatch; later positive controls are fixed-only. These are controlled callback tests, not React scheduling, real window IPC/disk, browser acquisition or native proof.

The original170c Standards PASS / Spec REJECT report remains immutable; this is a separate correction awaiting independent assessment. The30e5 acquisition controller now has its own source PASS/PASS receipt. The62faa page controller and6c485 page persistence remain separate reviews. Launch hold, retained instances, first-Send adoption/cleanup and full functional/visual/physical completion gates are unchanged.

### Persisted confirmed draft targets

A draft page intent may retain confirmedTarget containing the positive native worker PID and bounded native tab metadata, including original target ID, observed URL and title. This historical projection requires the original creation request and its desktop-request name; dead, malformed or unbound targets reject the entire view instead of disappearing during save. Restored page requests remain unknown, including those with a saved target. The projection neither acquires a worker nor authorizes frame/control access; it is not a native first-Send transfer or proof the target is still alive. Legacy intents without the field keep their existing shape. Older codecs reject this new field; there is no downgrade compatibility claim.

DraftBrowserPageCheckpoint.waitTarget extends the existing committed/saved observer for publication after creation or recovery. The original request must already be committed. A newly confirmed target can wait through its pre-commit interval, then requires the full same request, owner, address, PID and native metadata in both committed and saved views. After that projection appears, a removal/rewrite cancels the wait. A previously confirmed PID/name/target/backend/kind cannot be rebound; refreshed metadata for the same target can wait for its new exact acknowledgement. Failed/dropped saves, malformed views, abort and disposal retain the existing cancellation rules. The checkpoint does not write snapshots, invoke browsers or replace App ownership guards.

Eight controlled tests cover real temporary WindowStateStore save/reopen and validation, target publication gates, identity rejection, metadata refresh, cancellation and the actual page controller. That controller test saves the original request before one controlled create, saves the verified target before publication, then reopens unknown and performs journal/metadata-only recovery. Browser callbacks and the owner guard remain controlled; it does not exercise the actual window-owner composition, React/App, IPC/server/native or physical restart. The existing page-controller class is unchanged and does not treat saved targets as authority. Pending original controller/persistence and committed-owner correction reviews remain separate. No App producer/UI/frame/control path is enabled in this batch; live owner/page integration, first-Send adoption/cleanup, installed and physical acceptance remain required under the launch hold.

### Window-owned draft pages

DraftBrowserWindowPages now consumes the actual DraftBrowserWindowOwner, page controller and page checkpoint. App retains it beside the owner manager, publishes its live intent list through the existing window view, delivers committed/saved/failed callbacks and the same committed DraftController/draft/connection/action context, and invalidates both managers synchronously before Send/resume awaits. Deferred window cleanup disposes local controllers/subscriptions and checkpoint waits; it does not retire host ownership. No Browser UI action is enabled by this wiring. A future local blank launcher must retain its own address/instance until explicit owner preparation, then call create with that identity; create itself only allocates local page state for an already-ready original owner.

Restoration, getters and committed renders never inspect or create native resources. Page requests and confirmed history remain window-owned through routes and disconnection. Explicit edits can remain local offline, while unknown requests cannot be edited or replayed. Same-host/ID DraftController replacement cannot reuse an existing entry. Actual owner readiness, the committed original page, controller generation and current request participate in admission and publication. Request removal after observation, invalid owner/page projections and save failures invalidate work; late saved values cannot rehabilitate a still-invalid committed list. New requests/targets retain their existing pre-commit windows.

A completed target is first checked against retained native identity before any history update. Its full projection is then published into the current list and must receive the actual page checkpoint acknowledgement before readiness is exposed. The supplied page-controller guard is checked again after this wait; readiness retains a separate owner/window guard because the successful creation controller then finishes its lifetime. Route/action loss withdraws readiness. A later explicit inspection builds a fresh local recovery controller and rechecks original journal/metadata; it never repeats create. Original unknown request and native history remain available through failed publication. Draft project/conflict notifications and observed owner/context loss cancel unsent publication waits.

Eleven controlled tests compose the actual managers/controllers, real DraftController and temporary WindowStateStore with supplied browser callbacks. They exercise both save barriers, restored recovery, sent disconnection, completed-route recovery, Send/project invalidation, malformed/dropped projection, request rewrite, controller replacement, history replacement refusal and offline/disposal behavior. An actual maintained intermediate-source comparison found and fixes a pre-publication history overwrite: the old new-manager body updated a different target before the checkpoint refused it; the fixed body refuses before changing retained history. A second intermediate comparison proves actual project loss settles an unsent request wait without another save: owner invalidation now also disables the page controller, cancelling its internal checkpoint signal. These comparisons use their recorded paired fixture revisions; the final suite also includes completed-route recovery. They are not pre-feature/old full-App baselines.

App wiring remains source/typecheck only under the launch hold. The tests do not mount React, call real IPC/server/native browsers, transfer first-Send ownership or prove physical persistence. The creation/status/frame/control backend is not exercised through installed UI by this batch. Local blank-launcher integration, dock placement/rendering, explicit tab close/native lifetime, frame/input and first-Send restoration/adoption/cleanup remain required. The62faa page controller is separately accepted;6c485/db384 persistence and5fe committed-owner correction retain their distinct pending review boundaries. Whole GOAL and all historical evidence remain unchanged.


### Draft preview source and shared BrowserPanel

BrowserPanel accepts an explicit draft owner and fixed native target, using the same viewport, address, fit, options and input controls as session previews. The preview source routes metadata/frame/control to the real draft bridge, captures the original reference/target, checks original page readiness before and after each await, and never acquires an owner or substitutes a session or target. Session arguments and the existing selection storage key remain unchanged. A newly supplied readiness source must obtain its own frame before controls become enabled.

DraftBrowserWindowPages exposes a guard captured from one acknowledged ready entry. A prior guard cannot revive after committed route/connection loss or a later same-target inspection. Controlled tests cover those manager-to-preview boundaries and explicit unknown/error outcomes; server rendering exercises the shared shell without bridge calls. These are source/controlled tests, not mounted React, IPC, native pixels, input or frame evidence. App draft-page presentation and first-Send restoration remain unwired here; the existing launch hold remains in force.

### Local draft browser dock identity

A new local draft browser descriptor uses an encoded `draft:` target, with the original host, complete draft ID and browser instance ID. The factory creates only an idle address draft; it does not acquire an owner, create a native browser or borrow a session ID. The current window parser preserves its title, address and right/bottom placement. It rejects native targets and pending/unknown acquisition claims in this local descriptor; those belong to the separate draft owner/page records and their save guards. Malformed draft dock state rejects the whole save rather than acknowledging a dropped address. Existing non-draft legacy fallback remains unchanged.

Controlled temporary-store evidence covers save/reopen, movement, coexistence with session browser tabs, invalid-state preservation and refusal by the session-only acquisition controller. The selected old parser acknowledges the new dock but reopens without it, so this is a forward parser cutover, not lossless downgrade compatibility. The factory remains unwired to App actions until the shared draft UI/controller path consumes this exact identity and preserves original-owner admission through close/route/Send transitions. This source prerequisite does not enable a placeholder browser action or claim native/React/first-Send acceptance.

### Draft browser dock integration (source; not native acceptance)

The new-chat Browser action now inserts the persisted local draft descriptor in the ordinary right or bottom dock. It neither creates a session nor acquires a browser by rendering. The App reader handles the draft namespace before its session/project/file reader. A controller is retained for the original runtime presentation and actual DraftController; queued address/title updates revalidate that presentation. The shared blank browser/address and preview components provide the initial and acknowledged-page surfaces without a separate shell or CSS.

Explicit URL submission saves the draft, allocates and acknowledges its original owner record, acquires that owner, then creates a local page intent. The local page must itself commit and save before the page controller can request a creation ticket. Existing request and confirmed-target save barriers still apply. The preview receives the original owner/page readiness guard. An existing unknown page uses explicit owner inspection followed by original-request creation inspection; no acquisition or creation fallback is attached to a status check. Multiple saved owners require an explicit choice. Closing the initiating presentation aborts unsent work and suppresses late results while retaining any allocated owner and page request for recovery; it does not retire a host worker. Send invalidates admission synchronously, and committed route/connection changes invalidate both managers and the presentation controller.

Controlled validation uses the actual controllers and temporary WindowStateStore, plus static markup of the shared initial panel. App hooks, actual React commits, Electron IPC, native browser/frame/control, first-Send transfer/adoption and physical persistence remain unexecuted. Required follow-up remains: draft-aware command address focus/search, workspace-layout singleton/cleanup and address suggestions, intentional closed-page/history disposal and fresh-owner recovery, first-Send owner transfer/restore behavior, and matched full-App native acceptance. Layout-step new-browser eligibility remains session-only until its draft singleton contract is wired; existing content layout controls still apply. No existing GOAL requirement is excluded by these boundaries, and the launch hold remains in force.

The window-pages manager observes the actual draft project transition itself, before evaluating owner readiness. A restored page may subscribe through offline editing before the owner controller exists; it must still invalidate its page admission generation and abort request and target save waits on that one notification. Listener registration order is not an ownership guarantee. Equivalent draft text updates remain non-cancelling, existing conflict rejection remains unchanged, and construction/offline editing remain free of bridge work. Controlled regressions cover both listener orders and both waits, capturing settlement before teardown without another App commit or save. This corrects the separately rejected8bee subscription-order defect on the later source lineage; original8bee and c881 integration freezes are unchanged, and neither gains retroactive approval.

Draft dock instance IDs use the existing page-intent alphabet: one to100 ASCII letters, digits or hyphens. Both the local factory and saved draft-dock parser reject underscores instead of accepting an identity that cannot later bind to the original page intent. IDs are not rewritten or replaced; a malformed draft dock causes the existing whole-view save rejection, retaining the last good file. Host IDs and session-browser instance rules are unchanged. The controlled correction proves explicit IDs through actual save/reopen and page-intent binding; it does not establish old installed-profile migration, native behavior or App acceptance. Original5b17326a rejection is preserved separately.

Draft browser address focus now joins the existing installed command and palette path. Local and confirmed draft previews expose one host/draft focus group, separate from the original worker/page/selection identity. App publishes the current group on its committed workbench boundary; settings/plugin and active submission suppression remain in force. The existing focused-browser, right-panel preference, Bottom fallback, editable/IME gates, dirty address selection and deferred original-node validation are reused. Changing the group never changes native targets or stored selection keys. Controlled listener/document tests and real shared-panel static markup validate this source cutover; App mounting, command-menu close timing, actual keyboard/OS focus and native page input still require acceptance under a lifted launch hold. Draft command-search results, singleton/cleanup/suggestions and first-Send ownership remain separate required work.

## Draft workspace-layout command

The installed Step workspace layout command now uses the same unified right-panel transitions for the current draft as for a session. An available draft browser opens a local New tab descriptor without acquiring a worker or inspecting browser state. Closed/full retained content returns to split with the saved width; split content hides unless the exact right singleton is an unused current-draft launcher. Bottom remains independent.

Draft removal additionally requires its original presentation controller to have observed a pristine local page, with no pending operation, page intent or unresolved owner inspection. Address drafts (including an explicit empty string), titles, foreign owners and request/native history remain protected. The existing workspace-action/terminal cleanup fences still apply. A queued update and deferred Chat focus each revalidate the committed draft owner; submission or route changes cannot authorize an obsolete draft action. An unavailable draft can still hide/reopen retained content, but cannot create or dispose a browser. Controller disposal follows committed presentation removal.

Evidence in `.data/draft-browser-workspace-layout-2026-09-10/` covers the pure transition, actual hook with controlled React queues, the actual transpiled App callback and existing controller tests. This is source/controlled evidence, not a mounted App, native keyboard, browser worker or pixel result. The reference's separate non-unified/no-controller branch, draft search/address suggestions, previous-draft cleanup and first-Send native ownership remain required; this change does not claim those paths.

## Browser search page eligibility

Rechecking pinned `NG.getOpenTabs` establishes two separate gates: a WEB snapshot and a nonempty observed URL. Local NEW_TAB_PAGE launchers do not become search results through their default/custom title or unsent address draft. The existing window search projection now follows those gates instead of returning title-only placeholders; native WEB `about:blank` remains a page. Literal AND matching across saved title, page title and URL, insertion order, ten-result cap, hidden-dock activation and the shared four-read admission bound remain unchanged.

The earlier local-New-tab search behavior was an app adaptation, not pinned registry equivalence. Its tests now reflect the actual eligibility rule. `.data/browser-search-page-eligibility-2026-09-10/` preserves the before bytes, paired projection regression and exact primary windows. No dock, page, address, owner history or session is deleted by filtering search results.

This fixes eligibility in the existing session-window projection only. The pinned registry additionally retains observed snapshots and owner routes independent of active/visible docks. The app still needs that observed-history/refresh-failure behavior, draft page-intent and original-presentation mapping, exact route activation, closed/history/fresh-owner handling and full registry coverage. Do not substitute a live-metadata-only window list for that complete contract, or treat a saved confirmed target as current readiness. Draft page search integration remains required; unsent address text must not be used to fill missing observed URLs. No native/DOM/IPC/installed or whole-GOAL result is claimed.

## Retained observed browser search data

The session-window search consumer now keeps previously observed title/URL text across palette closure, offline state and failed or malformed refreshes. A record belongs to the original runtime dock presentation and exact native target; it is not native readiness, a restore request or control permission. Only committed presentation transitions update registry ownership. Closing/reopening, changing a target, or loss of the original presentation removes that record. A validated full native snapshot can establish target absence/death/replacement; unavailable responses merely make its retained text historical. The palette labels historical details “Last observed”.

The existing shared four-read limit is retained. Committed open/mode, connection, bridge or target changes invalidate delivery generations before passive effect cleanup, so an old uncancellable read cannot refill the current registry. Current custom titles are combined with observed page title/URL; literal matching and registry insertion order are retained. Entries carry their original presentation/target key, rechecked both before route activation and in the actual queued dock update. Hidden-dock movement preserves the presentation and selects its current destination.

`.data/browser-search-observed-registry-2026-09-10/` contains the exact source delta and controlled registry/hook/queued dock evidence. This is an in-memory registry for the current session-owned window entries, not full Codex registry or restart persistence. Seeding from preview events/persisted observations, draft owner/page-intent integration, closed/browser-use/detached inventory, route/history/fresh-owner/first-Send behavior and native/physical/visual acceptance remain required. The prior eligibility correction and all earlier draft/browser reviews stay separate; no proof is inherited merely by importing their bytes.

## Draft layout capability correction

Step workspace layout remains available for retained content when the Browser action is unavailable. Its dock updater now requires that action's capability before disposing a pristine draft singleton, as well as the original controller and cleanup guards. Without capability, Step hides and reopens the same presentation. Session behavior and creation eligibility are unchanged.

The original workspace-layout freeze failed this boundary: its capability flag guarded creation alone. `.data/draft-layout-capability-correction-2026-09-10/` retains that failure and a same-fixture correction using the actual transpiled App action and actual dock hook with controlled React queues. This checks unavailable hide/reopen and subsequent available disposal without bridge access; it is not mounted App, native capability delivery or queued capability-loss proof. The original review and all earlier sealed evidence remain unchanged.

## Draft page history in browser search

The window search consumer now seeds draft entries from a confirmed page intent's observed title and nonempty URL. It joins that intent to the original host, draft, page instance and attached dock presentation. Local launcher text and saved native identity without an observed URL cannot create a result. Seeding confirmed history labels it “Last observed” and does not grant native readiness or initiate owner inspection, acquisition or control. The separate read-only search refresh below can update these historical details. An ordinary default New tab label yields to the observed page title; a custom dock title remains in the literal search corpus.

App selection carries the original presentation, owner, request and native target key. It checks the currently committed page list, and the queued dock update checks that list again before activating the existing destination. Hiding or moving preserves that destination's identity; closing/reopening or replacing the page binding rejects the stale result. The current draft route is explicitly `new-conversation` on its owning host. Other saved draft IDs must not be silently mapped there or made into a session. Their genuine route/adoption during first-Send remains required.

`.data/draft-browser-search-history-2026-09-10/` retains projection, actual hook/queued dock and transpiled App callback tests, plus a disposable WindowStateStore save/reopen. These are source and controlled tests, not React mounting, browser IPC or physical restart evidence. Session search and its pending observed-registry review remain distinct. Live draft metadata refresh and preview-event updates, durable session observations, closed/browser-use/detached inventory, arbitrary saved draft routing and fresh-owner/first-Send/native/installed/visual acceptance remain required. This history integration does not close the complete registry contract.


## Live draft search refresh

While the command palette is open, its search owner can read metadata for original confirmed draft owners on connected hosts. It uses the existing lookup-only draft metadata endpoint and captures the full owner reference before waiting. Draft and session rounds share one four-request admission limit across replacement rounds; abort prevents queued dispatch and suppresses delivery without pretending to cancel sent IPC. No status, acquisition, page creation or control fallback is involved.

The registry validates returned draft identity, native PID and exact target before updating title/URL. Unavailable, failed or malformed responses retain last observed text as historical. A valid full snapshot establishing absence, death or replacement removes that search observation without deleting the dock, page intent or native resource. Repeated commits of the same saved history do not overwrite live observations or resurrect confirmed absence. An empty URL is rejected by the existing native metadata parser and cannot serve as authoritative absence evidence. The default New tab title follows the live page title; explicit custom titles are preserved.

This read path updates search details only, not page readiness, saved confirmed targets or backend ownership. `.data/draft-browser-search-refresh-2026-09-10/` preserves the exact consumer and shared admission tests. Actual React/IPC/socket/native and installed verification remain outstanding. Preview-event updates outside search, durable session observations, full closed/browser-use/detached registry coverage, arbitrary saved draft routes and fresh-owner/first-Send remain required; this does not complete browser parity or approve preceding snapshots.

## Search activation commit receipt

Browser search navigation now follows a runtime receipt from the same queued update that activates the original keyed dock presentation. An initial callback check alone is insufficient: another queued change may remove, reopen or retarget that tab. Rejected or superseded activation cannot navigate. The receipt is not serialized with window state and does not create a native resource.

A committed observer checks original presentation/page identity and active destination before navigating to its owner. It waits for that route's committed render, captures the original tab button, and rechecks the receipt, route, presentation, node and current focus at animation-frame delivery. It never resolves a replacement button by reusable tab ID as a fallback. A newer route, settings transition, selection, user focus or unmount cancels pending focus.

`.data/browser-search-activation-correction-2026-09-10/` contains the separate correction to the original1883 rejection. The same final fixture executes the maintained App callback and dock hook: the prior callback navigates before queued rejection, while the correction waits and suppresses it. Controlled node/route/RAF shapes prove the observer's decisions, not mounted React/Radix/DOM/native behavior. Draft and session route consumers share this receipt; their preceding search/refresh reviews and all broader browser gates remain separate.

### Preview metadata and window search observations

The App shares its window search registry with the palette hook and both session and draft preview panels. Before an existing preview metadata request, the panel captures a publication callback for the original committed presentation. It publishes that request's validated full metadata envelope only after the existing source/selection guard passes. Callback changes during an await cannot transfer that observation to another presentation; render-only callback changes are not committed. This does not add a metadata read, acquire or inspect a draft owner, or create a browser. Frame title callbacks remain separate; frame snapshots do not become full registry snapshots.

Preview and palette reads use per-record admission revisions plus the existing delivery generation and original record identity. Later-admitted reads supersede earlier ones; removal/reopen/retargeting or delivery invalidation rejects old results. A single preview delivery updates only its captured presentation, preserving sibling observations. Invalid/unavailable details retain historical text; a validated full snapshot may establish that the original target is absent. Existing custom-title and saved-draft identity rules remain. Saved details confer no native readiness or restoration permission.

The source-only acceptance uses actual panel/hook code with controlled React slots, callback ordering, metadata promises and document/RAF shapes. It includes a same-final-fixture prior-Panel comparison, closed-palette consumption with zero search calls, and session/draft original-owner controls. It is not mounted React, DOM, IPC, native browser or first-Send proof. The broader retained registry across closed browser identities and owner transitions, fresh-owner recovery, native/UI parity and first-Send ownership remain required, as do separate review gates for the preceding search changes. The launch hold remains in effect.

### Restored session search observations

Window view persistence now retains observed page title/URL text for each exact session-owned browser dock identity and native target. The versioned record contains the original host, session, dock ID, worker PID, native name and target ID. It stores no control context, creation ticket, readiness, frame or runtime presentation key. The existing local window store supplies its validation, byte limit, atomic-file/directory-sync acknowledgement, visible errors and independent window slots; no host/shared-preference schema is added.

The window parser refuses malformed, duplicated or owner/target-mismatched observations instead of acknowledging a dropped history field. Records must correspond to an attached canonical session browser dock, including hidden docks; a local New tab or draft page cannot be relabelled a session. Native identity/text limits and the existing 100-tab capacity bound the records. Legacy views with no observation field still load.

The App constructs its registry from restored records and includes current observation projection in its normal window save. Before the first registry commit, that projection preserves only records matching the restored dock, preventing the first render's save from erasing history. First commit binds them once to the new runtime presentation as historical text. A later close/reopen, native retarget, or initially absent binding cannot reuse bootstrap records. Render-only projections do not mutate ownership. Live lookup or preview metadata can refresh text, unavailable results preserve history, and validated original-target absence removes the saved observation without deleting the dock.

This is persistence for retained session dock observations, not the full reference registry. It does not enumerate closed/browser-use/detached pages, acquire or revive a worker, guarantee mixed-version downgrade preservation, or solve arbitrary draft routes/first-Send/fresh-owner transfer. Those requirements remain open. Controlled actual WindowStateStore save/reopen, current registry and same-fixture prior-parser comparison establish source behavior only. App forwarding is source/typecheck-supported; no app process restart, mounted React, IPC, native browser, installed or physical acceptance occurred under the launch hold.

### Search route-wait overlay cancellation

After keyed search activation is accepted, a committed Settings or Plugins transition while the original route is still waiting now retires the selection. Returning the overlay to its original state or later committing the destination cannot revive that selection's deferred focus. A fresh deliberate selection is required. Searches begun from an already-open overlay still complete when their requested destination route and overlay close commit together; repeated unchanged waiting commits do not cancel them. Existing admission, original-key and scheduled-frame guards remain.

The correction is limited to the shared selection observer and focused regressions. The same maintained runtime/fixture comparison executes controlled RAF callbacks: old source focuses after both open-return and close-return overlay excursions; corrected source does not, and fresh-selection and requested-route controls pass. These are controlled node/commit shapes, not mounted App, DOM/OS overlay timing or native focus evidence. Original4e57/1883/49b freezes and their reviews remain unchanged; this correction and the separate draft-page regression supplement need their own assessments within the existing reviewer contexts.

### Native original-target release prerequisite

The maintained OMP patch now exports `releaseTabForOwner` and `BROWSER_TAB_OWNER_CLOSE_VERSION=1`. Admission requires the exact native creator, tab name and target ID. Invalid, foreign, absent or already-dead-without-cleanup targets reject before dispatch; there is no lookup-by-URL, acquisition or replacement fallback. A pre-aborted signal rejects. Once admitted, the operation finishes its cleanup even if the caller stops waiting. The future host must retain request/outcome identity and distinguish a cancelled wait from confirmed release; this native helper is not that journal or transport.

One cleanup drain is retained per native object before callbacks run. Existing release and forced cleanup join that same drain, including synchronous abort reentry. The first cleanup owns its mode/options; later calls join rather than escalate to an independent destructive cleanup. Captured cleanup removes the name only if the map still contains that exact object. Both normal and forced paths finish browser release after earlier worker/orphan failures and raise the first cleanup failure. No name reuse or later target can be deleted by a stale cleanup result.

Owner release always uses `kill:false` and keeps the configured native backend behavior. A released managed tab does not imply destruction of an adopted external page or the last cmux surface; that native exception remains explicit. The controlled fixture supplies backend handles and workers and evaluates actual selected supervisor functions. It proves identity checks, replacement/reentry/drain ordering and error behavior, not actual browser/worker acknowledgement, OS cleanup, backend access or installed compatibility. API declarations are compiled separately; ordinary repo typecheck does not cover the private full native source graph. Installed sources and lockfile remain unchanged.

This is an unwired native prerequisite for faithful Close and separate Undo close. It does not yet change DockPanel/App close, add authenticated host receipts, restore a closed page, retire an owner, transfer first-Send ownership or approve retained search registry behavior. Those consumers and physical/visual acceptance remain required under the launch hold. Exact old/fixed cleanup comparisons use the same final controlled fixture; they do not execute OMP or a native browser.


### Worker original-target close boundary

Internal worker protocol40 adds `closeBrowserTab` to both session and browser-only owner handles. The parent validates and captures the original worker PID/name/target and owner identity, sends one request, and accepts only the child's matching confirmed result. It does not import OMP, create another worker, apply a desktop timeout, or treat a missing result as success. The child loads the separately maintained version1 native close API lazily, then revalidates the same owner object and ID before calling it. Unsupported packages and owner replacement during import fail closed, with no legacy name-only fallback. Existing pinned native backend and cleanup behavior remain the native prerequisite's responsibility.

The child registers close work before invoking the loader. Retirement prevents new close admission, waits for already-dispatched native closes, and then disposes the browser owner and session runtime. A close waiting on import is rejected before dispatch when retirement begins. Both explicit disposal and shutdown share retained native cleanup; failure of close cleanup cannot skip other owner cleanup or produce a successful disposal acknowledgement. The existing emergency worker-exit deadline remains an error boundary, not a promise of successful OS cleanup. An operation that has already reported a failure is not retroactively a successful release.

Controlled tests exercise the new owner/target checks, import and retirement interleavings, malformed confirmations and mutable inputs. The acceptance script evaluates the actual maintained child request/disposal functions and parent WorkerRuntime class with controlled native API, client, owner and response functions. Neither it nor the tests launch workers or import the native SDK. Required-method fixture migrations fail if these older workflows unexpectedly attempt close. Ordinary typecheck covers the maintained worker and fixtures; it is not native module/IPC execution or an installed-package test.

This is a worker prerequisite. Authenticated close routes, durable command/result ownership, host and renderer consumers, native closed-event registry removal, separate hide/detach/undo behavior, first-Send and native/physical/visual acceptance remain required. The preceding native patch has its own independent review; this integration supplies no approval for it and leaves installed bytes unchanged. The launch hold remains in force.


### Native close first-error and public-type correction

The ordinary headless close fallback now preserves a prior termination failure when orphan cleanup also fails, then still attempts browser release. Failure to receive the initial closed acknowledgement selects the fallback; the first cleanup error remains the termination failure rather than being overwritten by the later orphan error. The focused controlled scenario rejects all three cleanup stages and verifies release is drained before reporting that first error. It exercises the ordinary close entry, separately from force-first cleanup.

The public supervisor declaration now exports the same readonly name/targetId shape referenced by the owner-close function. A maintained declaration runner invokes the existing compiler with `skipLibCheck:false`, checks the actual public files plus positive and negative consumer cases, and retains the complete compiler log and all dependency diagnostics. It verifies readonly fields, required native identity and closed-result typing rather than relying on a positive call through an unresolved any-like type. This is a targeted public-contract gate: the expanded dependency graph still reports unrelated declaration errors and exits1; it is not a full SDK graph compile pass. Ordinary repository typecheck remains separate.

The same final controlled fixtures reproduce the rejected source's orphan-error substitution and undeclared public target, then pass those bounded checks for the correction. Initial TypeScript API setup failures remain preserved as setup failures, not regression evidence. Only the native supervisor source and its public declaration patch sections change; original exports and other patch sections remain byte-identical. Installed sources/configuration remain untouched. The original1e9 rejection is immutable; the correction requires assessment in its original reviewer contexts. Separate worker integration, host/UI/Close/Undo/native/physical and whole-GOAL gates do not inherit approval.

### Durable host close admission (2026-09-10)

Session `POST /v1/sessions/:sessionId/browser-close` and `browser-close-status`, and draft `POST /v1/draft-browser-owners/:ownerId/close` and `close-status`, now share a close-request journal and executor. A close request carries its original request ID, creation-ticket `controlEpoch`/`observedAt`, and exact worker PID/name/target ID. Draft bodies additionally retain the original `draftId`/`draftRevision` and nest the request under `close`. The existing session creation ticket or draft status ticket supplies this epoch; the separate draft interaction-control epoch is not a close ticket. These routes remain behind the existing host authentication/device admission and owner-header check. This source change does not add desktop IPC or enable App close.

The first successful claim atomically raises the host-state floor to schema20, preserving the prior device policy; reads do not migrate. Session and draft namespaces are distinct, and a draft owner ID cannot be rebound to another draft/revision by reusing a request ID. Claims precede lookup of an existing worker; neither close route acquires an absent worker. The shared executor admits at most eight outstanding requests per instance (session and draft HTTP each own an instance). The durable ID/hash reservation outlives that bounded pending map, route recreation, epoch expiry and host restart. A prior unresolved request is inspect-only unknown; no retry replays close. A new explicit request is a separate admission.

Completed means the exact original owner/PID/name/target returned `released: true`, the worker binding survived post-operation checks, and the matching journal finish committed. It does not mean an adopted external browser process was killed. Native pre-dispatch refusals are rejected; sent failures, replacement or missing confirmation are unknown. A failed durable finish returns a storage error and leaves the reservation unresolved. Close-status reads no native metadata and cannot turn absence into successful close. Stored receipts are historical outcomes, not current target availability. Admitted closes drain before the host store closes; draft worker cleanup and request drains start independently, and a failed cleanup cannot skip the outstanding close-history work.

Validation uses disposable SQLite/files, controlled worker handles and actual Request/Response routes. It neither launches a host/worker/native browser nor exercises authentication over a socket. Initial schema20 migration-guard failure is preserved separately from its correction. Native API correction774 remains a separate review of rejected1e9; worker0819 has its own bounded source acceptance. Desktop transport, App close/closed-event removal, hide/detach/undo, first-Send ownership and installed/native/physical/full appearance acceptance remain required. No preferences, installs, held instances or frozen reports are changed by these controlled validations.

### Desktop close transport (2026-09-10)

The isolated preload now exposes `browserClose.close(owner, request, hostId)` and `browserClose.status(owner, request, hostId)` for the existing shared session/draft close contract. This optional bridge signals desktop transport support only. It neither proves host/native capability nor enables an App close action. Main captures and validates the exact owner, ticket/request and target before asynchronous endpoint lookup, then checks the original trusted sender and explicit host again in the same continuation that starts transport. Credentials and origin stay in main. Existing draft-browser operations and native session routes retain their own interfaces.

Both operations use one original captured endpoint/owner and a single bounded JSON POST, with redirects rejected, a60s close or20s status deadline and32KiB success/16KiB error response limits. Close uses the host creation/status ticket epoch, not the separate draft interaction epoch. Response validation includes exact host, owner kind and IDs/revision, request ID, worker PID/name/target ID and `released:true` confirmation; status also validates the outer identity and nested receipt. Missing/pending history does not cause automatic close, creation, metadata lookup or a fallback request. Only explicit validated pre-admission400/405 INVALID_REQUEST or409 OWNER_MISMATCH can return a rejected close receipt. Other HTTP failures, timeout, transport loss or malformed confirmation remain errors with no replay. A client timeout cannot establish whether a sent close completed and does not undo the host's retained drain.

Controlled validation uses the actual transport/preload adapter and registered handler callbacks, plus disposable real host journals/RequestResponse routes behind a controlled fetch function. It does not import Electron at runtime or start a host/socket/browser/worker. Sender checks model trust before/after endpoint resolution; no native document-generation or OS navigation timing claim. The initial discriminated-union expectation compiler error is fixture-only; production bytes did not change for its repair. Shared identity validation is reused for receipts and the new close-status projection; existing host tests cover its predecessor receipt contract. The host261b source review remains separate, as do accepted native774/worker0819 scopes and historical1e9 rejection. Durable App intent/admission, close versus hide/detach/undo, native closed-event removal, first-Send and installed/native/physical/full appearance acceptance remain required under the launch hold.

### Browser Close: window-local intent and acknowledgement prerequisite

`browserCloses` retains the explicit host, session/draft owner, original presentation incarnation, immutable close request and optional strictly matching receipt. The window parser rejects malformed, duplicate or over-limit history instead of silently trimming unresolved requests. The list is bounded to 100 entries under the existing complete 512KiB document limit. History does not require a currently mounted dock tab or draft owner; a saved presentation incarnation is historical information, never permission to delete a restored/reopened tab. No host/session identity or existing draft/page/observation serialization changes.

`BrowserCloseCheckpoint` observes the current window's committed view and subsequent save acknowledgement without writing a captured route. Newly queued requests require an explicit absent predecessor; receipt publication can name its exact pending predecessor, preserving the original owner/request/presentation. An unrelated predecessor, committed removal/replacement, malformed history, failed save, abort or disposal rejects waiting admission. A failed/malformed commit clears current authority; a late saved view alone cannot restore it. A later valid commit still needs a subsequent save. This component acknowledges persistence only: it has no bridge calls, automatically restored dispatch or native readiness claim.

The consumer remains required: publish before dispatch, use the current original target/owner/capability guard again after the acknowledgement and in queued removal, retain unknown requests without replay, save exact outcomes before local removal, and offer explicit read-only history recovery. App wiring, original-incarnation close gating, closed-registry events, hide/detach/Undo, first-Send, actual React/IPC/native and physical acceptance remain open. Current coverage is a real temporary WindowStateStore save/reopen plus controlled checkpoint observations, not a mounted App or a power-loss/downgrade guarantee. Desktop transport941 is independently submitted; durable host261b, worker0819 and native774 have separate bounded source acceptances, not approval of this prerequisite.

### Browser Close: explicit window-owned operation

`BrowserCloseWindowOwner` consumes the saved-intent checkpoint for explicit session/draft Close and read-only status recovery. Construction, restoration and commits do not invoke a host. The caller supplies the original presentation, exact native target/owner and a committed generation guard. Session readiness uses its read-only metadata creation ticket; draft readiness uses the original owner status ticket followed by matching existing-target metadata (the separate viewport-control epoch is not a close ticket). No acquisition, frame, creation, alternate target or automatic retry is used.

The owner reserves a local operation before callbacks, saves the original request before close, copies transport arguments, then validates and saves the exact returned receipt before returning a removal guard. Source loss cancels pre-dispatch admission; after dispatch it suppresses removal while preserving a known receipt if the window can still save. Unknown/lost/malformed results retain the original request for explicit status inspection, without replay. A host-confirmed rejection permits a new deliberate close with a new ID while keeping history. Restored requests perform no operation until explicit inspection. Current source retention compares identity fields, not JSON property order.

This class is not yet connected to App or DockPanel. The App must deliver every committed connection/owner/capability loss, supply a captured generation rather than a reusable boolean, include the current intent list/save observer, and protect unresolved original sources from automatic cleanup. It must revalidate the returned guard AND the original keyed presentation inside the queued dock removal. Pending/unknown/rejected UI, explicit status action, completed-history retirement, closed-registry removal, local unsent launcher close, hide/detach/Undo and actual native close acceptance remain required. A returned completion is a saved receipt; it is not itself a tab removal, native event observation or current-target acquisition. Controlled bridge handles and real temporary window files do not prove mounted App/IPC/native behavior.

The close-history parser now visits every array index, including sparse holes. A sparse `browserCloses` array is rejected before WindowStateStore writes or acknowledges it; it cannot serialize into `null` entries that invalidate the entire window on reopen. The exact previous parser is retained in the original rejected intent freeze. Actual-store paired regressions cover an all-hole array and a valid entry followed by a hole, observing save acknowledgement, unchanged disk bytes and the reopened prior route/history. This correction changes no checkpoint/controller/Store logic and provides no App/native integration approval.

### Browser Close App consumer (2026-09-10)

The App now owns `BrowserCloseDockOwner` alongside the draft/page managers. Its committed context observes route, settings/plugin and owning-host connection loss before the close owner sees the window view; loss followed by return cannot revive an original operation. The existing window save observer forwards Close history and its acknowledgement. Both DockPanel regions dispatch browser Close through this owner; file tabs keep their existing close gate. A browser callback returns false to the generic async ID-based remover. Confirmed native completion and its saved receipt instead reach a queued original-presentation/descriptor check in `useWorkbenchDock`. Rename/unread updates can survive, but removed, reopened, moved or retargeted sources cannot be deleted by an older result.

Session targets use their existing worker identity. Draft targets come from the original live preview and its captured page guard, using the owner status ticket rather than the viewport control epoch. Saved target history alone does not authorize Close. Unsent local launchers can be removed without native calls, including offline, while pending/unknown creation, a saved page request/target or an unresolved workspace replacement remains protected. Queued local removal rechecks those conditions. Lost or invalid replies retain the tab and original request. A visible Check close status action performs only the original saved request's status read; restored presentation identity is never rebound to authorize removal. Existing search registry reconciliation drops the locally removed presentation on its subsequent commit; external native closed-event delivery remains separate.

Controlled tests execute the real close owner/checkpoint and temporary WindowStateStore, the actual App callback expression, and the actual hook with queued React-dispatcher slots. Draft preview values and host callbacks are controlled. They do not mount App/React, execute DOM focus, IPC, sockets, native browsers or installed artifacts. Post-close keyed keyboard focus, resolved-history retirement, native closed events, hide/detach/Undo, first-Send and physical/appearance acceptance remain required follow-ups. Original `1c796124` rejection stays immutable; sparse correction `4e0a23aa` and window owner `fff869ae` have separate accepted reports. This consumer requires its own independent source review; no prerequisite or full-goal approval is inherited.

### Browser Close committed focus (2026-09-10)

The App now captures focus from the original browser pill (including its sibling Close button) or page content at the explicit Close action. A runtime-only ID travels through the existing local/native removal callback. The queued dock updater publishes accepted/rejected removal and the surviving tab's presentation identity together; the window's persisted dock snapshot contains neither this receipt nor focus state. Existing search/replacement updates retain this separate transient receipt.

After an accepted removal commits, focus moves at a guarded animation frame to the exact surviving tab button, or visible Chat when the region became empty. The original focused node may disappear with the closed tab; a disabled Close button losing focus to body while waiting is also accounted for. Focus elsewhere cancels the intent through a document focus listener. Committed route/settings/plugin/host-connectivity excursions, rejected removal, replacement incarnation, newer DOM node, hidden/inert node and disposal suppress the old frame. The listener is removed on cancellation/completion. Status inspections do not initiate focus; saved history alone does not select any current tab.

The source/control tests cover both dock regions, empty-region Chat, queued receipt/nonpersistence, original focus-ID propagation through local/native callbacks, focus and route excursions, and successor/node replacement. Document shapes and React dispatchers are controlled: no mounted App, real DOM/OS focus, native browser or Electron/IPC execution occurred. Exact pinned post-close focus choreography, closed/full-width variants, history retirement, native closed events, hide/detach/Undo, first-Send and physical appearance remain acceptance work. The preceding `7bcfd9ac` App source freeze remains unchanged and separately under review.


## Explicit confirmed Close-history retirement

The window may dismiss a locally saved, exactly confirmed completed/rejected Close record. Pending/unknown receipts remain recoverable and cannot be trimmed. A completed record is offered only when its original tab ID is absent from the current committed dock; rejected records can be dismissed with a retained tab. This is local history management, not a host-journal deletion or permission to repeat an earlier request. The host keeps durable request identities and outcomes.

The existing Close owner serializes dismissal against active Close/status and other retirement. It registers a whole-list checkpoint before publishing removal and succeeds only after the exact reduced list commits and receives a subsequent save acknowledgement. Every sibling record must survive in order; changed projections, stale acknowledgements, save failure and disposal reject retirement. Failure restores the known record in memory for a later ordinary window save, without claiming disk rollback. A saved acknowledgement updates eligibility once; late observations after disposal are inert.

App shows confirmed/rejected records beside unresolved recovery. Dismiss moves focus synchronously from its focused button to the surviving Close-history group before removing the row, then reports save progress/result in that group. It schedules no later focus. Status checks remain explicit and never repeat Close; local history retirement makes no host call. Source-tab absence is checked at selection, not an atomic lease across the later save.

Controlled evidence covers actual temporary WindowStateStore save/reopen, exact checkpoint transitions, unresolved siblings, the 100-entry cap, failure/recovery, operation serialization and disposed late observers. The intermediate-old comparison isolates the introduced notification lifetime bug; it is not an old full Close implementation. App markup/focus is source/typecheck only, not mounted React/DOM, IPC, native Close or physical keyboard proof. The separate App7bc focus rejection and pending7cb focus review remain frozen; this scope does not grant their acceptance. Native closed events, hide/detach/Undo, first-Send, installed/physical and whole-goal gates remain open under the launch hold.

### Close focus settlement when no removal is queued

The App runs browser Close inside the focus owner's token-scoped lifetime. If readiness, capability, ownership or another guard retains the tab (or the operation throws) without queuing removal, the operation's finally path releases its admission-phase listener. It cancels only the exact token that started that operation; an older completion cannot cancel a newer selection. File Close retains its existing path and browser Close still returns false to the generic ID-based remover.

After the actual App removal callback successfully enqueues the original keyed update, it marks that same token as queued. Settlement then leaves focus to the existing accepted/refused committed removal receipt and guarded frame; it does not cancel a still-pending queued update or an accepted frame. A stale token cannot mark a newer selection as queued. Route, overlay, connection, focus-excursion and disposal cancellation remain owned by the existing focus helper.

The regression selects the original rejected 7cb App's two callback expressions against the same final focus/coordinator/fixture. All four controlled offline/missing-bridge/readiness/unknown cases leave the old listener and release it under the corrected App; source-current presence and zero queued removal are fixed-side later controls. Newer-token, thrown-action and queued receipt/frame tests are separate final controls. Controlled document/event shapes and actual callback expressions do not establish mounted React, DOM or native focus timing. Original 7bc/7cb rejections remain immutable; correction review must use the original 7cb contexts. History retirement c480 remains a separate pending source scope. Native/physical/appearance/whole-goal gates and launch hold remain unchanged.


### Native lifecycle producer prerequisite (source preparation)

The external-closure path still needs a genuine native producer. The inspected OMP18.1.10 worker `closed` message acknowledges requested worker shutdown: attach mode can disconnect without destroying its adopted page. The accepted supervisor metadata method projects cached native map state; its current message consumer has no unsolicited external-page destruction path. Existing UI refresh absence handling cannot compensate for an unobserved native target disappearance.

Puppeteer25.3.0's Page.Close deferred is also resolved through target/session detachment, so its event name alone cannot authorize registry deletion. The next implementation must distinguish an original connected backend's confirmed target absence or owner release from detach, recycle, timeout and disconnect. The inspected cmux line client provides request replies, not a proven surface-close notification contract. No backend configuration or unsupported backend scope may be silently changed.

The exact source/window preparation is under `.data/browser-native-lifecycle-contract-2026-09-10/primary-001`; it selects no public API and grants no implementation/native approval. Ordered owner/incarnation delivery and resynchronization, initial-subscription races, retained cleanup, full browser-use/persistence/detached membership and identity-bound renderer consumption remain required, alongside hide/detach/Undo/first-Send. No runtime or native operation was performed under the launch hold.


### cmux observation connection prerequisite

The maintained OMP patch adds an opt-in `connectionGeneration` to cmux requests. A caller must first obtain a defined current generation; disconnected or unauthenticated state supplies none. With that generation, the queued request checks the original connected lifetime at admission, before writing and after parsing its reply. It never reconnects. Ordinary callers that omit the option retain their existing connection/reconnection behavior. Passing an undefined option is ordinary behavior, not a guarded observation.

Only successful authentication publishes a new generation. Retired socket data, errors, close events and delayed write callbacks cannot feed or retire a replacement socket. A completed older connection attempt cannot clear a newer pending handshake. Existing backend configuration, passwords/relay setup, request serialization, timeouts and response parsing remain in place. This generation is local to the client object and is not a server identity or a restart-stable surface identity.

This is an unwired native-inspection prerequisite. It neither queries a surface nor treats a transport failure as absence, emits closed events or authorizes UI removal. The separate source trace of cmux's exact-UUID surface lookup is version-specific; native/backend capability validation and original owner/target binding are still required. Puppeteer reconciliation, ordered worker/host/desktop delivery, retained cleanup, full browser registry membership, hide/detach/Undo/first-Send and native/physical acceptance remain open. Controlled tests execute the actual selected complete cmux client with an EventEmitter socket model, not Node sockets, cmux, auth, SDK imports or native browsers. Public declaration checks are separate from the ordinary repository compiler. Installed sources, dependency lock and previous native patch sections remain unchanged.

### Relay tab observation prerequisite (source, September 10)

The maintained OMP patch adds a versioned read-only relay observation path. `OMP.getObservationContext` returns a fresh connection token only after an extension hello advertises `tabInspectionVersion: 1`. `OMP.inspectTab` requires that exact token and canonical `PAGE<n>` or `TAB<n>` identity. The extension performs an unfiltered `chrome.tabs.query({})`, validates every returned tab ID, and answers whether the original numeric tab exists within that extension's browser/profile visibility. It neither attaches a debugger nor creates, activates, groups, closes, or navigates a tab. Ineligibility and debugger detachment do not establish absence.

The relay checks the original extension connection again after the reply; replacement, disconnect, or another hello invalidates the token. Old extensions fail closed. Pending hello/RPC replies in the shipped extension are sent only to their originating socket. Eight reads may be outstanding in each relay and extension; the extension retains its bound while an uncancellable Chrome query remains pending, even after a relay timeout or disconnect. Query errors, malformed identities and capacity failures are errors, not absence. A synchronous send failure also clears the relay's pending timer.

This is an additive relay prerequisite, not native closure or retained-registry integration. Consumers must capture the connection context with the original owner/target before relying on later observations, retain original backend identity, and handle unavailable results and resynchronization explicitly. The token is a relay handshake lifetime, not a persisted browser-incarnation ID. Chrome tab IDs alone cannot prove identity across browser/extension lifetimes. The existing relay's cached target list, `Target.targetDestroyed` and missing-target `Target.getTargetInfo` fallback remain unsuitable as closure authority. CDP, cmux and relay all remain required backend paths; no installed extension, socket, browser or native lifecycle has been exercised by the controlled acceptance runner.

### Cmux relay handshake ownership correction (source, September 10)

The connection-generation prerequisite's first frozen revision did not pin the configured relay handshake during asynchronous key import/signing. If socket A closed while crypto was pending, its continuation could send A's authentication on socket B and let the stale read timeout destroy B. The separate correction passes the original socket through relay challenge/auth reads and writes, revalidates it after every asynchronous authentication step, and scopes desynchronization cleanup to that socket. An already-settled line waiter's timeout callback has no effect. Password and ordinary request paths share the same socket-aware read/write guard; configured relay credentials and authentication protocol remain unchanged.

Controlled overlap regressions hold key import or signing across A→B replacement and verify no foreign write, no stale timeout destroying B, and a usable replacement handshake/read. This is local callback evidence, not real relay authentication, a browser/server incarnation proof, or permission to launch a native backend. Original deffba57 rejection remains historical; the correction receives its own delta review in the original contexts. Target inspection and all configured backend integrations remain required.


### Original root target observation prerequisite (September 10)

The maintained OMP patch adds `target-observation.ts` and its matching declaration for headless/spawned/connected CDP and extension relay reads. It captures the pinned Puppeteer 25.3.0 original `_connection`; the public Browser interface has no equivalent root accessor without attachment. The internal accessor/type is an explicit compatibility dependency, guarded before/after each read. The reader sends only root `Target.getTargets` (explicit include-all filter) or versioned relay context/inspection commands. Relay retains the original handshake; it never recaptures after loss. Malformed inventories/replies, a non-page exact CDP target, transport failure or changed connection reject rather than becoming absence. No target attach, create, close, activation, discovery mutation or cached target inventory is used.

This is an **unwired read primitive**, not a native owner observer or close event. Native admission must capture before creation/adoption and retain with the exact original tab object/owner/backend; a fresh capture cannot recover historical identity. It owns no browser lease, persisted incarnation, listener, scheduling, concurrency limit or resync policy. Query timeout uses the pinned Connection timeout path, not physical query cancellation. CDP absence is only within the responding original browser endpoint; relay absence is only extension-visible membership under the retained handshake. Cmux remains a distinct required backend: its exact UUID/socket generation and supported presence/absence contract must be consumed separately. Backend identity, owner admission, lifecycle events, retained registry removal, hide/detach/Undo, first Send and native/installed/physical acceptance remain open.


### Correlated cmux replies (September 10)

The maintained socket-client patch adds opt-in `checkedReply: true` to `request`, requiring the captured authenticated `connectionGeneration`. Checked requests snapshot JSON object params before enqueue and recheck the original connection after serialization and after read, before interpreting even an error reply. The v2 envelope must echo the generated request ID, a boolean result flag and valid object result or code/message error. A malformed/unmatched response retires only that original stream so a delayed reply cannot contaminate later ordinary work. A matching server failure becomes `CmuxResponseError` with code/data/details, preserving the existing formatted message. Ordinary requests keep their previous reconnect/id-less parsing behavior.

This is an **unwired wire-protocol prerequisite**, not a cmux presence classifier. A correlated `not_found` is not physical-close authority; the exact runtime surface selector, original backend/server contract and ownership still require a dedicated native reader. The opt-in flag is not a read-method allowlist or an authorization boundary. No query, retention, resync, native owner capture, observer, registry removal, UI/first-Send or installed/physical gate is completed by it. Native/socket tests use controlled callbacks only; configured authentication, relay and installed package remain unchanged.

### Exact cmux surface observation prerequisite (September 10)

The maintained patch adds `captureCmuxSurfaceObservation` beside the checked socket client. It captures an already authenticated connection generation before target creation/adoption; it never connects, acquires, focuses, navigates, or closes a surface. Each read sends only `surface.list` with an exact runtime UUID and a checked reply. UUID normalization, full row validation, duplicate rejection, browser type, and original-generation checks precede a frozen positive snapshot. Window Dock owner/window identity may be equal; deferred browsers require no URL getter or webview materialization. This is a current owner-scoped snapshot, not a global inventory or persistent target identity.

The pinned cmux source research distinguishes a successful list missing its routed target from authoritative deletion. Missing/malformed/incompatible results throw. Correlated server errors propagate, including `not_found`; they are not converted to closed receipts. OMP does not pin the running cmux server implementation or obtain a server-incarnation token through these replies. That authority, capture in the actual native owner before acquisition/adoption, retained context/lease, observer/resync, and host/renderer consumption remain required. The primitive is unwired; all configured backends still need real lifecycle acceptance. Controlled selected reader/client tests are not cmux/socket/native execution.

### Original native owner observation capture (September 10)

The maintained supervisor captures observation before worker target selection or cmux creation/adoption, retaining it on the exact published tab object. Reuse and worker recycling keep that original context. Capture failure is retained and reported by inspection; it does not silently recapture history or change existing acquisition behavior. This is capture ownership, not a new creation-admission or server-incarnation guarantee.

`inspectTabForOwner` checks creator/name/target/object/backend and permanent retirement, reserves at most eight reads with one per tab, holds the browser through the read, then rechecks ownership after read and lease release. Read and cleanup errors are both retained. Puppeteer/relay positive or negative observations use their captured reader; cmux returns positive snapshots only and preserves server errors. It performs no acquire, close, focus, attach, or registry deletion. The ref hold postpones backend disposal but does not add a collective inspection drain to tab/session shutdown. Host/worker query transport, observer/resync, cmux original-server authority, registry retirement, all configured backend/native/physical acceptance remain required.

### Retained native inspection drain (September 10)

Native tab cleanup now waits independently for its close body and an already admitted owner inspection, including the inspection's browser-hold release. A close error cannot skip that drain. Expected lookup rejection because the owner retired is distinct from backend/hold/release failure; operational failures remain visible to both the query and cleanup. New inspection admission is barred by the existing permanent cleanup marker.

Active cleanup entries remain discoverable after the visible tab map removes the original. Owner/all/headless shutdown snapshots exact tab objects from both sets and settles every selected cleanup, retaining all failures. Same-name replacements are not substituted into that snapshot. The snapshot does not close admission to future tab acquisitions; the owning worker/session must still establish that boundary. Native host query/observer transport, server authority, event/resync/registry policy and real backend/physical acceptance remain required. These are controlled source tests, not SDK or native shutdown proof.

### Observation identity through final lease settlement (September 10)

The capture retains its original native object/kind assertion alongside the read operation. The published tab retains that same assertion with its original fields. After the browser-hold release settles, owner inspection checks both the current original tab and the capture-time native identity before returning an earlier result. It never recaptures or binds to a replacement handle. This closes the specific post-read lease-release gap found in fd3 source review; the historical rejection remains unchanged and the correction requires review in the original contexts.

Controlled cases mutate the native handle or kind during held release for all five configured kinds, preserving the tab/wrapper; unchanged owners and root absence remain positive controls. They do not establish real registry mutation delivery, native incarnation, physical absence/closure or installed execution. The separately submitted drain delta remains its own scope. Backend/server authority, host/worker observer transport, resync/retained membership and the full physical/first-Send milestone remain required.

### Worker-owned observation transport (September 10)

The existing session and draft workers share `inspectBrowserTab` under worker protocol41. The child retains an exact current owner through native module loading and the original-owner read, validates the projection, and rejects unsupported/malformed/stale results. The daemon checks the copied PID/owner/name/target response. Root/relay presence or absence remains scoped to its captured native owner; cmux absence remains unsupported. No acquisition, close, retry or renderer deletion follows from this operation.

Read admission is bounded through completion, including loading and shutdown. Close and observation drains begin independently before native owners are disposed; operational read errors remain visible during cleanup. The native supervisor identity and drain changes retain their own review verdicts. Host authenticated observation endpoints, subscriptions/resync, backend incarnation, registry/closed-state policy and real native/physical acceptance remain required.


### Host original-target observation endpoints (September 10)

The host now routes session `GET /v1/sessions/:id/browser-target-observation` and draft `POST /v1/draft-browser-owners/:id/target-observation` after the existing authentication/device-access gate. Both require the original host header and copied worker/name/target; the draft body also carries its exact draft/revision. They only look up existing workers. Draft forwarding uses the existing guarded registry handle and never starts a worker, probes metadata, creates a target, writes a journal, or interprets an unavailable owner as absence.

One host route manager bounds eight distinct pending operations, including worker lookup and final identity lookup, coalesces exact simultaneous inputs, and keeps no completed cache. It validates the worker result through the shared worker projection, checks original handle/owner/PID before dispatch and after the read/final lookup, and returns only the versioned owner/target/kind/presence envelope. Cmux remains positive-only. These sampled checks do not add a host connection-generation subscription or an atomic lease; a future observer must provide its own admission, loss, resync and retirement policy. Client disconnect does not pretend to cancel already dispatched native work or free its slot early.

Normal shutdown starts observation retirement before worker shutdown and retains all active lookups/reads through settlement before Store close. Dispatched read/parse failures remain visible to the caller and retained drain; pre-dispatch lookup failure is not native cleanup failure. Startup rollback starts worker retirement alongside the observation drain so a session read does not prevent its own worker from stopping; the existing rollback path still prioritizes the startup error. Request bodies reuse the existing 32 KiB/five-second reader. This is source wiring and controlled Request/Response/registry evidence, not authenticated live socket, child IPC, native backend, physical-close or complete lifecycle proof. Worker52232 keeps its own pending source review. Capture-correction0d777 is separately PASS/PASS for the fd3 identity gap, with an explicit nonblocking stale private acquisition type annotation; this host delta does not fix or typecheck that native annotation. Drain eef0 is separately accepted. Desktop transport/observer/resync/registry removal, backend incarnation and all native/physical/first-Send gates remain required.


### Acquisition observation record annotation (September 10)

The private worker acquisition local now uses `CapturedTabObservation`, matching the record returned by capture and accepted by publication. The former callable annotation survived the fd3 runtime correction and is retained as the severity3 source finding in its historical review. This is a separate type-only correction: capture, acquired tab identity, final lease-settlement assertion, drain and public declarations are unchanged. Selected compiler evidence must distinguish these local assignment/argument diagnostics from wider private/installed dependency errors; prior transpile-only controlled passes are not native type correctness or execution proof.


### Desktop original-target observation bridge (September 10)

`browserObservation.inspect` exposes one explicitly selected session or draft owner, worker PID and original name/target through preload and a registered main-process callback. Owner parsing reuses the existing strict session/draft identity contract; shared read types do not grant close authority. Preload retains separate copied input and validates the returned envelope. Main parses before endpoint lookup, then checks sender trust and exact resolved host immediately before fetch; it checks trust again before returning the result. This is checked sender/frame/origin admission, not a new document-generation loss subscription or cancellation-through-dispatch guarantee.

The transport captures the endpoint, owner and target, sends only the existing lookup-only observation route, rejects foreign headers before consuming their body, and bounds success/error JSON to32/16KiB. It uses a20-second fetch timeout, no retries, fallback, completed cache, metadata/frame probing or acquisition. HTTP/old-host/parse/timeout failures stay unavailable errors, never synthetic absence. The strict result keeps original host/session-or-draft/creator/PID/name/target and supported kind/presence; cmux still cannot produce authoritative absence. Fetch cancellation does not imply cancellation of host/native work, whose host/worker bounds remain separately owned.

Controlled tests exercise real adapters/registered callbacks and both actual host routes with controlled handles, not Electron IPC, live authentication, sockets or native backends. A bridge property means desktop support only; renderer observer/retained membership/resync, original backend incarnation, native closed events, hide/detach/Undo, first Send and physical/full-GOAL acceptance remain required. Worker52232 is separately PASS/PASS with its fixture-naming P3; hosta3ef is separately Standards PASS / Spec REJECT for skipped malformed-payload accounting during retirement; this desktop delta retains that rejected host context unchanged. Annotation41718766 has its own pending original-context assessment. Original fd3 and all historical freezes remain unchanged; no prerequisite approval is inferred through this desktop delta.

### Host observation parse errors during retirement (September 10)

Dispatched observation payloads are now validated before the post-read current-owner/retirement check. A malformed completed payload remains an operational parse failure even after retirement starts, while successful late payloads still fail the publication gate. The retained drain waits for every admitted read and reports parse errors after the others settle. No lookup, parser contract, owner identity, response fields, server ordering or native dispatch changes. Controlled session/draft regressions hold a second read while the first returns malformed data; valid late-reply controls remain clean drains with suppressed publication. This separate correction is submitted to the original HostObservation review contexts; original a3ef PASS/REJECT and the desktop a7c50 freeze retain their exact historical bodies and reports. No native, IPC, physical-close, renderer or whole-GOAL approval is inferred.

### Search refresh uses original-target presence (September 10)

The existing command-search refresh now reads the exact session/draft owner, worker PID and native target through `browserObservation.inspect`, sharing the existing four uncancellable search-read slots with metadata. Capture precedes admission; offline, cancelled, missing-capability and failed reads retain historical text. The three-second open-search cadence is unchanged. This is an authority correction to that existing refresh, not a native event producer or a substitute for subscribe/resync, full retained membership, or all-backend lifecycle work.

Cached native maps no longer erase search history merely because a target is missing/dead or a worker has changed. Only a validated exact original-target absence can retire the current captured search record. Cmux remains positive-only; its errors/missing surfaces never imply absence. Metadata can still supply observed text, but has a separate ordering from target inspection: newer preview metadata cannot cancel an in-flight authoritative inspection or resurrect a confirmed-absent record. Runtime presentation/owner changes and committed connection/bridge/open-state invalidation reject stale deliveries. A tombstone belongs to that exact current record; it is not persisted native incarnation authority or permission to remove dock tabs, page intents, unknown requests, owners or host journals.

Both session and draft paths preserve source data, original activation guards and shared preview forwarding. Session persistence omits a confirmed-absent search observation while leaving the dock descriptor intact; draft saved target history remains unchanged. New presentation/adoption, first Send, durable closed-state history, native external event delivery and physical acceptance retain their separate required contracts. No browser, worker, socket, Electron or native lifecycle is exercised by controlled registry/helper/hook fixtures. Desktop a7c50 is separately Standards PASS / Spec PASS (b867b3de), original host a3ef remains historically rejected with separate6a64 drain correction pending, and native annotation417 is separately accepted without a full native typecheck claim. These statuses do not substitute corrected bytes into earlier freezes.

### Observation settlement retains the native connection (September 11)

Root and cmux readers expose a synchronous `assertCurrent` check over their original captured connection. The supervisor retains that assertion with its existing native-object/kind and tab-owner checks, and invokes it again after awaited browser release before returning presence or absence. A shared Browser/client object is no longer sufficient when its root connection or authenticated cmux generation changes during release. Failed initial capture remains unavailable on inspection without recapture; ordinary acquisition semantics and mutation calls are unchanged.

This connection check neither sends an extra query nor reconnects. It is a root-connection/socket-generation fence, not a lease, original-server creation/adoption barrier, cmux server incarnation, or relay-extension token notification. Relay responses still validate their captured extension token during the read; an extension replacement after that read while the CDP root stays unchanged requires the separate lifecycle producer. Creation/adoption and failure cleanup must eventually retain the original backend through actual mutation dispatch, including worker endpoint selection and cmux queued generation checks; these are still required and are not solved by surrounding assertions.

Controlled selected reader/supervisor fixtures test release-gap connection loss with the native wrapper and target unchanged. They do not run a browser, SDK, native backend, worker, socket or installed application. Host parse-drain correction6a64 is now separately PASS/PASS in its original contexts; the desktop and search freezes retain their historical host bytes/status wording. Search authority430aa has its own pending review. Native event delivery, retained membership, first Send, all-backend and physical acceptance remain open under the launch hold.

### Preview titles do not invalidate original-target inspections (September 11)

The command-search hook now keys draft refresh lifetime by the existing original `sourceKey`, matching its session presentation keys. A committed preview/local title or observed URL change on that same source updates the registry and displayed text without aborting a held authoritative inspection. Presentation, owner/request/native target changes still change source identity; committed connection, bridge and open-state changes still invalidate delivery. No new refresh loop, timer, native producer or authority is added.

The correction's actual-hook controlled regression captures a preview callback after inspection starts, publishes newer preview metadata, commits a title-only dock rerender, then resolves the original absence. The row retires without deleting its dock/page intent, and late preview metadata cannot restore it. Separate source/connection/bridge/open loss-return cases retain old-delivery suppression and fresh inspection. These are controlled hook phases, not mounted React/DOM/native evidence. Original430aa remains Standards PASS / Spec REJECT for its frozen bytes; this named SPEC3 P1 correction returns only to its original SearchAuthority contexts. Native connection settlementec1b is independently submitted, and all native event/retained-membership/first-Send/physical/full-GOAL gates and launch hold remain.


### cmux requests retain the acquisition connection

The maintained native patch now captures the concrete cmux client and its current authenticated connection generation before the per-name acquisition queue, split creation or surface adoption. Reused cmux tabs must still match their retained observation connection; final acquisition settlement rechecks after releasing its hold. The supervisor supplies that retained request port to CmuxTab; creation, waits, navigation, subsequent tab requests, acquisition cleanup and owned surface close therefore use the original generation at the existing socket queue. Readiness publication also checks the original connection after optional metadata reads. Closing a surface uses the retained port independently of an aborted tool-run signal. Missing/lost connections fail without reconnecting a queued mutation onto a replacement backend.

This is source implementation pending independent review in `.data/browser-cmux-owner-dispatch-2026-09-11/`. Controlled queue evidence and strict/public type qualifications belong in its immutable handoff. It is not server-incarnation/physical-absence authority, all-backend creation admission, or actual native/SDK execution. Registry refcount disposal still has its existing mutable-handle boundary; full cached metadata freshness and lost-transport cleanup are separate required work. Direct external CmuxTab callers supplying a raw client retain their previous behavior. The launch hold remains.


### Registry cleanup retains its original resource

The maintained registry patch now snapshots cleanup resources before publishing an opened browser handle. Later release uses that original browser/client, kind, key, PID, profile and shared-daemon ownership, rather than mutable public routing fields. Caller-owned kind values are copied before awaited opening. The zero-reference transition records a disposal before invoking callbacks; later releases join that result, including failure, and further holds are rejected. A different handle under the same key remains independent. Existing local/shared headless, connected, relay, spawned and cmux teardown bodies keep their prior policies.

This source delta is pending independent review in `.data/browser-registry-resource-ownership-2026-09-11/`. It binds resource objects, not server incarnations or cmux socket generations. External structural handles bind at their first observed hold/release; acquireBrowser still returns an unheld handle. Native cleanup, worker/server admission, full event membership, physical and installed acceptance remain unproved under the launch hold.


### Original-connection CDP worker transport prerequisite

The maintained native patch adds an unwired transport that allocates an owned browser-target session on the captured Puppeteer root. It routes root and flattened descendant commands/events through that original connection, bounds pending requests and buffered messages, rejects foreign envelope session IDs, and exposes retained asynchronous disposal. Closing this transport detaches its own sessions and drains dispatched work; it does not disconnect the parent browser. Protocol errors retain their available code/data/message. This is transport ownership, not a method authorization policy.

The source scope is pending independent review in `.data/browser-owned-cdp-transport-2026-09-11/`. Experimental native browser-target attachment, complete native protocol behavior and actual worker IPC are unproved under the launch hold. Relay still requires an original-extension-bound logical-session adapter; no configured backend or production caller has been switched to this prerequisite. Initial creation/adoption, fallback/recycle, original target cleanup, events/resync/membership, first Send and native/installed/physical acceptance remain required.

### Aborted registry opens retain their acquired kind

The registry's post-open abort path now derives its existing spawned-process kill choice from the opened handle's copied kind. Mutating the caller's kind while Puppeteer connection is pending cannot turn original spawned cleanup into disconnect-only cleanup. Existing abort reporting, cleanup error handling and all other backend policies remain unchanged. The named correction returns to the original registry177f review contexts; the original rejection remains historical. Controlled held-connect tests are not native process cleanup proof. The owned CDP transport remains separately unwired and under review, with all native and physical gates held.

### Relay extension ownership through pending operations

The relay bridge now captures the original extension socket and handshake epoch for each RPC, checks them at dispatch and successful settlement, and retires pending requests on a new hello even when the socket object is reused. Replacement publishes the new socket before closing the old one, so reentrant close callbacks cannot restore stale ownership. The shared Runtime disable/enable cycle retains one owner across both sends; attachment retains one owner while waiting for pending detach or attach and before publishing its result.

This prerequisite does not add an owned browser-target session or switch a worker. The remaining relay logical-connection implementation must preserve original admission through queued commands, group/ungroup work, descendant aliases, event delivery, claims and retained detach-only cleanup. Ordinary downstream connections still have their legacy lifetimes; RPC settlement does not establish a native server incarnation, physical absence or caller ownership through every subsequent continuation. Controlled bridge cases and separate existing extension cases are not Chrome, websocket, IPC, native or physical acceptance. Launch hold remains.

### Relay grouping keeps its queued extension owner

Queued grouping now retains its original extension sender and exact tab object. Before sending and publishing, it checks current handshake ownership, the tab's current map entry, its grouping attempt and eligibility. Loss of eligibility retires an attempt even when a later update restores eligibility. A new socket or hello clears prior grouping acknowledgements and queued work; fresh eligible claims can schedule new work. Old completion only clears its own attempt, so it cannot unlock a newer group request. Group RPCs remain serialized and eligible tabs queued together remain batched.

This is shared grouping source work, pending its separate review. It does not create relay worker channels, bind all downstream callers, roll back already dispatched Chrome mutations, or prove native group creation/cleanup. Ungroup dispatch uses the existing synchronous original-extension RPC capture; logical-channel claims, retained cleanup, descendant aliases and worker admission remain required. The earlier extension-owner and other freezes keep their separate review status. No runtime or installed acceptance is inferred.

### Relay descendant sessions belong to each downstream parent

The relay now exposes an alias for each native descendant under each downstream parent session. Commands resolve only aliases owned by that connection; raw native session IDs and sibling aliases are not routing authority. Nested attachment events register the alias before delivery, and explicit attachment results use that same announced alias and target. Observed descendant topology is replayed when another page session attaches, in parent-before-child order. Replies retain the caller's envelope ID; original-source loss during a pending request prevents successful late delivery.

Local alias detach removes that client's descendant tree without detaching another client's shared native child. Page release and native detach remove the relevant aliases, and extension replacement/hello synchronously retires every old descendant mapping before later notifications. The relay uses flattened CDP sessions, matching the pinned Puppeteer client; deprecated non-flat Target tunneling is refused rather than forwarding embedded session IDs without ownership checks.

This source delta is pending independent review. It implements descendant routing in the current shared relay; logical browser-root channels, channel admission/lifetime, retained native detach and worker caller wiring remain required. Observed topology and aliases are not native server incarnations or physical closure authority. Existing prerequisite verdicts remain separate. Controlled fixtures and narrow compilation are not SDK/Chrome/socket/IPC/native or installed acceptance. Launch hold remains.

### Disconnect-invalidated attachment does not ban the next owner

The relay attach rejection path now revalidates its retained extension owner after logging and immediately before changing the tab's banned state. An old disconnect rejection cannot ban the same-URL tab in a later or already reconnected epoch; a genuine operational attach failure from the unchanged owner still bans as before. This named correction returns only to the original f6f extension-owner reviewers. Original rejected bytes and separate grouping/descendant freezes remain immutable. Controlled held-attach/handshake tests are not Chrome reconnect, worker or native acceptance; the launch hold remains.

### Relay browser roots retain their original extension

`Target.attachToBrowserTarget` now allocates a logical browser root within the shared relay. Each root owns its own discovery/autoattach state, claims, page sessions and descendant aliases. Browser commands use that root's flattened session envelope; page and descendant replies retain their existing aliases. Allocating a root does not emit an unsolicited attachment event to the parent registry's TargetManager. Nested roots remain part of the original physical connection.

A logical root captures the current extension handshake at allocation. Replacement, disconnect or a new hello permanently retires it; a later worker must deliberately allocate a new root. Commands and event delivery check that original owner. Closing a root retires its local admission and membership synchronously, then joins dispatched commands, automatic attachment work and in-flight grouping before original-owner cleanup. Other roots and registry sessions keep their own claims and debugger holds. Root disposal detaches its unheld debugger resources and grouping claims; it does not close the user's browser or remove native tabs. Pending cleanup failures are reported on the retained root close result. The bridge bounds live logical roots and pending commands; a bounded recent close-receipt set supports repeated disposal without rerunning cleanup.

This is a shared-relay source implementation pending its own independent review. No worker, backend selector or installed caller has been switched. Controlled public-wire fixtures exercise injected extension/socket/event/timer boundaries, not Puppeteer, Chrome, websocket or native execution. Protocol-wide compatibility, original native server/target incarnation, mutation admission through the actual worker, physical cleanup/event authority, first Send and installed/physical/whole-GOAL acceptance remain required under the launch hold. Earlier extension, grouping, descendant and named-correction freezes retain their exact bytes and separate verdicts.

### Page release retires aliases before callbacks

The relay now removes the original page session and all of its local descendant aliases before emitting any child detach notification. A reentrant native attachment announcement cannot mint a new alias under that retiring page, and a callback cannot dispatch through another original child that has not yet been notified. Other connections and deliberately admitted new page sessions remain independent. This named correction returns only to the original f65 descendant reviewers; original f65 and newer logical-channel freezes remain immutable. Controlled callback evidence is not native detach, Chrome scheduling, worker or physical acceptance; the launch hold remains.

### Worker CDP message channel prerequisite (2026-09-11)

The maintained native patch now contains `worker-cdp-channel.ts`: a parent endpoint that retains one original owned transport and a worker endpoint implementing Puppeteer's `ConnectionTransport`. The existing worker dispatcher must feed `receive()`; this module deliberately does not register another worker-inbox subscriber. Each attempt requires a fresh private channel identity and its own underlying owned transport. Credit acknowledgements bound in-flight data to 256 frames / 16 MiB in each direction; the worker's pre-connect buffer has the same independent bound. Protocol/capacity failures retire the attempt. Cleanup messages remain deliverable after data retirement.

Parent disposal invalidates admission before callbacks, joins the original transport's disposal, and sends an explicit drain result. Worker `close()` only initiates shutdown; retained `dispose()` awaits that result and preserves local/remote errors. A definitively lost port must call `abort(error)`, which reports failure instead of inventing successful cleanup. This is a private ordered-port protocol, not an authentication boundary. The owner must store the parent endpoint before `start()` because the upstream setter can synchronously replay messages.

This remains an **unwired prerequisite**. Real WorkerCore/supervisor consumption, initial/fallback/recycle owner capture, remaining-deadline accounting, exact orphan-target cleanup, inline termination and all configured backend integration are required next. Controlled message callbacks and the copied public `ConnectionTransport` declaration are not Worker/IPC/SDK/native or physical acceptance. Logical relay channel and page-release corrections keep their separate review status; no earlier freeze is replaced.

### Retiring relay roots keep capacity until settlement

The logical-root limit now counts every original physical-owner root until its disposal completes, including nested roots waiting for dispatched operations or native cleanup. Starting disposal no longer frees admission capacity. Successful or failed settled disposal releases that slot; completed close receipts retain their separate128-entry bound. This named49a correction preserves the original rejected freeze and returns only to its original logical-channel reviewers. Controlled source evidence is not native/worker/physical acceptance; launch hold remains.


### Opt-in WorkerCore owned channel and retained retirement (2026-09-11)

The private WorkerCore now accepts exactly one connection input: the existing explicit endpoint or a fresh `cdpChannel`. The owned path constructs `WorkerCdpChannel` before the loader await, uses the existing single inbox dispatcher, and calls Puppeteer `connect({transport})` without endpoint fallback. The parent must deliver init before starting its channel. The supervisor and worker-entry lifecycle are not switched by this delta; their original-owner allocation, worker/inline fallback, recycle, orphan cleanup, deadline and error accounting remain required.

`retire()` preserves the page for an eventual replacement facade; normal `close()` closes a headless page or its reported original target. Both retain one drain result, suppress later work/publication, join dispatched initialization/run cleanup, detach the facade, then await the original parent channel drain before reporting `closed`. A sent creation can still report its target after retirement for parent orphan cleanup. Definite port loss uses `abort(error)` and cannot fabricate a successful drain. Channel retirement notifies WorkerCore before Puppeteer's close callback, including malformed/capacity/send failures. Cleanup failures remain in the retained result and structured close receipt.

Evidence uses complete selected WorkerCore/channel bodies with injected loader/browser/page/runtime operations and controlled messages; no Worker, SDK, browser, socket or native process is launched. Legacy endpoint compatibility remains intentional until the separate parent/entry integration. This does not establish actual attach-to-browser-target support, relay/native behavior, original-backend admission, full protocol normalization, worker IPC, real deadlines, or full GOAL parity.


### Worker channel close-callback settlement correction (2026-09-11)

A genuine drain receipt or explicit failed-port abort still authorizes terminal settlement. The worker channel now publishes that outcome in a microtask after the active synchronous close callback/control post returns, so a callback that reentrantly supplies a valid drain and then throws cannot leave a successful retained disposal. Both local-close and incoming-close paths retain callback failure; clean nested drain, ordered delivery and the genuine-drain versus explicit-abort distinction are preserved. This is the named eecc close-first reentrant receive correction, not supervisor/entry or native lifecycle acceptance.


### Worker attempt owner integration in progress (2026-09-11)

The maintained patch now stages supervisor/entry integration with a retained per-attempt host. Acquisition captures the original browser object, native connection and kind before the name queue; worker initialization and recycle use fresh owned channels rather than reconnecting by endpoint. Fallback waits for attempted cleanup and keeps the original deadline. The host retains its message inbox and page-created receipt independently of setup/run subscribers, joins channel cleanup, and records failed physical termination without treating a void return as an exit acknowledgement. Inline retirement preserves message order and cannot interrupt a synchronous infinite loop.

The source now rejects publication after owner loss during hold release, cleans only the newly created original, and refuses fallback when reported orphan cleanup is unconfirmed. Controlled tests cover host callbacks, drain errors, original keyed publication and startup sequencing. They do not execute WorkerCore together with the real worker host, the entry module, IPC or any native browser. This integration remains in progress and is not an acceptance handoff: dispatched creation whose reply is lost during forced retirement, original-connection binding inside the existing orphan helper, complete recycle/entry coverage, and all configured backend behavior still require completion. Full native compilation remains failing; ordinary repository typecheck is separate. The launch hold remains unchanged.


### Worker initialization error during cleanup retirement (2026-09-11)

If initialization fails while live and retirement begins during its awaited page cleanup, the core now retains the original initialization error in the retirement result instead of suppressing its report and losing it. Page-cleanup errors remain independent; normal nonretired initialization still emits one failure report. The named258f correction returns only to the original consumer reviewers. Controlled owned-channel and explicit-endpoint fixtures do not establish a real endpoint/worker/native run, supervisor integration or whole-GOAL acceptance; original freezes stay immutable.


### Parent startup receipts and remaining deadline (2026-09-11, work in progress)

Startup now records each dispatched `Target.createTarget` outcome in the original parent transport before worker delivery. Retiring the worker wire waits for those native replies before detaching the owned session. Known target receipts survive lost worker messages and disposal errors; pending or unknown outcomes prevent automatic fallback. Failed unpublished attempts drain their known targets through the captured original connection. Successful initial publication and recycle stop recording subsequent ordinary creations, while preserving the bounded startup receipts. No host journal or durable restart recovery is claimed by this parent-memory record.

Setup and ready phase floors are clipped to the original remaining budget, including subscription replay and final hold-release time. A late ready result cannot publish. These checks use controlled clocks, sessions and worker callbacks; they do not prove wall-clock cancellation, physical worker termination or native backend behavior. The integration remains unreviewed work in progress. Actual Core/host/entry interaction, retained original backend on every configured backend, complete recycle coverage, process-loss recovery, native compilation and physical acceptance still require completion. Prior frozen source and correction reviews remain separate; the launch hold is unchanged.


### Controlled Core, host and entry integration (2026-09-11)

The combined controlled fixture now uses the complete selected corrected WorkerCore, actual WorkerAttempt and inline factory, both message channels, the parent-owned native transport, and the supervisor initialization function together. The browser loader/facade, native sessions, physical Worker port and clock remain injected boundaries. It covers headless/attach startup and close, constructor-only inline fallback, retirement during loader or creation, failed physical-port delivery, retained known/unknown creation outcomes and ordinary initialization failure. This does not establish actual process termination or native backend compatibility.

Two integration fixes are included: a closed worker receipt now rejects the pending supervisor initialization phase, and entry construction that synchronously closes its local transport no longer reinstalls parent-loss listeners. Already-queued message errors after local close are ignored. Separate complete-entry controls exercise buffered/direct inbox delivery and lost-parent cleanup. Recycle controls verify the actual caller's original target/deadline, loss before allocation and stale replacement cleanup, while using an injected startup method. They are not complete native recycle proof.

This source integration is being prepared for one separate review; its prerequisites retain their own frozen verdicts. No installed package, worker, SDK, browser, socket or native process was executed. Ordinary repository compilation and controlled fixture execution remain separate from the failing full private native type graph. All configured backend behavior, native/physical acceptance, first-Send and the full milestone gates remain required under the unchanged launch hold.


### Browser callback DOM types (2026-09-11)

Worker browser callbacks now use the standard browser DOM types instead of importing the server-side HTML parser's `HTMLElement` and merging it into global `Element`. The actionability callback retains its operations and accepts native HTML and SVG handles. The existing pinned `Frame.mainRealm(): Realm` augmentation is unchanged. This is a type-only change: complete before/after worker transpilation produces byte-identical JavaScript without evaluating it.

The exact extracted callback, result type and Frame augmentation compile against pinned Puppeteer25.3.0 public types with `strict` and `skipLibCheck: false`; the predecessor fails the same check on DOM conflicts. This is callback/public-contract evidence, not a full worker or SDK compile. The existing full private native graph remains failing and includes a separate installed fallback copy with the old ambient declarations. Its Bun/Node transfer-list mismatch, unresolved source assets/dependency types and other diagnostics remain open. The separate worker-attempt integration freeze is under its own review. No installed bytes, runtime behavior, launch hold or physical acceptance gate is changed by this correction.


### Bun worker transfer boundary (2026-09-11)

The child entry now captures Bun's global `postMessage` function once for outbound messages while retaining the original Node-compatible parent port for inbox and lifecycle handling. This keeps the shared `Bun.Transferable` contract, including ArrayBuffer and MessagePort, without casting a DOM port to Node's larger port interface or narrowing accepted transfers. Pinned Bun1.3.14 source implements the parent-port wrapper through the same worker-send primitive; this is source evidence, not execution of Bun's worker runtime.

Strict checking of the actual child/parent send expressions removes the child transfer-list diagnostic; four external Bun declaration errors remain unchanged. Controlled complete-entry fixtures cover original buffer/port references, captured send after a global replacement, existing inbox ownership, close/error cleanup and constructor-time close. They inject the Core, port and global send function; no native port transfer or worker process runs. Full native dependency compilation remains failing, and ordinary repository typecheck is separate.

Worker-attempt39ac now has separate Standards PASS / Spec PASS source reports. Its physical/native limitations remain; the DOM type correction and this transfer boundary have their own review scopes. No installed dependency, configuration, launch hold or whole-GOAL gate is changed.

### Computer worker transfer boundary (source check)

The computer worker entry now captures the Bun global send function after its existing parent/start guards and uses that captured function for the unchanged `Bun.Transferable[]` transport. The parent-port inbox, direct listener, close path and one-time selector startup remain unchanged. The strict transfer-only fixture exercises the actual entry send, protocol interface and parent send method against Bun1.3.14/Node declarations; the entry mismatch is removed while four existing Bun declaration errors remain. Complete-entry controlled startup/inbox/send/close checks preserve the original behavior; they do not execute a Worker, native desktop session, MessagePort transfer or IPC. The prior composed native graph remains a separate failing diagnostic baseline, not a native/SDK pass or acceptance through this source delta.


### Native fresh-buffer type preservation (source only)

Five annotations now preserve ordinary ArrayBuffer backing through existing allocations: collab envelope packing and its private queue, the sealed-share upload parameter, and object-storage concatenation. Broad input arrays and unpacked views remain unchanged. No allocation, conversion, byte range, request, queue or cleanup behavior changes; the four complete emitted JavaScript modules are identical. This type maintenance does not enable the excluded public sharing features.

Strict checks of the exact allocating helper bodies accept shared-backed input, require ordinary backing on copied output, and reject claiming ordinary backing for an arbitrary unpacked view. The private composed graph loses exactly four matching diagnostics but still fails with 20 others. Its separate Bun/catalog declaration candidates and exact optional fastembed type context remain unregistered and unreviewed; no whole-SDK/native pass follows from this source delta. Installed code, providers, workers, sockets, native processes and retained instances were not run or modified. The separate source review is pending, and the launch hold and full milestone requirements remain unchanged.


### Private Console, message-event and ACP declaration preparation (2026-09-11)

Private declaration candidates restore Node Console methods, preserve typed MessageEvent instances with and without DOM, and allow ACP's existing byte-stream adapter to accept Node's actual web-readable stream contract. The ACP implementation and its complete emitted JavaScript remain unchanged. These candidates are unregistered and have not received independent source review; installed dependencies are untouched.

Strict consumer checks retain invalid-input controls. The composed diagnostic sequence is 20 to 17 to 16: only the three Console/message-event errors and the ACP stream error disappear, with all remaining ordered diagnostic blocks identical. The full private SDK graph still fails on thirteen shared-buffer boundaries, one text-asset declaration conflict and two Transformers overrides. A passing narrow consumer or emission comparison is not native runtime, installed-package or whole-graph acceptance. The separate fresh-buffer source review remains requested, with no accepted verdict inferred here. Launch, backend, native identity, physical and full milestone gates remain unchanged.


### Native text-asset declaration preparation and review receipt (2026-09-11)

Fresh-buffer c4186d12 now has its own Standards PASS / Spec PASS source reports. That verdict covers the four native files and five annotations in its immutable freeze; it does not approve the later private dependency candidates or change its original 24-to-20 evidence.

A separate private candidate removes OMP's duplicate text-file module declaration and retains Bun's existing string export. Strict consumer checks preserve default text imports and the other declared asset patterns, while keeping HTMLBundle distinct. The private composed check now fails with fifteen diagnostics: thirteen shared-buffer boundaries and two Transformers processor type conflicts. Only the duplicate-export diagnostic disappeared; all remaining raw output, including the resolved-file list, is identical.

The Transformers conflicts reflect actual specialized input/output shapes that differ from their concrete bases. They require truthful public type modeling, including factories and callable instances, rather than annotations claiming nonexistent output fields. The shared-buffer boundary audit also distinguishes view conversion from ownership snapshots before hashing or asynchronous upload. Neither investigation is a production correction or independent acceptance. Runtime, installed, native, physical and full milestone gates remain open under the unchanged launch hold.


### Private upload/store byte ownership and rejected type models (2026-09-11)

Two private source candidates now establish input/supplier alias isolation at three provider upload boundaries and the blob registry. Gemini snapshots its admitted bytes before upload initialization, and Anthropic/OpenAI copy exact views at their existing synchronous File/Blob construction. The registry copies eager bytes before hashing or waiting for persistent existence, and copies lazy results before shared caching. Public byte inputs stay broad; these changes add real copies and do not promise atomic snapshots of concurrent shared-memory writers. Registry callers can still mutate publicly exposed retained objects, and arbitrary upstream keys are not redefined or validated here.

Controlled provider checks change from 8 passes/6 failures to 14/0, and the actual registry class under in-memory persistence/HTTP doubles changes from 5/9 to 14/0. Strict actual-provider source/consumer checks remove three errors; the narrower registry context removes its one Response error. These are separate scopes with their own selected inputs, not live provider, filesystem durability, SDK, native or independent acceptance.

The private composed graph selects those four runtime candidates over the maintained 78-record patch composition. It moves from 15 errors to 11 on the same 8,914 input keys, with only those four source hashes changing. All 3,216 resolved files were fenced; all remaining raw compiler output including the resolved list is identical. The nine remaining HTTP/upload byte boundaries still require consistent signing, chunking, fallback and content ownership. The maintained patch and installed packages have not acquired these private candidates.

Both Transformers declaration experiments are rejected for promotion. The first breaks valid subclass method overrides; the second preserves those methods but has a factory conflict and lets runtime-true instanceof checks restore false concrete-base input/output promises. Neither is selected in the composed graph. Their failing checks remain evidence for designing a coherent public type model, not reasons to remove assertions or suppress the two original errors.

Private Bun/catalog/ACP/asset candidates remain unregistered and unreviewed. The separate c418 source PASS/PASS does not approve them or these byte changes. Full native compilation still fails, and all launch, backend/protocol, identity, installed, physical, first-Send and whole-milestone gates remain open. No app, provider, worker, SDK or native runtime was launched.

### Private remaining upload boundaries (2026-09-11)

Five further private modules now copy admitted byte views at the remaining HTTP/upload boundaries. S3, Azure, native B2, Dropbox, OneDrive, Google Drive and ownCloud retain an owned request before signing, setup, chunking or link awaits. Callback Response and multipart File construction receive ordinary buffers; fallback shares one lazily requested owned snapshot across destinations in the same attempt. Public byte inputs remain broad. These changes add allocations and isolate subsequent caller mutation; they do not promise atomic snapshots under concurrent shared-memory writes or protection against a destination mutating its internal copy.

Separate controlled whole-module fixtures pass 13 service/helper, 23 object-storage and 30 drive cases. The identical final fixtures against their old sources fail 5, 15 and 12 cases respectively. OneDrive retains actual 64 MiB chunking, including the second-chunk failure/cancel path. Injected fetch, closed dependency maps, signing models and in-memory constructor/HTTP boundaries remain explicit limitations; these are not provider, SDK, native or independent acceptance. Earlier isolated service context failures and the drive's unchanged-helper diagnostic remain recorded separately.

Selecting these five files into the private composed graph reduces 11 diagnostics to 2. Both runs have the same 8,937 input keys, only the five package source hashes differ, and all 3,217 resolved files are pre-fenced. Every remaining raw compiler byte, including the resolved list, is identical without normalization. The two remaining Transformers override conflicts still fail compilation; the rejected public-type experiments remain unselected.

The maintained patch stays at 78 records and installed files remain unchanged. The nine private runtime candidates across this and the preceding upload/store work, plus private declaration contexts, remain unregistered and unreviewed. The separately accepted c418 annotations do not approve this composition. Native compilation, launch, physical/backend behavior, durable loss, first-Send and the full milestone remain open.

### Transformers prototype-family type investigation (2026-09-11)

Read-only source work confirms that the two remaining override errors reflect different real protocols: LFM2 omits required base image metadata, and MGP accepts tensor logits and returns a structured result instead of implementing the base token-batch decoder. Keeping exact concrete-base promises after their runtime-true base prototype checks is unsound. Earlier failed package models remain unchanged and unselected.

A new static language probe rejects two alternatives: constructor overloads break subclassing, and a receiver-conditional prototype predicate erases a reachable false branch. A conservative boolean precision mode passes synthetic construction, narrowing, subclass and receiver-factory controls; all 19 raw negative cases reject. Initial never-mode and factory-constraint failures remain preserved. No actual package declaration candidate has been implemented from that result, and the composed graph still fails with the original two diagnostics.

The next source model must preserve actual Callable invocation, receiver-sensitive MGP/Gemma factories, registry/component alternatives and image subclasses with additional required inputs. The small model proves none of those package-wide obligations. Author-family investigation and static standard-library compilation do not provide independent, SDK/native or runtime acceptance; all launch and full milestone holds remain unchanged.

### Private Transformers declaration composition (2026-09-11)

A fifteen-declaration private candidate now compiles the selected composed graph with zero diagnostics. Concrete image outputs, specialized image/decoder protocols, runtime-true prototype-family narrowing, receiver factories and registry alternatives remain distinct. Callable declarations follow the actual overridden method. A variadic type parameter preserves the base image method's accepted ignored arguments while preventing options-sensitive descendants from satisfying its generic method contract. No JavaScript, runtime fields, installed declarations or maintained patch bytes change.

The composed pair uses the same 12,565 input keys, with only fifteen selected declaration hashes changing. Both runs are stable and all 3,217 resolved files are pre-fenced; the result changes from two diagnostics to zero. Resolved membership matches but traversal order changes, so remaining raw-output equality is not claimed. Separate strict consumer runs cover callable/subclass/factory behavior and 114 expectation-free negative lines. Five private declaration patch steps reproduce exact before/after states. Their initial malformed-EOF check failure and later patch-format-only correction remain explicit; the composed pair retains its tested pre-format patch metadata.

Earlier alternatives remain unselected: the first actual candidate reaches composed zero but admits VitMatte through a generic method constraint; an arity-only variant rejects previously accepted ignored arguments. The final variadic form addresses both issues. Existing arbitrary mutable Processor components, generic Callable/Pipeline permissiveness and conditional generic inference remain bounded limitations, not whole-SDK safety claims. Evidence and preserved failures are in `.data/transformers-family-type-model-2026-09-11/PROVENANCE.md`.

This zero is an unreviewed private static composition containing other unregistered declaration and byte-ownership prerequisites. It does not approve those changes or the separate historical source reviews, and it does not establish installed/native execution, backend identity, physical behavior or first-Send. Independent review and maintained adoption remain ahead. The launch hold and every incomplete milestone gate remain unchanged.


### Combined Bun declaration compatibility candidate (2026-09-11)

The private Bun1.3.14 candidate now has one original-to-final composition covering TextEncoder/TLS and Console/MessageEvent declarations. Exactly three declaration files differ from the published original; final declaration bytes are unchanged from the earlier Console/MessageEvent candidate. Existing global constructor behavior, Bun TLS unions, Node methods and generic event payloads remain distinct. No runtime file or installed package changes.

The same combined final consumers run with and without DOM against both original and candidate declarations. Each pair uses the same 1,817 input keys, with only three selected declaration hashes changing: 13 and 24 original diagnostics respectively become zero. All resolved inputs are pre-fenced and stable. Expectation-free checks preserve sixteen distinct invalid-use lines across the two modes. Five private declaration patch steps reproduce all 363 package files. These are strict type/source checks; no TLS, Console, event, Worker, SDK or native runtime was exercised.

This candidate remains unregistered and awaits its own independent review. The separate Transformers review request and prior accepted source deltas do not approve it. Other private composition prerequisites, maintained adoption, all configured backend/native identity, installed/physical behavior, first-Send and the full milestone remain open under the launch hold.


### Catalog public declaration candidate (2026-09-11)

The private pi-catalog18.1.10 candidate now repairs the public models.json type target as well as its internal declaration reference. It copies the published model index contract with an extension-qualified type import and uses a type-only declaration reference from the generated models API. Runtime export targets, model JSON, provider lists and all other package fields remain unchanged.

Actual package-export resolution passes strict Bundler and NodeNext checks with JSON loading enabled and disabled. Each original/final pair has 2,820 input keys and only three selected differences, including an explicitly absent original declaration slot. The originals fail with five or four diagnostics; the final consumers pass. Resolution traces distinguish the original JSON fallback from the intended declaration, and seven inference/readonly/input controls reject in every final mode. Initial NodeNext source errors and weaker prefinal fixture checks remain preserved.

The declaration/manifest-only private patch round-trips exactly over the complete published package. No SDK module or model data was executed. The separate Bun context and this catalog candidate remain unregistered and require independent review before adoption; neither a consumer zero nor the separate Transformers review approves the broader native graph. Launch, installed/physical behavior, backend identity, first-Send and full milestone gates remain unchanged.


### ACP public stream type candidate (2026-09-11)

The private ACP candidate now checks the real package export and whole source adapter, without a pi-utils compiler path alias. It accepts both existing Node and global byte-stream contracts while preserving runtime code and the output/message contract. Strict DOM-loaded checks change from six diagnostics to zero; Node-only remains zero. Ten invalid-input/output controls still reject in each mode. These are compile-only checks, not stream delivery or ACP/native execution.

Both ACP baseline files match the published 18.1.10 archive. The copied package as a whole is not pristine: an existing unrelated env.ts change and empty Bun marker are identified separately. Neither receives acceptance through this candidate. The initial evidence-fence omission is preserved, and final checks include every resolved declaration. Independent source review and maintained adoption remain required; launch, installed/native behavior, physical acceptance, first-Send and the full milestone remain open.


### Text-asset declaration ownership candidate (2026-09-11)

New strict Node-only and DOM checks confirm that removing OMP's duplicate text-file declaration retains Bun's string type and eliminates the duplicate-export error. Thirteen negative controls still reject in each mode, including HTMLBundle remaining distinct from text. Eight real asset payloads are hashed; declaration-only controls do not prove runtime loading.

The candidate comes from the pinned upstream types/assets/index.d.ts, which is absent from the npm payload. Its earlier src/assets.d.ts path was a private compiler placement. The exact change removes 77 bytes, with all other declarations unchanged. This remains a separate unregistered source candidate awaiting independent review; no runtime, installed/native, physical, first-Send or full-milestone acceptance follows.


### Provider upload byte ownership review candidate (2026-09-11)

The three private provider upload changes are being prepared for their own source review. Gemini keeps one copied byte sequence through initialization and finalization; Anthropic and OpenAI copy exact views at their existing File/Blob construction boundary. Public input types remain broad. These are real allocations and do not promise an atomic snapshot of concurrently written shared memory or repair caller hashes made before admission.

Supplied controlled evidence changes from eight passes and six failures to fourteen passes; separate strict source checks remove three byte-boundary errors. Injected fetch and multipart models do not prove platform File/Blob behavior, live provider uploads or OAuth classification. No checks were rerun for this handoff. Other store/service/drive candidates, dependency declarations, maintained adoption, runtime/native/physical behavior, first-Send and the full milestone remain separate open gates under the launch hold.


### Blob-store byte ownership review candidate (2026-09-11)

The private registry candidate now isolates newly admitted eager bytes and each successful lazy materialization from later caller or supplier mutation. It copies the exact visible view before eager hashing or persistence awaits, and before publishing a lazy result to pending readers. Public byte inputs remain broad; null, empty, retry and eviction behavior remain distinct. These copies do not make concurrent shared-memory reads atomic or prevent direct registry clients from mutating exposed results.

Supplied controlled evidence changes from five passes and nine failures to fourteen passes. The held persistence model demonstrates agreement between the admitted hash and the bytes passed to storage; it does not execute the native session store or establish durability. Strict source compilation removes the Response body error, using an explicit limited dependency model. Final runtime maps have fourteen and fifteen inputs; compiler maps include a historical unused fixture and an extra candidate-only type probe. No checks were rerun to prepare this review.

This is a separate unregistered source candidate. Provider, service, object and drive uploads, independent review, maintained adoption, native and installed behavior, first-Send and the full milestone remain separate obligations. The launch hold remains unchanged.


### Service and fallback byte ownership review candidate (2026-09-11)

The two private service/helper changes are prepared for a separate source review. Fallback copies bytes only when a destination first requests them, then reuses the admitted copy within that attempt. Callback Response and multipart File construction copy exact visible views at their existing boundaries. Public inputs remain broad, lazy fallback behavior is unchanged, and a new deliberate attempt may obtain fresh bytes. Supplier or copy failure can still allow a later destination to retry acquisition; this is not an unconditional once-only supplier guarantee.

The supplied same-fixture controlled pair changes from eight passes and five failures to thirteen passes with ten exact shared input hashes. Inert callback, backend and multipart models establish bounded byte behavior, not live serving, provider or platform behavior. The historical compiler comparison includes three additional upload files: eleven diagnostics become two, with both compiler runs still failing. It does not establish an isolated service pass or approve those other candidates. No checks were rerun for this handoff.

Independent source review, maintained adoption and all native, installed, physical, first-Send and full milestone gates remain separate. Existing declaration and provider/blob-store review statuses do not approve this source. The launch hold is unchanged.


### Object-storage byte ownership review candidate (2026-09-11)

The private object-storage module is prepared for its own source review. S3, Azure and native B2 capture an owned visible byte sequence and request metadata before their signing or setup awaits, then use that record for body and publication reads. Public inputs and the unrelated GCS/selector code remain unchanged. This introduces copying; it does not make shared-memory reads atomic or repair previously computed caller keys.

Supplied controlled checks change from eight passes and fifteen failures to twenty-three passes, while narrow strict compilation removes three body-type errors. The actual source Azure/B2 cryptographic helpers run in the retained fixture through standard WebCrypto; S3 signing, request routing and provider replies are explicit models. Every old ownership failure stops at body equality, so later signing and metadata assertions are credited only where reached. No checks were rerun for this review preparation.

This remains a separate private candidate, with source review and maintained adoption still required. Other runtime and declaration candidates, live providers, installed/native/physical behavior, first-Send and the full milestone remain separate under the unchanged launch hold.


### Drive upload byte ownership source-review candidate (2026-09-11)

A separate private candidate captures visible bytes and request fields at Dropbox, OneDrive, Google Drive and ownCloud upload entry. The two complete baseline modules match the retained OMP18.1.10 archive; four entry-prefix changes preserve the existing protocol bodies and actual64MiB OneDrive chunk size. The original multipart helper remains unchanged and separately scoped.

The supplied whole-emission controlled fixture reports old18pass12fail versus candidate30pass0. Each runtime map has14 inputs; only the fixture and Node executable are shared absolute keys. Eleven old mutation cases accumulate body/publication failures; Google/shared stops at the controlled Blob rejection before fetch. Injected UUID mutation, guarded shared-backed Blob input and provider request models are explicitly limited evidence.

Final static/emit stages share1993 exact input hashes and each resolve177 files (185-member union). Both strict variants still fail: five diagnostics become the sole unchanged helper File/BlobPart error; emission exits2 on both. The historical report's177-map/two-hunk wording is qualified additively as1993 inputs/four hunks in two files. Earlier context-discovery failures and missing historical setup bytes remain preserved.

The separate handoff includes full sources, authored model declarations, fixture/producers/configs, all1993 final selected input copies and supplied records. It requests its own independent Standards/Spec review; delivery or other upload/declaration passes do not approve this candidate. No tests/compiler/emitter/provider/runtime or patch cycle was rerun for preparation, and no installed dependency or maintained patch changed. Launch, native/physical, first-Send and whole-GOAL acceptance remain open.


### Optional fastembed declaration-context review candidate (2026-09-11)

The separate private type context selects the unchanged published fastembed2.1.0 ESM public declaration entry and sibling, preserving six public exports and the actual enum used by pi-mnemopi's standard-model contract. This is compiler-only paths selection; it neither installs fastembed nor changes the existing optional on-demand native/model loader.

The supplied same-consumer comparison changes from three diagnostics to zero with the same catalog/Bun context candidates on both sides. Three expected-error controls retain CUSTOM, invented-model and numeric-cacheDir rejection. Historical maps contain6430 and6432 inputs, with the latter adding only the earlier output JSON/log. All450 final resolved inputs remain exact;12 unselected composed-source entries have since changed and are explicitly qualified. The compiler executable was not fenced. No compiler or consumer was rerun to prepare the handoff.

The exact declarations, configs, consumer/recorder, package provenance and full resolved context are prepared for a separate independent review. Historical composed25-to24 still fails; other published source passes do not approve this optional dependency or a coherent native graph. Maintained adoption, runtime availability, native/physical acceptance, first-Send and the full milestone remain open under the unchanged launch hold.


### Pinned Puppeteer RxJS declaration-context review candidate (2026-09-11)

The private compiler context maps Puppeteer's external RxJS type edge to the unchanged RxJS7.8.2 public declaration entry pinned by the retained Puppeteer25.3.0 lock. It includes the complete250-file declaration package, preserving the real exported types rather than supplying a stub. No bundled runtime, installed dependency or production configuration changes.

The supplied actual Connection declaration-graph check changes from four errors to zero. Both runs have439 identical selected input hashes, but resolve350 and539 files respectively;222 Node/compiler inputs were not captured at execution. Current copies of those dependencies are explicitly supplemental, not retroactive proof. No synthetic public-call consumer, negative suite or runtime event/stream check was supplied, and nothing was rerun for review preparation.

The exact declarations/configs and qualified evidence are prepared for their own independent source review. Maintained adoption, coherent native compilation, SDK/backend/physical behavior, first-Send and full milestone acceptance remain separate. Existing freezes, installed files, locks and the launch/runtime hold remain unchanged.

### Private tokenizer package declaration candidate

The private tokenizer candidate restores the full upstream schema and replaces49 leaked declaration aliases with relative package imports. The strict public DOM consumer passes; Node-only checking retains four existing TextEncoder/TextDecoder type errors. All runtime bundles and package exports remain unchanged. Six compiler records preserve the initial missing Buffer input and the final identical414-input maps. The isolated package patch cycle is exact; its earlier nested-repository setup failure remains recorded. Evidence is in `.data/tokenizer-package-declaration-repair-2026-09-11`; the separate source review packet is `.data/tokenizer-public-types-review-2026-09-11/source-001`. Review, dependency adoption, native execution and whole-goal acceptance remain separate.

### Expanded private native source graph (2026-09-11)

The new diagnostic graph checks all1,334 published OMP TypeScript/TSX roots plus two declaration roots. Initial453 diagnostics exposed mixed home17.3.4/local18.1.10 dependency resolution; exact local OMP export bindings remove those errors without source changes. Binding direct third-party dependencies to the installed OMP dependency locations preserves zero diagnostics and reduces home-resolved files from235 to one Buffer declaration. Final18,606 stable input hashes and all3,190 resolved files are retained. This remains a private source/type result, with the remaining builtin resolution, portable dependency topology, independent candidate adoption and installed/native/physical acceptance still open. Evidence and all setup/failure qualifications are in `.data/native-source-graph-expansion-2026-09-11/REPORT.md`; historical results are unchanged.

### Private builtin resolution completion (2026-09-11)

The full-source private compiler config now binds bare `buffer` to the existing Node24.13.3 builtin declaration. Identical18,613-input before/candidate maps both pass strict checking and five invalid-use controls; the sole resolved-file removal is the incidental home npm Buffer declaration. All1,334 published source roots remain checked. This is a configuration/preservation result, not behavioral RED or installed/runtime proof. The separate review packet `.data/native-build-resolution-review-2026-09-11/source-001` covers the complete source-root/dependency-binding recipe and its historical failures; candidate adoption and the full milestone remain open under the hold.

### Relocatable source-build qualification (2026-09-11)

Relocating the private build exposed a limit in the preceding `listFiles` evidence: package-identity deduplication hides 45 successful home `@opentelemetry/api` resolutions. The original trace and freeze remain unchanged, with an additive qualification delivered to the existing review. Staging also requires 51 previously unfenced package manifests and explicit internal dependency links. The earlier catalogue alias selects different bytes from the independently reviewed public-export repair.

The new private capsule binds the local OpenTelemetry package, composes the exact reviewed catalogue files and passes strict compilation outside the repository. All 3,190 listed files, 2,999 successful resolutions and 52 found package manifests remain inside the capsule; changed-input and escaping-link controls reject before compilation. Evidence and preserved failures are in `.data/native-relocatable-build-2026-09-11/REPORT.md`. This is reproducible source-build preparation, not maintained dependency adoption, an independently accepted composition, installed SDK behavior or runtime/physical acceptance. The launch hold and full completion requirements remain unchanged.

### Private source-package reconstruction (2026-09-11)

A fresh published OMP18.1.10 extraction plus the current maintained patch and nine exact reviewed upload-source selections reproduces the complete3,113-file compiled package. The original patch reverses with exit0 but leaves seven added files; this discrepancy is preserved. A separate one-diff-per-file candidate passes complete apply/reverse/reapply comparisons, including the nine published executable modes, and its source archive passes member-by-member readback. No maintained patch, dependency registration or installation changed. Evidence is `.data/native-package-composition-2026-09-11/REPORT.md`; this unreviewed package composition and the remaining dependency staging are separate from runtime, physical and whole-goal acceptance.

### Private dependency package staging (2026-09-11)

Seven complete pinned dependency packages now have private candidate archives with all4,742 members checked for exact bytes and modes. The reviewed Bun, catalogue, ACP, Transformers and tokenizer patches pass full private apply/reverse/reapply checks; RxJS and fastembed retain their published files. The existing pi-utils dotenv change remains a separate maintained prerequisite. The release asset declaration is staged at its canonical external `types/assets/index.d.ts` path, rather than inventing an npm source-file location. Evidence is `.data/native-dependency-packages-2026-09-11/REPORT.md`. Coherent resolution from these complete packages, dependency adoption and installed/native behavior remain separate gates.

The original build-resolution review810 is now Standards REJECT/Spec REJECT. The historical paragraph above describing45 home OpenTelemetry resolutions is qualified as44 unique OpenTelemetry declarations plus one string_decoder JavaScript file:54 successful lookup events across45 paths. The original trace, maps and earlier qualification remain immutable. The later private composed005 check is unreviewed preparation; its successful-resolution and package-metadata audit does not replace the original rejected freeze or confer native/runtime acceptance.

### Complete-package static source composition (2026-09-11)

The reconstructed OMP source archive and seven complete dependency archives now pass a separate strict private source check outside the repository. All1,334 source roots retain their exact bytes. The final26,522-input before/after maps are identical; all3,190 listed files,2,999 successful resolutions,79 package-metadata paths and3,077 positive file probes are internal and captured. No replaced historical package path is used. A real unrecorded package.json makes verification fail even when compilation passes; changed-source and escaping-link controls fail before compilation, and all controlled changes are restored.

Evidence and the preserved setup/verifier-refinement stages are in `.data/native-package-resolution-2026-09-11/REPORT.md`. This is an unreviewed static composition with broad captured transitive context. Original810 stays rejected; installed dependency/entry behavior, runtime, native/physical acceptance and full milestone completion remain open. No installer, maintained dependency adoption or SDK execution occurred.

The separate correction `c83f8475b5196a042abc5eab093a0c09eb224bc7cf6c945d3fc164cdd9cb36e2` is now frozen under `.data/native-complete-build-correction-2026-09-11/source-001` and submitted to the original NativeBuildResolution reviewer contexts. Its24-file scope includes all16 new preparation/build scripts;10,798 sealed records retain all26,522 final inputs through a full-path content index. Delivery is acknowledged, but review launch/completion and acceptance are not yet claimed. Original810 and all intermediate records remain immutable. The initial package composer's declared cwd field is explicitly qualified because that historical subprocess call did not pass cwd; later explicit-cwd cycles remain separate evidence.


## Private patch registration candidate (2026-09-11, d24a32ed)

The private registration candidate replaces the OMP and pi-utils patches and adds Bun, catalog, Transformers and tokenizer patches through the existing `patchedDependencies` packaging path. Only that property changes in the copied manifest. The combined pi-utils patch preserves the existing environment policy and ACP source/public declaration changes; recorded private apply/reverse/reapply reproduces the exact 328-file tree with modes. Existing packagers already copy registered patch files, so no replacement packaging mechanism is introduced.

Immutable source packet: `.data/native-patch-registration-review-2026-09-11/source-001`, delta `d24a32ed938e0e5cfe316a4b89117bf2601f4a754b6eae2ac467a68fdc2e75a5`, checksums `96890432f4441ddd3d7a34c7aab98adc44de5c287a048e63b7fb74b5309d58d4`. Thirteen scoped files and 187 sealed records. First request for a distinct Standards/Spec pair was delivered; launch/completion and acceptance are not established by delivery. c83f remains a separate original-context correction; original 810 rejections remain immutable.

No candidate lock or maintained adoption exists. Managed CLI/host select source, while upstream npm CLI remains prebuilt; worker entry selection and installed-byte binding still require explicit verification. Production install is distinct from the static declaration/asset context. The source audit and historical evidence are in the packet; no new installer/compiler/SDK/native/runtime action occurred in this registration freeze. Launch and whole-GOAL holds remain.


## Build correction receipt and lock inputs (2026-09-11)

c83f is now separately published Standards PASS / Spec PASS, zero findings in the original NativeBuildResolution contexts. Local report hashes match owner `95986c8957be63427d3d254ed3241cf93d0a500a2929ed1c944f892322de9f11` and independent `46f5fe23d8921e16ac74a447dc9d42ed727631cfdcd06a188b342a5fd4f92bd5`. This receipt supersedes pending status only; original 810 rejection/history and c83f frozen documents remain unchanged. d24a remains a distinct registration review, with no verdict recorded here.

`.data/native-lock-generation-preparation-2026-09-11/REPORT.md` records the next inert workspace input copy: four manifests, original lock, six patches, three workspaces and 556 locked package entries. A structural comparator rejects the old two-patch lock. No package manager ran and no candidate lock exists. The pinned documentation audit identifies implementation gaps for global config, dotenv and hooks; a safe invocation is not inferred from current CLI documentation. Manifest/lock/patch adoption, installed source-byte binding and runtime/native acceptance remain open under the unchanged launch hold.


## Bun install source preparation (2026-09-11)

Pinned Bun source inspection shows that install ignores the runtime `--no-env-file` flag and still loads production dotenv files. Global config remains active with an explicit config path, and a configured security scanner can run before the lock-only return despite `--ignore-scripts`. Cache failure can also fall back to a project node_modules cache. These are source findings, not executed reproductions or proof about the local binary.

`.data/bun-install-source-preparation-2026-09-11/REPORT.md` records twenty complete pinned source files, configuration/execution traces and an unexecuted private operation plan. The second plan explicitly supplies the subprocess cwd; its predecessor remains intact. No config setup, Bun invocation, candidate lock, dependency adoption or installed/runtime action occurred. d24a now has one distinct review pair launched; no verdict is recorded here. c83f remains separately published PASS/PASS, original810 remains rejected, and all launch/whole-GOAL holds remain.


## Registration publication and private lock preparation (2026-09-11)

d24a is now separately published Standards PASS / Spec PASS, zero findings; local hashes match owner eeec1a94 and independent4305eb61. Frozen pending descriptions remain historical. The private workspace's eleven inputs are unchanged, with empty private configuration and cache/temp directories prepared by the recorded Python producer. The local Bun binary matches the official GitHub and npm1.3.14 arm64 release bytes; npm gitHead differs from the GitHub source tag and is retained as a provenance limit.

Evidence: `.data/native-lock-generation-preparation-2026-09-11/execution-readiness-001.json` and `.data/bun-release-identity-2026-09-11/`. No Bun command or candidate lock has been produced. Explicit coordination is still needed for the private package-manager step under the installation/runtime hold; maintained adoption and installed/native acceptance remain separate.


## Candidate package-content comparison (2026-09-11)

A private comparator now checks an explicitly selected package against its pinned complete archive: bytes, membership and file modes. Seventeen controlled filesystem cases passed, including all7,855 members across eight candidate archives and rejection of changed/missing/extra content, member links, lost executable bits and a package root escaping its boundary. Nested dependency entries remain explicitly unverified and untraversed. The selected archive set is source-build context, not a requirement to install every development/declaration package at runtime.

Evidence: `.data/package-content-verification-2026-09-11/REPORT.md`. This new checker has no independent source verdict and has not checked a package-manager-produced installation. Maintained packaging/runtime code is unchanged. The earlier lock-only execution request remains pending; no Bun, SDK, native or installed action occurred, and all holds remain.


The comparator is now frozen as `6dc18c8552bf7a2930327750a5470231186d87c7a77dfbc2b61246e7ef68c598` under `.data/package-content-comparator-review-2026-09-11/source-001`: four new scoped files and41 sealed records. Its first request for a distinct PackageContent Standards/Spec pair was delivered. Delivery is not verified reviewer launch or acceptance. This review includes the checker, controlled fixture, report and sealing producer; existing archive sources are context. No package-manager operation or additional fixture run occurred during sealing/review dispatch. The earlier lock-only permission request and all launch holds remain pending/unchanged.


## Current first-Send ownership boundary (2026-09-11)

The new browser-only acquisition path is present, but first Send still has no browser continuation in its create command. App's beforeSubmission calls disable old actions; they do not transfer ownership. Ordinary local create omits even the draft reference, while worktree/environment creation retains its separate prepared-destination and durable native-session flow. Projectless browser admission uses the host default directory, whereas projectless session creation allocates a new workspace directory. A renderer dock rekey or observed-URL restore cannot establish the original resource relationship.

Current worker initialization is once-only, browser metadata/control dispatch retains browserOwner.id, and a newly created session runs in a fresh child with its own native registry. The candidate native supervisor's observation closure also captures original ownerSessionId; rewriting that field invalidates its observation identity check. First-Send integration therefore needs an explicit admitted lifetime/access transition that preserves the real creator/backend/page and binds the resulting session's tool and cleanup paths. Directory/setup policy must remain distinct for ordinary project, projectless and prepared worktree cases. No transfer API is selected or approved by this trace.

Exact current-source snapshots and findings are under `.data/browser-first-send-current-seam-2026-09-11/`. The desktop trace captured15 complete files with stable hashes; no App/host/SDK/native code was executed or changed. Earlier pinned primary restoration evidence remains separate. Lock-only permission is still pending, the comparator's first review request is delivered without a verdict, and all launch/native/physical/whole-GOAL holds remain.

### First-Send resource and execution contexts

Further source analysis rules out two incomplete continuations. Creating the session inside the old browser worker cannot reproduce a worktree environment established after that worker started. Forwarding complete browser runs from a fresh session to the old worker also leaves browser JavaScript in the old process: the native runtime deliberately exposes its real `process`, filesystem and module loading. Updating only cwd or forwarding nested tools does not solve that difference.

The implementation direction is a fresh session and evaluation context backed by the retained original browser resource. The current owned-channel path covers the four Puppeteer kind tags, including connected and relay; cmux has a separate original-client/surface path. Neither provides a complete cross-session lease today. All configured backends remain required. Nested calls must reach the actual new session's native tool/permission bridge, and artifacts must belong to that session. The original creator, backend and observation identity must survive unchanged.

The command audit also distinguishes browser birth revision from submitted prompt revision. Editing a draft after opening its browser must not rebind or invalidate that browser merely because its revision is older. First-Send admission needs both references, an exclusive original-create claim and a durable session/browser association. Session creation followed by failed prompt delivery must preserve that association; old draft cleanup must not destroy it. Existing command deduplication and worktree preparation provide parts of this flow, but do not implement the association.

The three author-family design analyses, corrected backend comparison and command audit are private source preparation, not independent review or implementation acceptance. The comparison captured73 complete files (2,355,317 bytes), checked46 existing source hashes and verified before/copy/after equality. The separate command capture contains10 complete files. No production API was added or enabled, and no project tests, compiler, SDK or native operation ran for this preparation. The comparator now has its one distinct pair launched, with no verdict received here. Lock-only permission and launch/physical/whole-GOAL holds remain unchanged.

## Browser run session services (2026-09-11)

The package-content comparator now has its separately published source PASS/PASS (owner eb453e3d, independent704cf581). Earlier pending-at-capture notes remain historical; this does not authorize lock-only execution or installation.

The maintained native patch now extracts a local `BrowserRunContext`: the existing run snapshot plus a callback to the actual caller's native `callSessionTool`. Worker pending runs and cmux runtime hooks consume it. Public `runInTab` and the existing cmux session/snapshot entry remain compatible; internal navigation/viewport helpers carry a snapshot without a tool callback. Browser acquisition, creator identity, target and worker lifecycle are not rebound.

This extraction is a prerequisite for the fresh-session design, not that design's implementation. Browser JavaScript still executes in the existing worker/cmux process. Cross-process services, fresh environment, durable first-Send binding, all-backend transport and cleanup authority remain required. The copied native source graph passed strict compilation; the public consumer check retains an unrelated CustomToolAdapter declaration error. Controlled routing evidence and an independent source verdict are still pending at this entry. Evidence and unchanged-prefix records are under `.data/browser-run-session-services-2026-09-11/`. No installed package, native browser/worker or retained instance was executed or changed; lock-only and launch holds remain.

Final source validation for the run-service extraction records seven controlled scenarios and ordinary repository compilation passing. The final copied native source graph also passes strict compilation; the public declaration consumer still fails at the unchanged CustomToolAdapter.execute declaration. Initial fixture parse failure and pre-refinement runs remain preserved. The six-file delta has an exact private apply/reverse/reapply cycle after the unchanged78-section prefix. The source handoff remains unreviewed; none of these results establish fresh-session execution, native permission policy, installed/IPC behavior or first-Send completion.


## Retained evaluator initialization prerequisite (2026-09-11)

The private WorkerCore patch now has an explicit `retained` mode for an original owned CDP channel and target. It requires captured screenshot/dialog policy, validates and copies initialization before awaits, rejects target replacement before readiness, avoids creation/navigation/setup replay, and preserves the page while joining channel disposal. Controlled source fixtures pass 44 cases; copied native source and the narrow public payload contract typecheck. Evidence: `.data/browser-retained-evaluation-2026-09-11/REPORT.md`; independent review pending.

This mode is not wired to first Send. Original-owner admission, a fresh evaluator process/environment, old-facade and dialog-operation drain, supervisor cleanup authority, cmux retention, durable loss/recovery and native/physical acceptance remain required. The run-services refactor has its own pending review; compilation does not approve it or any other prerequisite. Launch and installation holds remain.


## Worker dialog-action retirement (2026-09-11)

The private Core patch now tracks automatic accept/dismiss operations before dispatch, so evaluator retirement joins existing dialog work. Failures after retirement and report-callback failures remain observable through the retained drain; normal live error reporting remains. The complete controlled fixture passes 56 cases, and ordinary and copied native-source compilation pass. Evidence: `.data/browser-dialog-retirement-2026-09-11/REPORT.md`; independent review requested separately.

This closes a Core-level drain prerequisite, not the original-resource/session handoff. Parent admission and cleanup ownership, final-process environment, cmux, durable loss/recovery and first-Send/native/physical acceptance remain open. BrowserRunServices4c14 is now separately published PASS/PASS; its public TS2416 limitation remains, and earlier pending-at-capture notes are historical. No lockfile/install or launch authorization follows.


## Original cmux capability for reuse and run admission (2026-09-11)

The native supervisor privately retains each cmux tab's acquisition-time checked client and exact tab identity. Existing reuse and run paths reject replacement of that connection, kind object or retained target/facade/creator before dispatch; a fresh acquisition owns a fresh record. This supplies an original-source prerequisite for the first-Send lease without changing public APIs or native creator ownership.

Saved controlled evidence in `.data/browser-cmux-retained-source-2026-09-11/` gives current 11/0 versus old 2/9 on the same final fixture/input maps; all nine old first failures dispatch the controlled run body. Both positives pass on both sides. Ordinary compilation exits 0; the private source graph also exits 0 with listFiles-only evidence. Public replacement cleanup, actual cmux socket/native execution, parent lease, fresh-session environment, durable binding/recovery and all-backend first Send remain required. This source delta awaits its distinct independent pair; prior run-services/retained-evaluation passes and dialog-drain review remain separate. Launch and installation holds remain.


## Supervisor session-tool cleanup before evaluator handoff (2026-09-11)

The original supervisor now tracks dispatched session-tool completion separately from PendingRun. Existing cleanup bars new tool calls, aborts and joins callbacks even after a worker result removes the run, retains unreported operational/delivery errors and suppresses replies to a replacement worker. Normal live failure/cancellation replies remain. This is actual cleanup integration, not a preserve-resource lease or first-Send implementation.

Saved evidence in `.data/browser-supervisor-work-drain-2026-09-11/` records final14/0 versus original2/12 with the same fixture and selected maps, ordinary compilation0, and copied-source compilation/private patch-cycle records. Initial green11-case evidence missed two source issues later found in author-family audit; final source and three added controls are distinct and retained. Real native Bash cancellation is source-traced, not executed. Actual resource disposal, cmux execution/request drain, parent lease, fresh final-environment session creation, durable binding/recovery, native/physical and first-Send acceptance remain required. Independent review is pending; prior passes give no approval to this delta. Launch and installation holds remain.

## Cmux work retained after caller cancellation (2026-09-11)

Existing supervisor cleanup now joins the original cmux runtime, local tools, admitted guest methods and underlying client requests. Cancellation can return promptly while these remain pending; the original context stays installed, fresh runs wait for successful drain, and unreported failures stay observable. Dropped upload methods and returned handles are covered; internal drain/set/clear operations are kept off the guest facade.

Evidence in `.data/browser-cmux-run-drain-2026-09-11/` records final20/0 versus original3/17 using the same fixture and selected maps, ordinary/copy-source compilation0 and a two-file private patch cycle. Initial modeled-fixture passes, the actual run-scope regression and later type failure remain separate evidence. This does not drain arbitrary detached guest continuations or establish backend completion, original-resource lease, fresh-process evaluation, durable continuation or first Send. Independent review is pending; the supervisor and cmux admission reviews remain separate. Dialog retirement b689 is now separately published PASS/PASS, with historical pending notes preserved. Installation and launch holds remain.

### Worker process startup directory (2026-09-11, separate source review pending)

The actual host create/browser-owner/open paths now supply a canonical startup directory and matching child PWD before worker initialization. Prepared worktree environment metadata must match that directory; configured profile, relative worker entry and explicit session-storage paths preserve parent resolution. Discovery has no new per-operation startup directory. Private worker protocol is 42.

Reopen captures the original native header id/cwd and canonical directory before process creation, then compares those values before SessionManager.open and retains the post-open identity check. Relative saved cwd is explicitly refused before spawn rather than reinterpreted inside that cwd; failed admission releases the file reservation. The shared header reader retains existing title-slot and legacy parsing. This does not establish atomic filesystem identity, import exclusivity, a resource lease or first-Send continuation. Controlled extracted class/method fixtures and ordinary compilation are separate from SDK/worker/IPC/native/installed execution, which remains held. Evidence: `.data/worker-startup-context-2026-09-11/`.

### Original resource reservation (2026-09-11, implementation in progress)

The native supervisor patch and browser-only owner now have a retained reservation for evaluator handoff. It fences the original tab and name before callbacks, cancels old evaluation and joins admitted work before reporting quiescence. Explicit original-owner disposal waits for those drains and preserves the existing distinction between owned pages and attached targets. Failed or partially retired reservations cannot be silently unreserved.

This checkpoint is not yet consumed by worker IPC or either session-create path. Fresh evaluator/backend forwarding, durable continuation binding, session browser routing, renderer receipts and all-backend first-Send acceptance remain required. The native API has no independent verdict yet. `.data/browser-evaluation-reservation-2026-09-11/STATUS.md` records the current source and controlled-evidence limits. Ae1af cmux drain is now separately published PASS/PASS; d9aab startup-context has its own pending pair. Earlier pending-at-capture notes and launch/installation holds remain unchanged.


### Browser reservation worker transport (2026-09-11, implementation; independent review pending)

Private protocol 43 adds reservation and lookup-only status requests on the existing browser-only worker. The original NativeBrowserOwner keeps the resource handle and destruction authority. Worker-local receipts copy owner, PID, target and operation identity, retain up to 64 operations including failures, and permanently retain observed owner loss. Repeated pending requests for one exact operation share its wire request; the parent admits at most 64 distinct pending reserves and eight status reads. A separate disposal slot keeps shutdown dispatch available when the ordinary 128-request queue is full.

Disposal stops reservation admission and joins its tasks concurrently with original owner cleanup. It does not infer physical completion from a timer or fabricate an acknowledgement. The draft registry wraps post-dispatch ownership loss as an unknown reservation outcome; preflight refusal stays separate. Later draft text revisions preserve the original browser birth binding, while directory/owner retirement invalidates access. Status never creates, reconnects or retries a native operation.

This delta consumes the reservation interface internally but does not implement the final evaluator, either session-create path, durable first-Send command/binding recovery or renderer enablement. Native reservation f460 and startup-profile correction 4ce remain separate pending reviews. Original d9aab remains Standards PASS / Spec REJECT P1 for its frozen bytes. All installation, actual Worker/IPC/native, physical and launch holds remain. Evidence is under `.data/browser-worker-reservation-2026-09-11/`.

### Original cmux evaluation channel source checkpoint

The original ready native reservation now exposes a pinned cmux evaluation channel and joins its outstanding requests before original-resource cleanup. A fresh actual CmuxTab can restore copied URL/title/viewport/observed references and use the original client's complete facade request schemas without an extra observation or connection. Channel disposal only revokes/drains evaluation access; explicit surface.close and original resource disposition stay separate. Evidence and pending independent review: `.data/browser-reservation-backend-2026-09-11/`.

This is not the fresh-session/first-Send consumer: original/fresh worker IPC, CDP/relay channels, final-environment evaluator installation, both session-create paths, durable binding/recovery and renderer receipts remain required. Controlled40 cases and copied native compiler0 do not establish native/SDK/physical behavior; the public consumer graph retains the existing TS2416 error. No installation, launch, retained-instance action or other scope acceptance is implied.

### Original reserved CDP opening and drain source checkpoint

The original ready worker-backed reservation can allocate one channel through its captured connection owner. Pending allocation is recorded before native callbacks, repeated callers join the first opening and timeout, and failure/disposal never retries or replaces it. The readonly descriptor carries the retained target, focus/dialog policy and channel ID. Existing ParentWorkerCdpChannel frames bind one receiver; original-resource cleanup joins late allocation and channel drain before destructive resource cleanup and browser-hold release.

This channel preserves the native browser-root API. Its target ID selects retained bootstrap; it does not restrict all commands to one page. Native new-page/browser-wide actions, sibling and newly created target ownership, relay claim/group continuity and all-method session mediation require the remaining integration. The channel itself does not create a page, reopen an endpoint, install an evaluator, bind two workers or approve first Send. Evidence and independent review preparation are under `.data/browser-cdp-reservation-channel-2026-09-11/`.

Native reservation f460 and startup-profile correction 4ce are now separately published PASS/PASS; their earlier pending-at-capture notes stay historical. Worker reservation c52 has its own pair, and the cmux channel remains a separate submitted scope. No acceptance transfers between them. Installed/native/SDK/actual Worker/IPC/physical/launch, package-manager/newlock/lock-only and wholeGOAL holds remain.

### Original worker evaluation IPC source

The browser-only worker now exposes reserved CDP and CMUX evaluator access through its original WorkerClient. Opening requires an existing ready reservation; bounded retained records do not recreate a lost owner or switch backends. CDP routing is installed before native start and forwards native data/ACK/close/drained frames. CMUX uses owned bounded requests, increasing sequences and parent receipt acknowledgements; missing delivery keeps operational errors available to cleanup. Channel disposal has separate request capacity and joins actual native work while original owner/resource cleanup remains independent.

The draft registry carries its existing admission checks through these handles and permits terminal drain after admission loss. This source bridge does not yet install an evaluator in a fresh session, transfer native ownership, commit a durable source/destination binding, or enable first Send. Native CMUX/CDP prerequisite reviews remain separate; controlled fixtures and ordinary compilation do not establish real IPC, SDK, all-backend or installed behavior.
