diff --git a/dist/types/agent.d.ts b/dist/types/agent.d.ts
index 7e5954b..e558da0 100644
--- a/dist/types/agent.d.ts
+++ b/dist/types/agent.d.ts
@@ -3,7 +3,7 @@ import type { Dialect } from "@oh-my-pi/pi-ai/dialect";
 import type { HarmonyAuditEvent } from "@oh-my-pi/pi-ai/utils/harmony-leak";
 import type { AppendOnlyContextManager } from "./append-only-context.js";
 import { Tokenizer } from "./tokenizer.js";
-import type { AgentBeforeModelCall, AgentEvent, AgentLoopConfig, AgentMessage, AgentState, AgentTool, AgentToolContext, AgentTurnEndContext, AsideMessage, StreamFn, ToolCallContext, ToolChoiceDirective } from "./types.js";
+import type { AgentBeforeModelCall, AgentEvent, AgentLoopConfig, AgentMessage, AgentState, AgentTool, AgentToolContext, AgentTurnEndContext, AgentTurnReviewHooks, AsideMessage, StreamFn, ToolCallContext, ToolChoiceDirective } from "./types.js";
 export declare class AgentBusyError extends Error {
     constructor(message?: string);
 }
@@ -387,6 +387,12 @@ export declare class Agent {
     setAssistantMessageEventInterceptor(fn: ((message: AssistantMessage, event: AssistantMessageEvent) => void) | undefined): void;
     setOnBeforeYield(fn: (() => Promise<void> | void) | undefined): void;
     setOnTurnEnd(fn: ((messages: AgentMessage[], signal?: AbortSignal, context?: AgentTurnEndContext) => Promise<void> | void) | undefined): void;
+    /**
+     * Install or remove the awaited turn-review lifecycle (see
+     * {@link AgentTurnReviewHooks}). Sampled when a run starts: hooks installed
+     * mid-run take effect on the next run. `undefined` restores the plain loop.
+     */
+    setTurnReviewHooks(hooks: AgentTurnReviewHooks | undefined): void;
     /**
      * Install or replace the host pre-model-call gate; pass `undefined` to
      * remove it. Gates are sampled when a run starts: installing the first
diff --git a/dist/types/types.d.ts b/dist/types/types.d.ts
index e3bedd4..ea274d1 100644
--- a/dist/types/types.d.ts
+++ b/dist/types/types.d.ts
@@ -29,6 +29,36 @@ export interface AgentTurnEndContext {
     /** True when the current tool-loop batch is continuing without yielding to post-turn steering. */
     willContinue: boolean;
 }
+/** Terminal outcome of a run, or `running` for a settled tool batch inside a live run. */
+export type AgentTurnReviewOutcome = "running" | "completed" | "aborted" | "error";
+/** One tool call as it actually settled inside an executed batch. */
+export interface AgentTurnReviewTool {
+    callId: string;
+    name: string;
+    /** Arguments the call ran with (post `beforeToolCall` / argument transforms). */
+    args: unknown;
+    /** The result emitted with `tool_execution_end`; never-invoked calls carry synthetic details. */
+    result: AgentToolResult<unknown>;
+    isError: boolean;
+}
+export interface AgentTurnReviewSettledContext {
+    outcome: AgentTurnReviewOutcome;
+    /** Results of the batch that just settled; empty on the terminal callback. */
+    tools: AgentTurnReviewTool[];
+}
+/**
+ * Awaited turn-review lifecycle. Unlike event subscribers, both hooks are
+ * joined by the loop: `onInputsCommitted` completes before the provider call
+ * and tool dispatch of the turn that committed `messages`, `onSettled`
+ * completes before the loop proceeds past a tool batch and, with a terminal
+ * outcome, before `agent_end` is pushed. A rejection fails the run.
+ */
+export interface AgentTurnReviewHooks {
+    /** Input messages committed to the live context and emitted as `message_start`/`message_end`. */
+    onInputsCommitted(messages: readonly AgentMessage[]): Promise<void>;
+    /** A tool batch settled (`running`), or the run reached its terminal outcome. */
+    onSettled(context: AgentTurnReviewSettledContext): Promise<void>;
+}
 export interface AgentPreModelCallStop {
     /** Stop the agent loop before sending the next provider request. */
     stop: true;
@@ -453,6 +483,11 @@ export interface AgentLoopConfig extends SimpleStreamOptions {
      * Throwing surfaces as a tool-error result and does not abort the rest of the batch.
      */
     afterToolCall?: (context: AfterToolCallContext, signal?: AbortSignal) => Promise<AfterToolCallResult | undefined> | AfterToolCallResult | undefined;
+    /**
+     * Opt-in awaited turn-review lifecycle; see {@link AgentTurnReviewHooks}.
+     * Undefined keeps the loop free of any extra work.
+     */
+    turnReview?: AgentTurnReviewHooks;
     /**
      * Opt-in OpenTelemetry instrumentation. Passing `{}` enables the loop's
      * GenAI-semantic-convention spans (`invoke_agent`, `chat`, `execute_tool`)
diff --git a/src/agent-loop.ts b/src/agent-loop.ts
index 150af72..5d4b2f3 100644
--- a/src/agent-loop.ts
+++ b/src/agent-loop.ts
@@ -77,6 +77,8 @@ import type {
 	AgentToolCall,
 	AgentToolResult,
 	AgentTurnEndContext,
+	AgentTurnReviewOutcome,
+	AgentTurnReviewTool,
 	AsideMessage,
 	BeforeToolCallResult,
 	CommittableAsideMessage,
@@ -998,6 +1000,18 @@ function emitInputMessages(stream: EventStream<AgentEvent, AgentMessage[]>, mess
 	}
 }
 
+/**
+ * A turn-review hook rejected while the run was already unwinding `runError`.
+ * Neither failure may be dropped: the run error stays the headline, the hook
+ * failure rides along as the cause.
+ */
+function turnReviewFailure(runError: unknown, hookError: unknown): Error {
+	const describe = (error: unknown): string => (error instanceof Error ? error.message : String(error));
+	return new Error(`${describe(runError)} (turn review observer also failed: ${describe(hookError)})`, {
+		cause: hookError,
+	});
+}
+
 /**
  * Resolve aside entries at the moment the loop is about to inject them. Each entry
  * is either a ready {@link AgentMessage} or a sync thunk evaluated here so the
@@ -1056,12 +1070,43 @@ async function runLoopBody(
 	const softRequirementState = config.softToolRequirementState ?? { escalations: 0 };
 	let preserveSoftRequirementState = false;
 
+	// Opt-in awaited turn-review joins. Every terminal exit funnels through
+	// `end`/`finishReview` so the observer's final callback completes before
+	// `agent_end` resolves the stream; the failure unwind below reuses it.
+	const review = config.turnReview;
+	let reviewFinished = false;
+	const finishReview = async (outcome: Exclude<AgentTurnReviewOutcome, "running">): Promise<void> => {
+		if (!review || reviewFinished) return;
+		reviewFinished = true;
+		await review.onSettled({ outcome, tools: [] });
+	};
+	const end = async (outcome: Exclude<AgentTurnReviewOutcome, "running">): Promise<void> => {
+		await finishReview(outcome);
+		endAgentStream(stream, newMessages, telemetry, stepCounter.count);
+	};
+	// A user/external abort ends the run as `aborted`; a terminal tool-result
+	// abort (subagent yield) is a graceful completion.
+	const externallyAborted = (): boolean =>
+		signal?.aborted === true && signal.reason !== TERMINAL_TOOL_RESULT_ABORT_REASON;
+	// Emit committed input messages and join the observer before anything
+	// downstream (provider call, tool dispatch) can act on them. `pendingError`
+	// is the run failure already unwinding when the emit happens on an error path.
+	const commitInputs = async (messages: readonly AgentMessage[], pendingError?: unknown): Promise<void> => {
+		emitInputMessages(stream, messages);
+		if (!review || messages.length === 0) return;
+		try {
+			await review.onInputsCommitted(messages);
+		} catch (hookError) {
+			throw pendingError === undefined ? hookError : turnReviewFailure(pendingError, hookError);
+		}
+	};
+
 	let pendingMessages: AgentMessage[] = [];
 	try {
 		let messagesToEmit = [...initialMessages];
 		if (isDeadlineExceeded(config.deadline)) {
-			emitInputMessages(stream, messagesToEmit);
-			endAgentStream(stream, newMessages, telemetry, stepCounter.count);
+			await commitInputs(messagesToEmit);
+			await end("aborted");
 			return;
 		}
 		// Check for steering messages at start (user may have typed while waiting).
@@ -1071,7 +1116,7 @@ async function runLoopBody(
 			pendingMessages = signal?.aborted ? [] : (await config.getSteeringMessages?.(signal)) || [];
 		} catch (error) {
 			stream.push({ type: "turn_start" });
-			emitInputMessages(stream, messagesToEmit);
+			await commitInputs(messagesToEmit, error);
 			throw error;
 		}
 		let harmonyRetryAttempt = 0;
@@ -1098,7 +1143,7 @@ async function runLoopBody(
 		const resumeTail = unpairedToolCallTail(currentContext.messages);
 		if (resumeTail) {
 			stream.push({ type: "turn_start" });
-			emitInputMessages(stream, messagesToEmit);
+			await commitInputs(messagesToEmit);
 			messagesToEmit = [];
 			turnOpen = true;
 			const executionResult = await executeToolCalls(
@@ -1121,7 +1166,7 @@ async function runLoopBody(
 			// A tool hook may mark its completed result as terminal (e.g. subagent
 			// yield) — same stop-before-next-model-call rule as the main loop.
 			if (signal?.reason === TERMINAL_TOOL_RESULT_ABORT_REASON) {
-				endAgentStream(stream, newMessages, telemetry, stepCounter.count);
+				await end("completed");
 				return;
 			}
 		}
@@ -1133,8 +1178,8 @@ async function runLoopBody(
 			// Inner loop: process tool calls and steering messages
 			while (hasMoreToolCalls || pendingMessages.length > 0) {
 				if (isDeadlineExceeded(config.deadline)) {
-					emitInputMessages(stream, messagesToEmit);
-					endAgentStream(stream, newMessages, telemetry, stepCounter.count);
+					await commitInputs(messagesToEmit);
+					await end("aborted");
 					return;
 				}
 				// Yield at the top of each iteration to prevent busy-wait when
@@ -1198,7 +1243,7 @@ async function runLoopBody(
 				} catch (error) {
 					if (!turnOpen) {
 						stream.push({ type: "turn_start" });
-						emitInputMessages(stream, turnMessages);
+						await commitInputs(turnMessages, error);
 						turnOpen = true;
 					}
 					throw error;
@@ -1215,12 +1260,12 @@ async function runLoopBody(
 							config.onToolChoiceRejected?.();
 						} catch (error) {
 							stream.push({ type: "turn_start" });
-							emitInputMessages(stream, turnMessages);
+							await commitInputs(turnMessages, error);
 							turnOpen = true;
 							throw error;
 						}
 					}
-					emitInputMessages(stream, turnMessages);
+					await commitInputs(turnMessages);
 					if (turnOpen) {
 						const stopMessage = createGateStopMessage(preparedProviderCall.model, gateResult.reason);
 						currentContext.messages.push(stopMessage);
@@ -1240,13 +1285,13 @@ async function runLoopBody(
 						turnOpen = false;
 					}
 					preserveSoftRequirementState = !signal?.aborted;
-					endAgentStream(stream, newMessages, telemetry, stepCounter.count);
+					await end(externallyAborted() ? "aborted" : "completed");
 					return;
 				}
 
 				if (!turnOpen) {
 					stream.push({ type: "turn_start" });
-					emitInputMessages(stream, turnMessages);
+					await commitInputs(turnMessages);
 					turnOpen = true;
 				}
 
@@ -1354,8 +1399,7 @@ async function runLoopBody(
 					await emitTurnEnd(stream, currentContext, message, toolResults, config, signal, { willContinue: false });
 					turnOpen = false;
 
-					stream.push(buildAgentEndEvent(newMessages, telemetry, stepCounter.count));
-					stream.end(newMessages);
+					await end(message.stopReason === "aborted" ? "aborted" : "error");
 					return;
 				}
 
@@ -1501,7 +1545,7 @@ async function runLoopBody(
 				turnOpen = false;
 
 				if (isDeadlineExceeded(config.deadline)) {
-					endAgentStream(stream, newMessages, telemetry, stepCounter.count);
+					await end("aborted");
 					return;
 				}
 				// On external abort (user interrupt), leave the steering queue intact: the
@@ -1524,7 +1568,7 @@ async function runLoopBody(
 			}
 
 			if (isDeadlineExceeded(config.deadline)) {
-				endAgentStream(stream, newMessages, telemetry, stepCounter.count);
+				await end("aborted");
 				return;
 			}
 
@@ -1532,7 +1576,7 @@ async function runLoopBody(
 			await config.onBeforeYield?.();
 
 			if (isDeadlineExceeded(config.deadline)) {
-				endAgentStream(stream, newMessages, telemetry, stepCounter.count);
+				await end("aborted");
 				return;
 			}
 			// Skip queue drains when externally aborted (same stranding hazard as above).
@@ -1552,7 +1596,17 @@ async function runLoopBody(
 			break;
 		}
 
-		endAgentStream(stream, newMessages, telemetry, stepCounter.count);
+		await end(externallyAborted() ? "aborted" : "completed");
+	} catch (error) {
+		// The observer must still see the run settle when the loop fails or is
+		// torn down by an abort that surfaced as a throw; a rejection here must
+		// not eclipse the run error.
+		try {
+			await finishReview(externallyAborted() ? "aborted" : "error");
+		} catch (hookError) {
+			throw turnReviewFailure(error, hookError);
+		}
+		throw error;
 	} finally {
 		discardAsides(pendingMessages, new Error("Aside message was not committed before the agent loop ended"));
 		if (!preserveSoftRequirementState) {
@@ -2888,6 +2942,25 @@ async function executeToolCalls(
 		}
 	}
 
+	// Every record now carries its settled result (executed, blocked, aborted
+	// or skipped placeholder) and the `message_end` for it has been pushed.
+	// Join the observer before the caller pairs the results into the context
+	// so the batch is reported as it actually settled.
+	if (config.turnReview) {
+		const tools: AgentTurnReviewTool[] = [];
+		for (const record of records) {
+			if (!record.result) continue;
+			tools.push({
+				callId: record.toolCall.id,
+				name: record.toolCall.name,
+				args: record.args,
+				result: record.result,
+				isError: record.isError,
+			});
+		}
+		await config.turnReview.onSettled({ outcome: "running", tools });
+	}
+
 	return { toolResults: emittedToolResults };
 }
 
diff --git a/src/agent.ts b/src/agent.ts
index 275c0c2..8d8807b 100644
--- a/src/agent.ts
+++ b/src/agent.ts
@@ -10,6 +10,7 @@ import {
 	type CursorExecHandlers,
 	type CursorToolResultHandler,
 	type Effort,
+	type EventStream,
 	type ImageContent,
 	type Message,
 	type Model,
@@ -49,6 +50,7 @@ import type {
 	AgentTool,
 	AgentToolContext,
 	AgentTurnEndContext,
+	AgentTurnReviewHooks,
 	AsideMessage,
 	StreamFn,
 	ToolCallContext,
@@ -423,6 +425,7 @@ export class Agent {
 	#onHarmonyLeak?: (event: HarmonyAuditEvent) => void | Promise<void>;
 	#onBeforeYield?: () => Promise<void> | void;
 	#onTurnEnd?: (messages: AgentMessage[], signal?: AbortSignal, context?: AgentTurnEndContext) => Promise<void> | void;
+	#turnReviewHooks?: AgentTurnReviewHooks;
 	#beforeModelCall?: AgentBeforeModelCall;
 	#additionalBeforeModelCalls = new Set<AgentBeforeModelCall>();
 	#asideMessageProvider?: () => AsideMessage[] | Promise<AsideMessage[]>;
@@ -865,6 +868,14 @@ export class Agent {
 	): void {
 		this.#onTurnEnd = fn;
 	}
+	/**
+	 * Install or remove the awaited turn-review lifecycle (see
+	 * {@link AgentTurnReviewHooks}). Sampled when a run starts: hooks installed
+	 * mid-run take effect on the next run. `undefined` restores the plain loop.
+	 */
+	setTurnReviewHooks(hooks: AgentTurnReviewHooks | undefined): void {
+		this.#turnReviewHooks = hooks;
+	}
 
 	/**
 	 * Install or replace the host pre-model-call gate; pass `undefined` to
@@ -1405,6 +1416,60 @@ export class Agent {
 			return refreshToolChoiceForActiveTools(options?.toolChoice, this.#state.tools);
 		};
 
+		// Turn-review hooks are awaited by the loop right after it pushed the
+		// events they describe, but listeners (the session's persistence) only
+		// see those events once the consumer below has emitted them. `drained`
+		// resolves when every event pushed before the call has been emitted and
+		// rejects if the consumer exits first, so a join can never strand the
+		// core loop behind a dead consumer.
+		const turnReviewHooks = this.#turnReviewHooks;
+		let loopStream: EventStream<AgentEvent, AgentMessage[]> | undefined;
+		let eventsTaken = 0;
+		let eventsEmitted = 0;
+		let eventConsumerClosed = false;
+		let consumerParked = false;
+		const drainWaiters = new Set<() => void>();
+		const notifyDrainWaiters = (): void => {
+			const waiters = [...drainWaiters];
+			drainWaiters.clear();
+			for (const wake of waiters) wake();
+		};
+		const drained = async (): Promise<void> => {
+			// The loop can reach a hook synchronously inside `agentLoop()` (deadline
+			// already exceeded), before the stream handle is assigned below.
+			while (loopStream === undefined) {
+				if (eventConsumerClosed) {
+					throw new Error("Agent event consumer exited before turn-review events were delivered");
+				}
+				await new Promise<void>(resolve => drainWaiters.add(resolve));
+			}
+			// `eventsTaken` excludes an event the iterator already handed to the
+			// parked consumer whose body has not resumed yet (`push` resolves the
+			// waiter directly, and `next()` shifts the queue head synchronously).
+			// Nothing pushes while the loop awaits a hook, so the backlog is that
+			// event plus the queue.
+			const inFlight = consumerParked && loopStream.waiting.length === 0 ? 1 : 0;
+			const target = eventsTaken + inFlight + loopStream.queue.length;
+			while (eventsEmitted < target) {
+				if (eventConsumerClosed) {
+					throw new Error("Agent event consumer exited before turn-review events were delivered");
+				}
+				await new Promise<void>(resolve => drainWaiters.add(resolve));
+			}
+		};
+		const turnReview: AgentLoopConfig["turnReview"] = turnReviewHooks
+			? {
+					onInputsCommitted: async messages => {
+						await drained();
+						await turnReviewHooks.onInputsCommitted(messages);
+					},
+					onSettled: async settled => {
+						await drained();
+						await turnReviewHooks.onSettled(settled);
+					},
+				}
+			: undefined;
+
 		const config: AgentLoopConfig = {
 			model,
 			reasoning,
@@ -1475,6 +1540,7 @@ export class Agent {
 			onAssistantMessageEvent: this.#onAssistantMessageEvent,
 			onHarmonyLeak: this.#onHarmonyLeak,
 			onTurnEnd: (messages, signal, context) => this.#onTurnEnd?.(messages, signal, context),
+			turnReview,
 			getToolChoice,
 			softToolRequirementState: this.#softToolRequirementState,
 			onToolChoiceRejected: () => {
@@ -1528,8 +1594,13 @@ export class Agent {
 			const stream = messages
 				? agentLoop(messages, context, config, loopSignal, this.streamFn)
 				: agentLoopContinue(context, config, loopSignal, this.streamFn);
+			loopStream = stream;
+			if (drainWaiters.size > 0) notifyDrainWaiters();
 
+			consumerParked = true;
 			for await (const event of stream) {
+				consumerParked = false;
+				eventsTaken++;
 				if (event.type === "turn_start") turnOpen = true;
 				if (event.type === "turn_end") turnOpen = false;
 				// Update internal state based on events
@@ -1553,6 +1624,9 @@ export class Agent {
 						// If so, split the message to emit tool results at the correct position.
 						if (event.message.role === "assistant" && this.#cursorToolResultBuffer.length > 0) {
 							await this.#emitCursorSplitAssistantMessage(event.message as AssistantMessage);
+							eventsEmitted++;
+							consumerParked = true;
+							if (drainWaiters.size > 0) notifyDrainWaiters();
 							continue; // Skip default emit - split method handles everything
 						}
 						this.#state.streamMessage = null;
@@ -1581,6 +1655,9 @@ export class Agent {
 
 				// Emit to listeners
 				this.#emit(event);
+				eventsEmitted++;
+				consumerParked = true;
+				if (drainWaiters.size > 0) notifyDrainWaiters();
 			}
 
 			// Handle any remaining partial message
@@ -1704,6 +1781,8 @@ export class Agent {
 				this.#emit({ type: "agent_end", messages: [errorMsg] });
 			}
 		} finally {
+			eventConsumerClosed = true;
+			notifyDrainWaiters();
 			resolveRun?.();
 			if (this.#abortController === loopAbortController) {
 				this.#state.isStreaming = false;
diff --git a/src/types.ts b/src/types.ts
index 4392845..4adef2d 100644
--- a/src/types.ts
+++ b/src/types.ts
@@ -58,6 +58,40 @@ export interface AgentTurnEndContext {
 	willContinue: boolean;
 }
 
+/** Terminal outcome of a run, or `running` for a settled tool batch inside a live run. */
+export type AgentTurnReviewOutcome = "running" | "completed" | "aborted" | "error";
+
+/** One tool call as it actually settled inside an executed batch. */
+export interface AgentTurnReviewTool {
+	callId: string;
+	name: string;
+	/** Arguments the call ran with (post `beforeToolCall` / argument transforms). */
+	args: unknown;
+	/** The result emitted with `tool_execution_end`; never-invoked calls carry synthetic details. */
+	result: AgentToolResult<unknown>;
+	isError: boolean;
+}
+
+export interface AgentTurnReviewSettledContext {
+	outcome: AgentTurnReviewOutcome;
+	/** Results of the batch that just settled; empty on the terminal callback. */
+	tools: AgentTurnReviewTool[];
+}
+
+/**
+ * Awaited turn-review lifecycle. Unlike event subscribers, both hooks are
+ * joined by the loop: `onInputsCommitted` completes before the provider call
+ * and tool dispatch of the turn that committed `messages`, `onSettled`
+ * completes before the loop proceeds past a tool batch and, with a terminal
+ * outcome, before `agent_end` is pushed. A rejection fails the run.
+ */
+export interface AgentTurnReviewHooks {
+	/** Input messages committed to the live context and emitted as `message_start`/`message_end`. */
+	onInputsCommitted(messages: readonly AgentMessage[]): Promise<void>;
+	/** A tool batch settled (`running`), or the run reached its terminal outcome. */
+	onSettled(context: AgentTurnReviewSettledContext): Promise<void>;
+}
+
 export interface AgentPreModelCallStop {
 	/** Stop the agent loop before sending the next provider request. */
 	stop: true;
@@ -529,6 +563,11 @@ export interface AgentLoopConfig extends SimpleStreamOptions {
 		context: AfterToolCallContext,
 		signal?: AbortSignal,
 	) => Promise<AfterToolCallResult | undefined> | AfterToolCallResult | undefined;
+	/**
+	 * Opt-in awaited turn-review lifecycle; see {@link AgentTurnReviewHooks}.
+	 * Undefined keeps the loop free of any extra work.
+	 */
+	turnReview?: AgentTurnReviewHooks;
 	/**
 	 * Opt-in OpenTelemetry instrumentation. Passing `{}` enables the loop's
 	 * GenAI-semantic-convention spans (`invoke_agent`, `chat`, `execute_tool`)
